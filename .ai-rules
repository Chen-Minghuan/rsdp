# AI 协作补充规则

## 写代码前
1. 先读 `AGENTS.md` 和 `docs/05-status/当前进度.md`。
2. 涉及数据库改动时先读 `docs/02-architecture/数据库设计.md`。
3. 涉及接口变化时先读 `docs/02-architecture/API设计.md`。
4. 不确定时提问，不要猜测。

## 写代码时
1. 遵循项目中已有的代码风格。
2. 新增 public 方法必须写 JavaDoc。
3. 不要引入未在 `AGENTS.md` 中声明的新依赖；如确需引入，先向用户说明理由。
4. 修改后必须运行相关测试或 `make test`。
5. 保持最小改动原则，不要一次性重构无关模块。

## 写代码后
1. 简要说明改动点。
2. 如果有接口变化，同步更新 `docs/02-architecture/API设计.md`。
3. 更新 `docs/05-status/当前进度.md`。
