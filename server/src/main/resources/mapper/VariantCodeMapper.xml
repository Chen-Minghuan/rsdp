<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "https://mybatis.org/dtd/mybatis-3.0-mapper.dtd">
<mapper namespace="com.rsdp.mapper.VariantCodeMapper">

    <select id="allocateSequence" resultType="java.lang.Long">
        INSERT INTO variant_code_counter (rspu_id, sequence_value, updated_at)
        VALUES (#{rspuId}, 1, CURRENT_TIMESTAMP)
        ON CONFLICT (rspu_id)
        DO UPDATE SET sequence_value = variant_code_counter.sequence_value + 1,
                      updated_at = CURRENT_TIMESTAMP
        RETURNING sequence_value
    </select>

</mapper>
