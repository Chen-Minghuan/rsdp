<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
    "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.rsdp.mapper.RecommendationScoreConfigMapper">

    <select id="selectByConfigKey" resultType="com.rsdp.entity.RecommendationScoreConfig">
        SELECT
            config_id AS configId,
            config_key AS configKey,
            name,
            description,
            weights,
            is_default AS isDefault,
            is_active AS isActive,
            created_by AS createdBy,
            created_at AS createdAt,
            updated_at AS updatedAt
        FROM recommendation_score_config
        WHERE config_key = #{configKey}
        LIMIT 1
    </select>

    <update id="clearOtherDefaults">
        UPDATE recommendation_score_config
        SET is_default = false
        WHERE config_id != #{excludeConfigId}
    </update>

</mapper>
