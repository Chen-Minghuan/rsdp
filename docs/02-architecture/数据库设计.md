# RSDP 多模态录入数据库设计

> 聚焦"多模态数据录入 + AI 识别分类"阶段的数据库底座，为后续全链条工厂→销售报价打好基础。
> 部署目标：Windows 本地服务器/工作站（推荐 NVIDIA RTX 独立显卡）。
> 数据库：PostgreSQL 16+，JSON 字段统一使用 JSONB 以获得更好的查询性能和 GIN 索引支持。

---

## 一、总体 ER 关系

```
┌─────────────────┐       ┌─────────────────┐       ┌─────────────────┐
│  rspu_master    │ 1:N   │  rspu_variant   │ 1:N   │  rsku_supply    │
│  款式主档案      │◀─────▶│  款式变体表      │◀─────▶│  工厂供应子表    │
└────────┬────────┘       └────────┬────────┘       └────────┬────────┘
         │                         │                         │
         │ 1:N                     │ 1:N                     │ N:1
         ▼                         ▼                         ▼
┌─────────────────┐       ┌─────────────────┐       ┌─────────────────┐
│  rspu_style     │       │  image_assets   │       │  factory_master │
│  风格关联表      │       │  图片资源表      │       │  工厂档案        │
└─────────────────┘       └────────┬────────┘       └─────────────────┘
                                   │
                                   │ 1:N
                                   ▼
                          ┌─────────────────┐
                          │ ai_recognition  │
                          │ AI 识别记录表    │
                          └─────────────────┘
```

**辅助表**

- `rspu_scene` — RSPU 多场景关联表
- `category_dict` — 品类/风格/材质/尺寸/颜色/场景字典
- `async_task` — 异步任务
- `audit_log` — 审计日志
- `price_history` — RSKU 价格变更历史
- `user_operator` — 操作员/复核员
- `scheme` — 搭配方案主表
- `scheme_item` — 搭配方案项表

---

## 二、核心设计思想：RSPU → Variant → RSKU

家具行业的数据特点是"同款多尺寸、同色、多面料、多厂报价"。为此引入三层模型：

| 层级 | 表 | 职责 |
|:-----|:---|:-----|
| **款式概念** | `rspu_master` | 抽象款式：品类、六维标签、风格、场景、预算带 |
| **款式变体** | `rspu_variant` | 具体可售组合：尺寸 + 颜色 + 材质 |
| **工厂报价** | `rsku_supply` | 某工厂对某变体的出厂价、交期、MOQ、质量评分 |

示例：
- RSPU：`FS-MC-001` 中古风休闲椅
- Variant 1：`FS-MC-001-S-BE-PE` 小号 + 米白 + PE仿藤
- Variant 2：`FS-MC-001-L-CA-TN` 大号 + 焦糖棕 + 真藤
- RSKU：`FS-MC-001-S-BE-PE-A004` A004 厂对该变体的报价

---

## 三、核心表详细设计

### 3.1 rspu_master — 款式主档案（RSPU）

**一条记录 = 一个款式概念**，不含尺寸、颜色、材质、工厂、价格信息。

| 字段 | 类型 | 约束 | 说明 |
|:-----|:-----|:-----|:-----|
| `rspu_id` | VARCHAR(64) | PRIMARY KEY | 双层编码，如 `FS-MC-001-M` |
| `category_code` | VARCHAR(16) | NOT NULL | 类别 2 位：`FS`/`SF`/`TB`/`BS`/`OF` 等 |
| `category_path` | TEXT | NOT NULL | JSON：`["家具","座椅","休闲椅","单椅"]` |
| `positioning_label` | VARCHAR(64) | NOT NULL | 主风格/主职级，如 `中古风` / `总裁级` |
| `six_dim_tags` | JSONB | | 六维标签：`{"A":"A字架形","B":"编织镂空",...}` |
| `style_vector` | JSONB | | 512 维向量备份，主向量存 ChromaDB |
| `reference_price_band` | VARCHAR(16) | | `low` / `mid` / `high` |
| `budget_range` | JSONB | | `[800, 3500]` |
| `warranty_years` | INTEGER | | 典型质保年限 |
| `key_specs` | JSONB | | 框架材质、海绵密度等关键规格 |
| `status` | VARCHAR(16) | DEFAULT 'active' | `active` / `discontinued` / `draft` |
| `review_status` | VARCHAR(16) | DEFAULT '待复核' | `待复核` / `已确认` / `存疑` |
| `aesthetics_confidence` | VARCHAR(16) | | `high` / `mid` / `low` |
| `source_agent_version` | VARCHAR(64) | | Ollama 模型版本 |
| `created_at` | TIMESTAMP | | |
| `updated_at` | TIMESTAMP | | |
| `deleted_at` | TIMESTAMP | | 软删除 |

### 3.2 rspu_style — 多风格关联表

一个 RSPU 可属于多个风格，其中一个可标记为主风格。

| 字段 | 类型 | 约束 | 说明 |
|:-----|:-----|:-----|:-----|
| `rspu_id` | VARCHAR(64) | PK, FK | 款式 ID |
| `dict_type` | VARCHAR(32) | PK, FK | 固定为 `style` |
| `style_code` | VARCHAR(32) | PK, FK | 风格码 |
| `is_primary` | BOOLEAN | DEFAULT FALSE | 是否主风格 |
| `created_at` | TIMESTAMP | | |

### 3.3 rspu_scene — 多场景关联表

一个 RSPU 可适用于多个场景。

| 字段 | 类型 | 约束 | 说明 |
|:-----|:-----|:-----|:-----|
| `rspu_id` | VARCHAR(64) | PK, FK | 款式 ID |
| `dict_type` | VARCHAR(32) | PK, FK | 固定为 `scene` |
| `scene_code` | VARCHAR(32) | PK, FK | 场景码 |
| `created_at` | TIMESTAMP | | |

### 3.4 rspu_variant — 款式变体表

**一条记录 = 一个具体的尺寸 × 颜色 × 材质组合**。

| 字段 | 类型 | 约束 | 说明 |
|:-----|:-----|:-----|:-----|
| `variant_id` | VARCHAR(64) | PRIMARY KEY | 如 `FS-MC-001-S-BE-PE` |
| `rspu_id` | VARCHAR(64) | NOT NULL, FK | 关联 RSPU |
| `variant_code` | VARCHAR(32) | | 变体编码，如 `单人位` / `S` / `M` |
| `size_code` | VARCHAR(32) | | 尺寸码，关联字典 |
| `dimensions` | JSONB | | `{"w":560,"d":580,"h":780,"unit":"mm"}` |
| `color_code` | VARCHAR(32) | | 颜色码 |
| `material_code` | VARCHAR(32) | | 主材质码 |
| `material_mix` | JSONB | | 多种材质组合 `["实木框架","布艺座包"]` |
| `reference_price_band` | VARCHAR(16) | | 该变体参考价格带 |
| `status` | VARCHAR(16) | DEFAULT 'active' | |
| `created_at` | TIMESTAMP | | |
| `updated_at` | TIMESTAMP | | |
| `deleted_at` | TIMESTAMP | | 软删除 |

### 3.5 rsku_supply — 工厂供应子表（RSKU）

**一条记录 = 一个工厂对某个变体的报价**。

| 字段 | 类型 | 约束 | 说明 |
|:-----|:-----|:-----|:-----|
| `rsku_id` | VARCHAR(64) | PRIMARY KEY | 如 `FS-MC-001-S-BE-PE-A004` |
| `rspu_id` | VARCHAR(64) | NOT NULL, FK | 关联 RSPU |
| `variant_id` | VARCHAR(64) | FK | 关联变体 |
| `factory_code` | VARCHAR(16) | NOT NULL, FK | 工厂代码 |
| `factory_sku` | VARCHAR(64) | | 工厂原始编码 |
| `factory_price` | DECIMAL(18,2) | | 出厂价（建议 AES 加密） |
| `price_band` | VARCHAR(16) | | `low` / `mid` / `high` |
| `material_description` | TEXT | | 工厂详细材质说明 |
| `lead_time_days` | INTEGER | | 交期 |
| `moq` | INTEGER | | 最小起订量 |
| `structure_strength_rating` | VARCHAR(32) | | 家用结构 / 商用结构 / 需验证 |
| `flame_retardant_capability` | VARCHAR(32) | | 可做有案例 / 可做无案例 / 不可做 |
| `factory_photo_path` | TEXT | | 工厂实拍图路径 |
| `factory_credit_score` | INTEGER | | 履约评分 0-100 |
| `on_time_rate` | DECIMAL(5,4) | | 准时率 |
| `quality_return_rate` | DECIMAL(5,4) | | 退货率 |
| `diff_notes` | TEXT | | 差异备注 |
| `quote_confidence` | VARCHAR(16) | | `high` / `mid` / `low` |
| `review_status` | VARCHAR(16) | DEFAULT '待复核' | |
| `price_updated` | DATE | | 价格更新日期 |
| `created_at` | TIMESTAMP | | |
| `updated_at` | TIMESTAMP | | |
| `deleted_at` | TIMESTAMP | | 软删除 |

**唯一约束**：`(rspu_id, variant_id, factory_code)` 唯一。

### 3.6 price_history — 价格历史表

| 字段 | 类型 | 约束 | 说明 |
|:-----|:-----|:-----|:-----|
| `history_id` | SERIAL | PRIMARY KEY | |
| `rsku_id` | VARCHAR(64) | NOT NULL, FK | 关联 RSKU |
| `old_price` | DECIMAL(18,2) | | 旧价格 |
| `new_price` | DECIMAL(18,2) | | 新价格 |
| `changed_by` | VARCHAR(64) | | 修改人 |
| `change_reason` | TEXT | | 变更原因 |
| `created_at` | TIMESTAMP | | |

### 3.7 factory_master — 工厂档案

与上一版基本一致，补充 `deleted_at` 软删除字段。

| 字段 | 类型 | 说明 |
|:-----|:-----|:-----|
| `factory_code` | VARCHAR(16) | PRIMARY KEY |
| `factory_name` | VARCHAR(128) | 工厂全称 |
| `factory_level` | VARCHAR(8) | `S` / `A` / `B` / `C` |
| `home_commercial_tag` | VARCHAR(16) | 家用级 / 商用级 |
| `certification` | JSONB | `["BSCI","ISO9001"]` |
| `engineering_cases` | JSONB | 工程案例 |
| `region` / `address` / `contact_*` | | 地址联系人 |
| `status` | VARCHAR(16) | `active` / `suspended` / `blacklisted` |
| `deleted_at` | TIMESTAMP | 软删除 |

### 3.8 image_assets — 图片资源表

增加 `variant_id` 字段，支持三种归属：
- 款式通用图：`rspu_id` 有值
- 变体专属图：`variant_id` 有值
- 工厂实拍图：`rsku_id` 有值

| 字段 | 类型 | 说明 |
|:-----|:-----|:-----|
| `image_id` | VARCHAR(64) | PRIMARY KEY |
| `rspu_id` | VARCHAR(64) | FK |
| `variant_id` | VARCHAR(64) | FK，新增 |
| `rsku_id` | VARCHAR(64) | FK |
| `image_type` | VARCHAR(32) | `white_bg` / `factory_photo` / `detail` / `scene` / `original` |
| `storage_path` | TEXT | MinIO 或本地路径 |
| `is_primary` | BOOLEAN | 是否主图 |
| `ai_processed` | BOOLEAN | 是否已跑 AI |
| `quality_score` | DECIMAL(5,4) | 图片质量评分 |
| `uploaded_by` | VARCHAR(64) | 上传人 |
| `created_at` | TIMESTAMP | |

### 3.9 ai_recognition — AI 识别记录表

与上一版基本一致，JSON 字段统一改为 JSONB。

### 3.10 async_task / audit_log / user_operator

- `async_task`：异步任务状态跟踪
- `audit_log`：记录 INSERT/UPDATE/DELETE 的旧值和新值
- `user_operator`：操作员基础表，支撑复核人和审计日志

### 3.11 category_dict — 字典表

复合主键 `(dict_type, dict_code)`，新增 `size`、`color`、`scene` 等字典类型。

| dict_type | 说明 |
|:----------|:-----|
| `category` | 产品类别 |
| `style` | 家装风格 |
| `grade` | 办公职级 |
| `material` | 材质 |
| `six_dim_*` | 六维标签各维度 |
| `factory_level` | 工厂等级 |
| `review_status` | 复核状态 |
| `size` | 尺寸码 |
| `color` | 颜色码 |
| `scene` | 场景标签 |

---

## 四、多模态数据流转

```
1. 员工上传图片
        ↓
2. image_assets 写入记录（ai_processed=0）
        ↓
3. async_task 启动 AI 识别任务
        ↓
4. SpringBoot 异步调用本地 Ollama：
   ├─ POST /api/embeddings → 向量编码 → 写入 ChromaDB
   │                          └─ 512 维向量备份到 rspu_master.style_vector
   ├─ POST /api/generate   → Qwen 标签生成 → 写入 ai_recognition
   │                          └─ 解析到 rspu_master（风格、六维）
   │                          └─ 解析到 rspu_variant（尺寸、颜色、材质）
   └─ POST /api/generate   → 多图比对终审 → 写入 ai_recognition
        ↓
5. SpringBoot 汇总结果，更新 review_status = '待复核'
        ↓
6. 员工在 Web 界面复核：
   ├─ 确认 → review_status = '已确认'
   ├─ 修正 → 更新字段 + 写入 audit_log
   └─ 存疑 → review_status = '存疑'
```

---

## 五、风格数据库扩展（新增表）

为支持「风格数据库 Skill」，在核心产品表之外新增案例库、元素库、搭配公式库及匹配结果/反馈表。所有编码类字段复用 `category_dict` 受控词表。

```sql
-- 案例库：成功/失败的设计案例
CREATE TABLE IF NOT EXISTS style_case (
    case_id VARCHAR(64) PRIMARY KEY,
    case_name VARCHAR(128) NOT NULL,
    style_code VARCHAR(32) NOT NULL,
    room_type VARCHAR(32),
    is_success BOOLEAN NOT NULL DEFAULT TRUE,
    source_type VARCHAR(32),
    source_url TEXT,
    description TEXT,
    image_url TEXT,
    ai_raw_output JSONB,
    review_status VARCHAR(16) DEFAULT '待复核',
    created_by VARCHAR(64),
    created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
    updated_at TIMESTAMP
);

-- 元素库：从案例中拆解出的标准化元素
CREATE TABLE IF NOT EXISTS style_element (
    element_id VARCHAR(64) PRIMARY KEY,
    case_id VARCHAR(64) NOT NULL,
    element_type VARCHAR(32) NOT NULL,
    element_value VARCHAR(128) NOT NULL,
    normalized_code VARCHAR(64),
    is_primary BOOLEAN DEFAULT FALSE,
    confidence VARCHAR(16),
    notes TEXT,
    FOREIGN KEY (case_id) REFERENCES style_case(case_id)
);

-- 搭配公式库：可解释的搭配规则
CREATE TABLE IF NOT EXISTS style_matching_formula (
    formula_id VARCHAR(64) PRIMARY KEY,
    name VARCHAR(256) NOT NULL,
    style_code VARCHAR(32) NOT NULL,
    room_type VARCHAR(32),
    priority INTEGER DEFAULT 0,
    formula_json JSONB NOT NULL,
    source_case_ids JSONB,
    negative_case_ids JSONB,
    success_count INTEGER DEFAULT 0,
    fail_count INTEGER DEFAULT 0,
    status VARCHAR(16) DEFAULT 'active',
    created_by VARCHAR(64),
    created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
    updated_at TIMESTAMP
);

-- 产品-风格匹配结果：产品录入后自动计算
CREATE TABLE IF NOT EXISTS product_style_match (
    match_id SERIAL PRIMARY KEY,
    rspu_id VARCHAR(64) NOT NULL,
    style_code VARCHAR(32) NOT NULL,
    element_match JSONB,
    formula_scores JSONB,
    overall_score DECIMAL(5,4),
    confidence VARCHAR(16),
    created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
    updated_at TIMESTAMP,
    UNIQUE (rspu_id, style_code)
);

-- 推荐反馈：用于后续优化公式
CREATE TABLE IF NOT EXISTS matching_feedback (
    feedback_id SERIAL PRIMARY KEY,
    rspu_id VARCHAR(64) NOT NULL,
    recommended_rspu_id VARCHAR(64) NOT NULL,
    formula_id VARCHAR(64),
    score DECIMAL(5,4),
    feedback VARCHAR(16),
    reason TEXT,
    created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP
);

-- 索引
CREATE INDEX IF NOT EXISTS idx_style_case_style ON style_case(style_code, is_success);
CREATE INDEX IF NOT EXISTS idx_style_element_case ON style_element(case_id);
CREATE INDEX IF NOT EXISTS idx_style_element_type ON style_element(element_type, normalized_code);
CREATE INDEX IF NOT EXISTS idx_formula_style_room ON style_matching_formula(style_code, room_type, status);
CREATE INDEX IF NOT EXISTS idx_product_match_rspu ON product_style_match(rspu_id);
CREATE INDEX IF NOT EXISTS idx_product_match_score ON product_style_match(overall_score DESC);
```

导入数据格式规范见 `docs/03-guides/风格数据库导入数据格式.md`。

---

## 六、关键设计决策

| 决策 | 说明 |
|:-----|:-----|
| **RSPU / Variant / RSKU 三层分离** | RSPU 管款式概念，Variant 管尺寸/颜色/材质组合，RSKU 管工厂报价 |
| **多风格/多场景用关联表** | 避免 JSON 数组查询不便，支持按风格/场景高效筛选 |
| **变体独立成表** | 解决同一款式多尺寸、多色、多面料的问题 |
| **图片归属到三层** | `rspu_id` / `variant_id` / `rsku_id` 覆盖通用图、变体图、工厂实拍图 |
| **价格历史独立表** | 追踪报价变更，支撑后续比价分析 |
| **向量主存 ChromaDB，PG 备份** | 检索性能交给专用向量库 |
| **JSONB 替代 TEXT** | PostgreSQL 下利用 GIN 索引加速标签查询 |

---

## 七、与第二阶段（工厂→销售报价）的衔接

当前数据库已为后续报价系统预留关键字段：

| 当前表/字段 | 第二阶段用途 |
|:------------|:-------------|
| `rspu_master.reference_price_band` | 销售端预算筛选 |
| `rspu_master.budget_range` | 客户预期价格区间 |
| `rspu_variant.reference_price_band` | 变体级预算筛选 |
| `rsku_supply.factory_price` | 成本价基础 |
| `rsku_supply.lead_time_days` | 报价单交期 |
| `rsku_supply.moq` | 起订量校验 |
| `price_history` | 价格趋势分析 |
| `factory_master.factory_level` | 工厂分级定价策略 |
| `image_assets.is_primary` | 产品册/报价单主图 |

后续报价阶段只需新增：客户档案、项目档案、报价单头/行表、价格策略表即可。

---

## 七、搭配方案表

### 7.1 scheme — 搭配方案主表

用户手动搭配的产品组合，可持久化保存并再次生成报价单。

| 字段 | 类型 | 约束 | 说明 |
|:-----|:-----|:-----|:-----|
| `scheme_id` | VARCHAR(64) | PRIMARY KEY | 方案 ID，如 `SCHEME-XXXX` |
| `scheme_name` | VARCHAR(128) | NOT NULL | 方案名称 |
| `room_type` | VARCHAR(32) | | 空间类型 |
| `budget_limit` | DECIMAL(18,2) | | 预算上限 |
| `total_price` | DECIMAL(18,2) | NOT NULL DEFAULT 0 | 方案总价 |
| `factory_count` | INTEGER | NOT NULL DEFAULT 0 | 涉及工厂数 |
| `max_lead_time_days` | INTEGER | NOT NULL DEFAULT 0 | 最大交期 |
| `item_count` | INTEGER | NOT NULL DEFAULT 0 | 项数 |
| `status` | VARCHAR(16) | DEFAULT 'active' | `active` / `deleted` |
| `created_by` | VARCHAR(64) | | 创建人 |
| `created_at` | TIMESTAMP | | |
| `updated_at` | TIMESTAMP | | |
| `deleted_at` | TIMESTAMP | | 软删除 |

### 7.2 scheme_item — 搭配方案项表

| 字段 | 类型 | 约束 | 说明 |
|:-----|:-----|:-----|:-----|
| `scheme_item_id` | SERIAL | PRIMARY KEY | 自增 ID |
| `scheme_id` | VARCHAR(64) | FK | 所属方案 |
| `rspu_id` | VARCHAR(64) | FK | 款式 ID |
| `rsku_id` | VARCHAR(64) | FK | 选中的工厂报价 |
| `factory_code` | VARCHAR(16) | | 工厂代码 |
| `factory_price` | DECIMAL(18,2) | | 保存时的出厂价 |
| `lead_time_days` | INTEGER | | 保存时的交期 |
| `moq` | INTEGER | | 保存时的 MOQ |
| `sort_order` | INTEGER | DEFAULT 0 | 排序 |
| `created_at` | TIMESTAMP | | |
