## 后端测试

```bash
cd server
mvn test
```

- 单元测试：分布在 `src/test/java/com/rsdp/` 下的 `config/`、`controller/`、`event/`、`migration/`、`security/`、`service/`、`util/` 等包。
- 涉及外部 HTTP 调用（AI / ChromaDB）的测试使用 WireMock 模拟（如 `service/VisionServiceTest`、`service/chroma/ChromaDbClientTest`、`service/EmbeddingServiceTest`），无独立 `integration/` 目录。

## 前端测试

```bash
cd web
pnpm type-check
pnpm test
pnpm lint
```

- 前端已配置 Vitest：`pnpm test`（`vitest run`），已有 `src/router/router.test.ts` 等单元测试。
- `pnpm lint` 为真实 ESLint 检查（flat config，`eslint.config.js`，`eslint src --ext .ts,.vue`）。
- Playwright E2E 仍在规划中。

## 全量测试

```bash
make test
```
