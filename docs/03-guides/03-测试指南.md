## 后端测试

```bash
cd server
mvn test
```

- 单元测试：`src/test/java/com/rsdp/service/` 和 `src/test/java/com/rsdp/engine/`
- 集成测试：`src/test/java/com/rsdp/integration/`，使用 WireMock 模拟 AI 和 ChromaDB 调用。

## 前端测试

```bash
cd web
pnpm type-check
pnpm lint
```

- 当前 MVP 尚未配置 Vitest/Playwright，类型检查作为基础质量 gate。
- `pnpm lint` 当前为占位实现（ESLint flat config for Vue/TS 待补充），不会实际检查代码。
- 后续将补充单元测试：Vitest；E2E 测试：Playwright。

## 全量测试

```bash
make test
```
