## P0（近期必须完成）

- [x] 产品元数据编辑：`PUT /api/v1/products/{rspuId}`（2026-06-29 完成）
  - [x] 后端：支持部分字段更新（定位标签、颜色、材质、场景、六维标签、价格带、保修年限等）
  - [x] 后端：同步维护 `rspu_style` / `rspu_scene` 关联表
  - [x] 后端：写审计日志
  - [x] 前端：`ProductDetailView.vue` 增加「编辑元数据」弹窗
  - [x] 测试：`ProductQueryServiceTest` 补充更新用例
- [x] 更新本文档，清理过时条目（2026-06-29）
- [x] 修复 `reset_db.sql` 与 `init_db.sql` 结构不一致（2026-07-01 完成）
  - 补齐 `scheme`、`scheme_item`、`rspu_code_counter`、`variant_code_counter` 四张表
  - 补齐 `rspu_master.review_comment` 字段
  - 补齐 GIN 索引 `idx_rspu_material_tags_gin`
- [x] 修复 `AsyncTaskProcessor.processProductEntry` 长事务问题（2026-07-01 完成）
  - 移除方法级 `@Transactional`
  - 新增 `AiRecognitionPersistenceService`，数据库写入拆分为独立短事务
  - 外部 HTTP（AI / Embedding / ChromaDB）与数据库写入分离事务
- [x] 修复 `ProductQueryService.deleteProduct` 事务内调用 ChromaDB（2026-07-01 完成）
  - 新增 `RspuDeletedEvent` / `RspuDeletedEventListener`
  - ChromaDB 向量删除改为事务提交后异步执行
- [x] `FactoryMaster` JSONB 字段 `certification` / `engineeringCases` 映射修复（2026-07-01 完成）
  - Entity 加 `@TableField(typeHandler = JsonbTypeHandler.class)`
  - `FactoryCreateRequest` / `FactoryResponse` / `FactoryService` 同步透传字段
- [x] 敏感价格字段加密：`rsku_supply.factory_price`、`scheme_item.factory_price`（2026-07-01 完成）
  - 实现 `EncryptTypeHandler`（AES-256-GCM，Base64 密文）
  - DDL 改为 `TEXT`，应用层统一加解密
  - `application-dev.yml` 提供开发环境默认密钥，生产环境通过 `RSDP_ENCRYPTION_KEY` 注入
- [x] 前端修复 `ProductDetailView.vue` 同组件路由复用不刷新（2026-07-01 完成）
  - 使用 `onBeforeRouteUpdate` 重新加载
- [x] 前端限制 `QuoteBuilderView.vue` URL 产品 ID 数量（2026-07-01 完成）
  - [x] `rspuIds` 去重并截断至 `MAX_ITEMS`，超限提示
- [x] Code Review P0：修复前端 FormData 上传丢失 multipart boundary（2026-07-02 完成）
  - [x] 位置：`web/src/api/product.ts`、`web/src/api/rsku.ts`、`web/src/api/retrieval.ts`
  - [x] 删除手动设置的 `Content-Type: multipart/form-data`，让 Axios 自动生成带 boundary 的 header
  - [x] 前端 lint + type-check 通过
- [x] Code Review P0：修复 `FactoryDetailView` / `RskuDetailView` 同组件路由切换不刷新（2026-07-02 完成）
  - [x] `factoryCode` / `rspuId` / `rskuId` 改为 `computed(() => route.params.xxx as string)`
  - [x] 配合 `onBeforeRouteUpdate` 重新加载数据
  - [x] 前端 lint + type-check 通过
- [x] Code Review P0：修复 AI 搭配推荐风格过滤语义错误（2026-07-02 完成）
  - [x] 位置：`server/src/main/java/com/rsdp/service/AiMatchingService.java`
  - [x] 统一按风格码（`rspu_style.style_code`）过滤
  - [x] 同步更新 `AiMatchingServiceTest`
  - [x] 后端测试 143 个全部通过
- [x] Code Review P0：修复生产配置与代码读取路径不一致（2026-07-02 完成）
  - [x] 统一 `application-prod.yml` 使用 `rsdp.ai.*` / `rsdp.chromadb.*` 层级
  - [x] 移除 `application.yml` 中默认激活的 `spring.profiles.active: dev`
  - [x] 后端测试 143 个全部通过
- [x] Code Review P0：明文/密文价格解密兜底（2026-07-02 完成）
  - [x] `AesEncryptionUtil.decrypt` 解密失败时，对合法数字明文兜底返回并记录告警日志
  - [x] 同步增加 `AesEncryptionUtilTest` 明文兜底测试
  - [x] 后端测试 143 个全部通过
- [x] Code Review P0：RSPU/RSKU 主键与业务编码分离决策落地（2026-07-02 完成）
  - [x] 确认风格/尺寸/材质/工厂均为可变属性，不适合写死进主键
  - [x] 决策：`rspu_id` / `rsku_id` 继续使用 UUID 主键；业务编码仅作为展示字段（未来可新增 `rspu_code` / `rsku_code`）
  - [x] 多风格/多尺寸/多材质通过 `rspu_style` / `rspu_variant` / `rsku_supply` 关联表表达
  - [x] 更新 `AGENTS.md`、`docs/06-reference/01-业务规则说明.md`、新增 ADR `004-为什么RSPU和RSKU使用UUID主键而非业务编码作为主键.md`
  - [x] `RspuCodeService` 保持为可选能力，MVP 阶段不强制替代 UUID 主键

## P1（下周优先）

- [x] 锚点搭配推荐：实现 `POST /api/v1/matching/recommend` 真正推荐逻辑（2026-06-29 完成）
  - [x] 以某个 RSPU 为锚点，AI 推荐目标品类产品
  - [x] 前端 `AnchorMatchingView.vue` 接入真实接口
- [x] 搭配方案管理：保存、查询、详情、删除手动搭配方案（2026-06-29 完成）
  - [x] 后端 `scheme` / `scheme_item` 表与 CRUD 接口
  - [x] 前端 `SchemeListView.vue` / `SchemeDetailView.vue`
  - [x] `QuoteBuilderView.vue` 增加「保存为方案」
- [x] 报价单导出：支持导出 Excel 报价单（2026-06-30 完成）
  - [x] 后端：引入 EasyExcel 生成 Excel
  - [x] 前端：`QuoteBuilderView.vue` / `SchemeDetailView.vue` 增加导出按钮
- [x] 删除/软删除能力（2026-06-30 完成）
  - [x] `DELETE /api/v1/products/{rspuId}`：RSPU 软删除
  - [x] `DELETE /api/v1/sku/{rskuId}`：报价记录软删除
  - [x] 前端：列表/详情页增加删除入口（带二次确认）
- [x] 优化以图搜图性能：消除 `RetrievalService` 中的 N+1 查询（2026-07-01 完成）
  - [x] 批量查询图片 URL（`image_assets`）
  - [x] 批量查询 RSPU 元数据（`rspu_master`）
- [x] Controller 层补齐参数校验（2026-07-01 完成）
  - [x] 所有 Controller 类加 `@Validated`
  - [x] `@PathVariable String` 参数加 `@NotBlank`
  - [x] `ProductController.list` 加 `@Valid`，`ProductListRequest.size` 限制上限
  - [x] `AdminController.backfillVectors` 的 `batchSize` 加 `@Min/@Max`
  - [x] `RetrievalController.searchSimilar` 加 `@Valid`，`topK` 限制上限
  - [x] 批量图片上传加空文件 / 类型 / 大小校验
- [x] Service 层外部调用增强健壮性（2026-07-01 完成）
  - [x] `ChromaDbClient`、`EmbeddingService`、`VisionService` 配置超时与重试
  - [x] 链式 API 响应判空，避免 NPE
  - [x] 将直接 `throw new RuntimeException` 改为业务异常
- [x] 治理列表页 N+1 查询（2026-07-01 完成）
  - [x] `ProductQueryService.toSummary` 批量查询主图
  - [x] `ProductQueryService.listProducts` 风格/场景筛选改用 `EXISTS`
  - [x] `RspuRelationService.toResponse` 批量查 RSPU / 图片 / 最低价
  - [x] `RskuService.toResponse` 批量查工厂
  - [x] `QuoteService.generateQuote` 批量查 RSKU / RSPU / 工厂 / 图片
  - [x] `AiMatchingService` 批量查 candidate 最低价 / cheapest RSKU / 主图 / 工厂
- [x] 数据库与 MyBatis-Plus 一致性加固（2026-07-01 完成）
  - [x] `RspuScene` / `RspuStyle` / `CategoryDict` 复合主键字段标注 `@TableId` / `@TableField`
  - [x] `RspuMaster` / `RskuSupply` / `RspuRelation` / `RspuVariant` / `FactoryMaster` / `Scheme` 启用 `@TableLogic` 逻辑删除
  - [x] 移除 Service 层手动的 `isNull("deleted_at")` 条件
  - [x] `scheme_item.factory_code` 加外键
  - [x] `rsku_supply` 唯一索引拆分为部分唯一索引（处理可 NULL 的 `variant_id`）
  - [x] `factory_level_capability` 加“唯一主评级”部分唯一索引
  - [x] `SchemeItemMapper.insertBatch` 空集合保护
  - [x] `scheme_item.scheme_item_id` DDL 改为 `BIGSERIAL`
- [x] 业务规则边界校验（2026-07-01 完成）
  - [x] `RspuCodeService` 注入 `DictService` 校验 category/style/size 字典存在性，seq 超过 999 抛业务异常
  - [x] `RspuVariantService` 确认 `variantCodeMapper.allocateSequence` 原子分配，插入时捕获 `DataIntegrityViolationException` 兜底
  - [x] `SchemeService.create/update` 校验 `items` 非空、方案名称非空
  - [x] `FactoryService.extendCapability` 显式标注 `@Transactional(propagation = Propagation.REQUIRED)` 并补充事务语义说明
- [x] 前端边界加固（2026-07-01 完成）
  - [x] `api/client.ts` 统一判断 `ApiResult.code`
  - [x] 上传类接口覆盖更大超时
  - [x] `ProductEntryView.vue` 校验文件大小与真实 MIME 类型
  - [x] `VisualSearchView.vue` 修复 ObjectURL 泄漏，组件卸载时清理
  - [x] `QuoteBuilderView.vue` 浮点价格精度、价格为 0 显示、`MAX_ITEMS` 真正阻止加载
  - [x] 各详情页校验路由参数非空
  - [x] `FactoryListView.vue` / `SchemeListView.vue` 增加分页
- [x] 历史价格数据加密迁移（2026-07-01 完成）
  - [x] 新增 `MigrationProperties` 与 `PriceEncryptionMigration` CommandLineRunner
  - [x] 迁移 `rsku_supply` / `scheme_item` 的明文 `factory_price`，幂等执行
  - [x] 配置开关默认关闭，应用启动时按需触发
  - [x] `PriceEncryptionMigrationTest` 覆盖核心场景
- [x] 工厂能力等级在报价/推荐中落地（2026-07-01 完成）
  - [x] `QuoteService.generateQuote` 校验 RSKU 产品等级是否在工厂 `capableLevels` 内
  - [x] `AiMatchingService` 推荐最低价 RSKU 时按能力等级过滤
  - [x] `RskuResponse` 暴露 `factoryCapableLevels`，前端显示匹配状态
  - [x] 更新相关测试，后端 131 个测试通过，前端 lint + type-check 通过
- [x] 以图搜图效果优化（2026-07-01 完成）
  - [x] `RetrievalService` 图片查询时调用 `VisionService` 提取查询图 AI 视觉标签
  - [x] 按风格、主色、材质、场景、六维标签对比做重排加成
  - [x] 跨 RSPU 同款去重：按 `category + style + color + material` 指纹去重
  - [x] `SimilarProductResponse` 与前端展示 `aestheticsConfidence` 置信度
  - [x] 向量召回候选池从 `topK * 3` 扩大到 `topK * 5`
  - [x] `VisionService` 失败降级，不影响检索
  - [x] 更新 `RetrievalServiceTest`，后端 132 个测试通过，前端 lint + type-check 通过
- [x] 工厂 Excel 报价单批量导入（2026-07-01 完成）
  - [x] 新增 `RskuImportService` 与 `RskuImportController`
  - [x] EasyExcel 解析 + 数据校验 + 跳过/更新模式
  - [x] 前端 `RskuImportView.vue` 与路由
  - [x] 更新 `RskuImportServiceTest`，后端 137 个测试通过，前端 lint + type-check 通过
- [x] 产品人工批量导入（Excel）（2026-07-02 完成）
  - [x] 设计产品导入模板：RSPU 基础字段 + 变体字段 + 主图/详情图 URL
  - [x] 后端 `ProductImportService` + `ProductImportController`：EasyExcel 解析、字典码校验、RSPU/变体/图片写入
  - [x] 支持「存在时跳过」与「存在时更新」两种模式
  - [x] 前端 `ProductImportView.vue`：下载模板、上传 Excel、展示导入结果与失败明细
  - [x] 前端在「产品库」页面增加批量导入入口
  - [x] 后端单元测试覆盖成功导入、跳过、更新、校验失败、空文件、超大文件场景
- [x] 搭配方案/报价单支持产品数量选择（2026-07-02 完成）
  - [x] 后端 `scheme_item` 表增加 `quantity` 字段（默认 1）
  - [x] 后端 `SchemeItem` 实体、`QuoteService`/`SchemeService` 接入数量计算
  - [x] `QuoteResponse` 返回单价 × 数量后的分项小计与方案总价
  - [x] 前端 `QuoteBuilderView.vue`：已选产品列表支持修改数量
  - [x] 前端 `SchemeDetailView.vue` / 报价单导出 Excel 展示数量与小计
  - [x] 更新 `SchemeServiceTest`、`QuoteServiceTest`、`QuoteExportServiceTest`
- [x] Code Review P1：治理 `SchemeService.getSchemeDetail` N+1 查询（2026-07-03 完成）
  - [x] 批量查询 `RspuMaster`、`FactoryMaster`、`ImageAssets` 后再组装响应
- [ ] Code Review P1：治理 `AiMatchingService` 全表加载问题
  - [ ] 将风格/品类条件下推到 SQL，避免内存过滤
- [x] Code Review P1：治理 `FactoryService.listFactories` N+1 能力等级查询（2026-07-03 完成）
  - [x] 批量查询能力等级并按工厂分组
- [x] Code Review P1：修复 `LocalStorageService` 目录遍历风险（2026-07-03 完成）
  - [ ] 对绝对路径 `objectKey` 也做白名单校验，确保在允许根目录内
- [x] Code Review P1：修复 `RskuController.updatePrice` 未校验路径 `rspuId`（2026-07-03 完成）
  - [x] Service/Controller 中校验 `rsku.getRspuId().equals(rspuId)`
- [x] Code Review P1：`RskuImportService` 更新报价时同步写入 `price_history`（2026-07-03 完成）
  - [ ] 复用 `RskuService.updateRskuPrice` 或在导入更新分支补充价格历史
- [ ] Code Review P1：`RskuImportService` 文件类型校验改用文件魔数
  - [ ] 不再依赖客户端 `Content-Type`，结合 PK/OLE2 头或解析失败判定
- [x] Code Review P1：修复 `AsyncTaskProcessor` 向量写入失败仍标记任务成功（2026-07-03 完成）
  - [x] 明确向量失败时任务标记为 `partial_success`，`errorMessage` 提示向量写入失败
  - [x] 前端 `ProductEntryView.vue` / `types/task.ts` 支持 `partial_success` 状态展示
- [x] Code Review P1：修复 `VectorBackfillService` PG 与 ChromaDB 不一致（2026-07-03 完成）
  - [x] 调整写入顺序：先写 ChromaDB，成功后再更新 PG 的 `style_vector`
- [x] Code Review P1：`PriceEncryptionMigration` 分批查询避免 OOM（2026-07-03 完成）
  - [ ] 按主键或 `LIMIT/OFFSET` 分批处理，或改为手动运维接口
- [x] Code Review P1：新品录入与元数据更新增加字典码校验（2026-07-03 完成）
  - [x] `ProductService.createEntry` 校验 `categoryCode`
  - [x] `ProductQueryService.updateProduct` 校验风格/场景/材质字典码
- [x] Code Review P1：`AsyncConfig` 线程池队列满策略优化（2026-07-03 完成）
  - [x] 默认拒绝策略改为 `AbortPolicy`，避免 `CallerRunsPolicy` 阻塞 HTTP 线程
  - [x] 新增 `LoggingRejectedExecutionHandler`，拒绝时记录 ERROR 日志（可接入日志告警）
  - [x] 支持通过 `rsdp.async.rejected-policy` 配置切换策略
  - [x] `AdminController` 新增 `GET /api/v1/admin/async/metrics` 暴露线程池指标
- [x] 产品人工批量导入：文件类型校验改用文件魔数（2026-07-02 完成）
  - [x] `ProductImportService.validateFile` 不再依赖客户端 `Content-Type`，结合 PK/OLE2 头或解析失败判定
  - [x] 新增 `ExcelFileValidator` 工具类，覆盖 .xlsx / .xls / CSV
  - [x] 新增 `ExcelFileValidatorTest` 单元测试
- [x] 产品人工批量导入：图片 URL 增加 SSRF 防护（2026-07-02 完成）
  - [x] 新增 `ImageUrlValidator` 工具类：默认禁止 local / 内网 / 保留地址，支持 host 白名单
  - [x] `ProductImportService.downloadImages` 先校验 URL 安全再下载，不安全 URL 单独记录失败明细
  - [x] `application.yml` / `application-dev.yml` 增加 `rsdp.import.allowed-image-hosts` 配置
  - [x] 新增 `ImageUrlValidatorTest` 单元测试，更新 `ProductImportServiceTest`
- [ ] Code Review P1：治理 `UserService.listUsers` N+1 查询
  - [ ] `toResponse` 中对每个用户分别查询角色和工厂关联，列表页放大后性能差
  - [ ] 建议：在 `listUsers` 中按当前页 `userId` 集合批量查询 `sys_user_role` / `sys_user_factory`，再映射到响应对象
  - [ ] 验收：`mvn test` 通过，用户列表响应时间无明显随页数增长
- [ ] Code Review P1：JWT 角色/权限变更后已签发 token 不同步
  - [ ] 现状：token 中携带角色和权限，变更角色或权限后旧 token 仍按旧权限生效，需重新登录
  - [ ] 建议方案（优先推荐 A）：
    - A. token 版本号 + 用户表 `token_version`：签发时写入 version，过滤器对比版本号，变更角色时递增版本号使旧 token 失效
    - B. 短 token + refresh token：访问 token 有效期 15~30 分钟，refresh token 长期；权限变更可在 refresh 时重新加载
    - C. 过滤器实时查库加载权限：JWT 只存 `userId`，每次请求查库加载权限，权限变更即时生效但有查询开销
  - [ ] 待决策后落地

## P2（后续规划）

- [x] 风格数据库 Skill：表结构、导入接口、产品匹配与推荐集成
  - [x] 表结构已同步到 `init_db.sql` / `reset_db.sql`
  - [x] 种子数据：`database/seed_style_knowledge.sql`（11 正向案例 / 9 失败案例 / 247 元素 / 11 公式）
  - [x] 产品录入时自动匹配 `product_style_match` 已实现
  - [ ] ⚠️ **当前数据仅覆盖客厅（LIVING_ROOM）**：书房、卧室、餐厅等空间案例与公式待补充
  - [ ] 导入接口（Controller + Service）待实现
  - [ ] 搭配推荐 API 待实现
- [x] 以图搜图 / 同款判定：`POST /api/v1/retrieval/similar`（2026-06-30 完成）
  - [x] 接入 ChromaDB 向量写入与检索
  - [x] 图片向量化 + 三层检索（向量 + 元数据 + 规则）
  - [x] 前端 `VisualSearchView.vue` 支持以图/以文检索
- [ ] 空间校验：餐桌椅耦合、平面占位、产品组合冲突（大版本迭代后再评估）
- [x] 工厂 Excel 报价单批量导入
- [ ] PPT / 方案导出
- [x] 用户权限与登录（第一批基础改造完成）
  - [x] RBAC 数据库表与种子数据
  - [x] 后端权限字符串鉴权 + 数据权限框架
  - [x] 最小用户管理后台（列表/新增/编辑/重置密码/启用禁用）
  - [ ] 后续：角色/权限管理页面、菜单级权限、操作日志
- [ ] Code Review P2：DESIGNER 角色 `SELF_CREATED` 数据范围未落地
  - [ ] 现状：`DataScope.SELF_CREATED` 目前生成 `1=0`，设计师角色看不到任何 RSKU 数据
  - [ ] 建议：待业务确认后，either 在 `rsku_supply` 等数据表补 `created_by` 字段并在 SQL 中过滤，or 将 DESIGNER 角色的数据范围调整为 `ALL` / `FACTORY`
  - [ ] 验收：DESIGNER 角色按业务预期能看到对应数据，数据权限测试覆盖
- [ ] Code Review P2：统一 `sys_user_factory.factory_code` 字段宽度
  - [ ] 现状：`sys_user_factory.factory_code` 为 `VARCHAR(64)`，而被引用的 `factory_master.factory_code` 为 `VARCHAR(16)`
  - [ ] 建议：将 `sys_user_factory.factory_code` 改为 `VARCHAR(16)`，与 `factory_master.factory_code` 保持一致
  - [ ] 验收：`database/init_db.sql`、`database/reset_db.sql` 与实体字段宽度一致，测试通过
- [ ] 切换到本地 Ollama 推理（可选）
- [ ] 改进 `RetrievalServiceTest`：移除反射注入 `ObjectMapper`，改为构造函数注入
- [ ] 限制上传图片大小并在 Embedding 前做压缩/缩放（后端侧）
- [ ] `ChromaDbClient` 集合 ID 缓存增加失效与重试机制
- [ ] 统一 ChromaDB metadata 构建，消除 `AsyncTaskProcessor` 与 `VectorBackfillService` 重复代码
- [ ] 细化全局异常处理：区分 AI 服务异常、存储异常、系统异常
- [ ] `AdminController` 增加基础认证/授权控制（至少限制内网/管理员角色）
- [ ] 明确 `AuditLogService.insert` 事务策略（与业务同事务或独立 `REQUIRES_NEW`）
- [ ] `ImageAssets.qualityScore` 由 `Double` 改为 `BigDecimal`
- [ ] 统一 JSONB TypeHandler：`JsonbTypeHandler` 与 `PostgreSqlJsonbTypeHandler` 合并
- [ ] `AuditLog.oldValue/newValue` 类型由 `Object` 改为明确的 Map/快照类型
- [ ] 数据库时区治理：`TIMESTAMP` 升级为 `TIMESTAMPTZ`，Java 改用 `OffsetDateTime`
- [ ] 前端 DataTable `columns` 定义补充 TypeScript 泛型
- [ ] `App.vue` `router.push` 增加 NavigationFailure 处理
- [ ] `QuoteController.export` 文件名增加 UUID/随机后缀避免并发重复
- [ ] 前端 `NImage` 增加 404 fallback 处理
- [ ] 前端数字输入框补充业务上限（如 `budgetLimit` max）
- [ ] 清理逻辑删除启用后的遗留代码
  - 移除 Service 层多余的 `entity.getDeletedAt() != null` 防御性判断（`selectById`/`selectBatchIds` 已自动过滤）
  - 统一 `SchemeService.deleteScheme` 删除语义：只依赖 `@TableLogic` 的 `deleted_at`，不额外设置 `status = "deleted"`
- [ ] 补齐 `@TableLogic` 集成测试
  - 验证 `selectById`/`selectList` 自动追加 `deleted_at IS NULL`
  - 验证 `deleteById`/`updateById` 正确设置 `deleted_at = now()`
- [ ] `scheme` 软删除时级联清理 `scheme_item` 子项，避免残留孤儿记录
- [ ] Code Review P2：`DictService` 增加缓存
  - [ ] 热点字典（品类、工厂等级等）使用 `@Cacheable` 或启动预热
- [ ] Code Review P2：`AuditLogService` 写入失败告警与真实操作人
  - [ ] 接入 Spring Security 或从请求上下文获取用户/IP
  - [ ] 审计失败增加计数器/告警事件
- [ ] Code Review P2：前端统一 `SchemeItem` 类型命名
  - [ ] 区分 `types/matching.ts` 与 `types/scheme.ts` 中的同名接口
- [ ] Code Review P2：路由增加 404 回退页
  - [ ] `web/src/router/index.ts` 添加 `/:pathMatch(.*)*` 路由
- [ ] Code Review P2：`SchemeListView.vue` 改为后端分页
  - [ ] 后端 `SchemeService` 支持分页参数后前端接入
- [ ] Code Review P2：`GlobalExceptionHandler` 对 `ExternalServiceException` 返回真正 503 HTTP 状态
- [ ] Code Review P2：`RetryInterceptor` 支持 5xx 重试与指数退避
- [ ] Code Review P2：`MinioStorageService.exists` 区分“不存在”与“异常”
- [ ] Code Review P2：`StorageProperties` 敏感默认值改为空并启动校验
- [ ] Code Review P2：`RspuRelationService.createRelation` 排除已软删除关系
- [ ] Code Review P2：`RskuService.createRsku` 细化 `DataIntegrityViolationException` 处理
- [ ] Code Review P2：`VisualSearchView.vue` 切换图片时先释放旧 Object URL
- [ ] Code Review P2：`RoomSchemeView.vue` 生成报价单时携带 AI 推荐的 RSKU
- [ ] 产品人工批量导入：并发创建变体唯一索引
  - [ ] 为 `rspu_variant(rspu_id, size_code, color_code, material_code)` 增加部分唯一索引，避免并发导入产生重复变体
- [ ] 产品人工批量导入：增强 Excel 内重复行检测
  - [ ] 当同一产品在 Excel 中分别用 `rspuId` 和 `externalCode` 引用时，应识别为同一产品，避免重复处理
- [x] Code Review P1：`RskuImportService` 文件类型校验改用文件魔数
  - [x] 复用 `ExcelFileValidator`，不再依赖客户端 `Content-Type`
- [x] Code Review P2：统一 JSONB TypeHandler
  - [x] 合并 `JsonbTypeHandler` 与 `PostgreSqlJsonbTypeHandler`
- [x] 基础设施与代码使用不一致：Redis
  - [x] 接入 Spring Cache，生产环境使用 Redis，开发环境回退 ConcurrentMap
- [x] 文档与 DDL 不同步：风格数据库
  - [x] 在 `init_db.sql` / `reset_db.sql` 中补充 `style_case` / `style_element` / `style_matching_formula` 等表
- [x] 数据库脚本细节一致性
  - [x] 统一 `init_db.sql` 文档引用路径

## 业务边界问题治理（2026-07-02 全项目 review）

> 以下问题按业务风险与修复优先级分组，建议从上到下依次处理。

### P0 — 立即处理（安全、合规、核心数据一致性）

- [x] 引入身份认证与授权体系（2026-07-03 完成）
  - [x] 后端增加 `spring-boot-starter-security` + JWT 登录
  - [x] 写接口按角色要求认证，`operator` 从登录上下文获取
  - [x] 替换 `AuditLogService` 硬编码 `"admin"`（后续可逐步清理调用点入参）
- [x] CORS 配置改为具体域名白名单（2026-07-06 完成）
  - [x] 删除 `WebMvcConfig`，统一由 `SecurityConfig` 按 `RSDP_ALLOWED_ORIGINS` 管理
  - [x] 启用 `allowCredentials(true)` 以支持 HttpOnly Cookie
- [ ] 清理生产环境硬编码密钥与弱默认密码
  - [ ] 删除 `application-dev.yml` 硬编码 AES 密钥，开发环境也要求环境变量注入
  - [ ] `deploy/docker-compose.yml` PostgreSQL/MinIO 密码改为环境变量读取
  - [ ] `.env.example` 中 `RSDP_ENCRYPTION_KEY` 不再填充真实密钥
- [ ] `price_history` 价格字段改为 AES 加密存储
  - [ ] DDL 将 `old_price/new_price` 改为 `TEXT`
  - [ ] `PriceHistory` entity 增加 `EncryptTypeHandler`
- [ ] 审计快照中敏感价格字段脱敏
  - [ ] `AuditLogService` 序列化 `RskuSupply` 等对象前，对 `factoryPrice` 等字段脱敏或二次加密
- [ ] `ImageUrlValidator` SSRF 防护加固
  - [ ] 未解析域名默认拒绝
  - [ ] 关闭 `URLConnection` 自动重定向，限制重定向次数
  - [ ] scheme 统一小写校验，限制端口为 80/443/常见图片 CDN 端口
  - [ ] 下载连接阶段重新校验解析后的 IP

### P1 — 本周处理（核心业务规则一致性）

- [ ] RSKU 导入排除已软删除报价
  - [ ] `RskuImportService.preloadExisting` 增加 `deleted_at IS NULL` 过滤
- [ ] RSKU 导入更新报价时同步记录 `price_history`
  - [ ] `RskuImportService.updateExistingRsku` 写入价格历史，与单条更新语义一致
- [ ] RSKU 价格必须大于 0
  - [ ] `RskuCreateRequest` / `RskuPriceUpdateRequest` 将 `@PositiveOrZero` 改为 `@Positive`
- [ ] 核心受控字典字段增加服务层强校验
  - [x] `ProductService.createEntry` 校验 `categoryCode`（2026-07-03 完成）
  - [x] `ProductQueryService.updateProduct` 校验 `positioningLabel`/`sceneTags`/`productLevel`（2026-07-03 完成）
  - [ ] `ProductQueryService.reviewProduct` 校验 `reviewStatus` 枚举
  - [ ] `RskuImportService` 校验 `quoteConfidence`
- [ ] RSPU 软删除时级联处理关联数据
  - [ ] 级联软删除 `rspu_variant`、`rsku_supply`、`image_assets`、`rspu_style`、`rspu_scene`、`rspu_relation`
  - [ ] 或至少在查询/搭配方案层增加已删除校验
- [ ] 变体创建增加唯一性校验
  - [ ] `RspuVariantService.createVariant` 校验同一 RSPU 下 `(size_code, color_code, material_code)` 不重复
  - [ ] 数据库补充唯一索引兜底
- [ ] AI 视觉识别输出约束
  - [ ] `OpenAiChatRequest` 增加 `response_format: json_object` 与合理 `max_tokens`
  - [ ] 解析失败时兜底返回空标签或失败状态
- [ ] ChromaDB 向量维度固定
  - [ ] `ChromaDbProperties` 配置维度，`createCollection` 时显式声明
- [ ] Redis 缓存故障降级
  - [ ] `CacheConfig` 注册 `CacheErrorHandler`，Redis 异常时降级查库并告警

### P2 — 短期治理（数据质量、稳定性、可维护性）

- [ ] 补齐 `@TableLogic` 软删除集成测试
  - [ ] 验证查询/删除/更新行为符合预期
  - [ ] 若版本不兼容，评估改为数值型删除标记
- [ ] 同步数据库设计文档与实现
  - [ ] 修正 `factory_price`/`scheme_item.factory_price` 类型为加密 TEXT
  - [ ] 修正 `rspu_id` 为 UUID 主键，删除过时双层编码示例
- [ ] `image_assets` 增加软删除字段
  - [ ] DDL 增加 `deleted_at`，entity 启用 `@TableLogic`
  - [ ] 统一走逻辑删除，避免 `ai_recognition` 外键硬删除冲突
- [ ] 导入更新模式下空单元格不覆盖已有字段
  - [ ] `RskuImportService.updateExistingRsku` 仅更新非 null 字段
  - [ ] `ProductImportService.updateRspu` 仅更新 Excel 中显式有值字段
- [ ] 导入事务粒度拆分
  - [ ] `RskuImportService.importRskus` 改为按行/小批次独立事务，避免整批回滚
- [ ] 异步任务增加任务级超时
  - [ ] `AsyncTaskProcessor` 使用 `Future.get(timeout)` 或 `CompletableFuture.orTimeout`
- [ ] AI 低置信度结果业务分支
  - [ ] `confidence=low` 时设置 `reviewStatus=存疑` 或状态保持待复核
- [ ] 以图搜图增加相似度阈值过滤
  - [ ] `RetrievalService` 对 `finalScore` 低于阈值的候选直接丢弃
- [ ] 产品下架/删除时同步 ChromaDB 向量
  - [ ] `RspuDeletedEventListener` 扩展向量删除/状态更新
  - [ ] 检索重排后二次查询 PostgreSQL 校验状态

### P3 — 中期优化（健壮性、用户体验）

- [ ] 批量插入 XML 字段补全
  - [ ] `RskuSupplyMapper.xml` / `SchemeItemMapper.xml` insertBatch 补齐所有列
- [ ] 工厂代码软删除后重建策略
  - [ ] 创建前显式查询含已删除记录，提供恢复或强制新编码策略
- [ ] 工厂主等级统一归一化为 `dictCode`
  - [ ] 拒绝中文名，或在校验层做名称→编码转换
- [ ] 图片下载后真实内容与魔数校验
  - [ ] `ProductImportService.downloadSingleImage` 校验文件头、MIME 白名单、最大尺寸
- [ ] 报价单导出防御 null 项
  - [ ] `QuoteExportService` 过滤/校验 `quoteItems` 中的 null 元素
- [ ] 搭配方案预算上限校验
  - [ ] `SchemeService.create/update` 校验 `totalPrice` 不超过 `budgetLimit`

## 代码缺陷治理（2026-07-02 全项目代码 review）

> 以下问题按开发落地顺序与风险等级分组。Ollama 相关项因项目稳定后再启用，本次暂不纳入。

### P0 — 立即修复（代码级 bug / 部署阻塞 / 前端运行时风险）

- [ ] 修复 Docker 构建 JAR 名称不匹配
  - [ ] `server/Dockerfile` 改为复制 `target/rsdp-server-*.jar`，或在 `pom.xml` 中设置 `<finalName>rsdp-backend</finalName>`
- [ ] docker-compose 向 Server 注入 `DASHSCOPE_API_KEY`
  - [ ] `deploy/docker-compose.yml` 的 `server.environment` 增加 `- DASHSCOPE_API_KEY=${DASHSCOPE_API_KEY}`
- [ ] Makefile / 脚本连接 PostgreSQL 使用正确端口
  - [ ] `Makefile`、`scripts/setup.sh`、`scripts/backup_db.sh` 中的 `psql`/`pg_dump` 增加 `-p 5433`
  - [ ] `setup.sh` 补充 `export PGPASSWORD`，避免非信任认证环境下挂起
- [ ] 修复 `QuoteBuilderView` ref 对象属性直接 `v-model` 导致的响应式丢失
  - [ ] `selectedRskuMap` / `quantityMap` 改用 `reactive` 或在模板中使用 `computed` 包装
- [ ] 前端组件卸载时取消进行中的异步请求
  - [ ] ProductDetailView、FactoryDetailView、RskuDetailView、QuoteBuilderView、SchemeDetailView、ProductListView 等统一使用 `AbortController`
- [ ] 释放 `VisualSearchView` 旧 Object URL
  - [ ] `handleFileChange` 中创建新 URL 前先 `URL.revokeObjectURL(previewUrl.value)`
- [ ] 修复 `ProductEntryView` 轮询并发
  - [ ] `setInterval` 改为 `setTimeout` 链式调用，或增加 `isPolling` 锁
- [ ] 前端 RSKU / 变体表单字段与后端接口类型对齐
  - [ ] `ProductDetailView.vue` 移除 `rskuForm.productLevel` / `variantForm.productLevel`，或确认后端接口支持
- [ ] 修复 `RskuController.updatePrice` 重复查询 RSKU
  - [ ] 将 `rspuId` 归属校验下沉到 Service，或复用查询结果
- [ ] 修复 `JsonbTypeHandler` 空值处理与类型判断
  - [ ] 重写 `setNullParameter`，显式设置 `PGobject.setValue(null)` 或 `ps.setNull(i, Types.OTHER)`
  - [ ] `isStringType()` 使用 `instanceof Class<?>` 判断，避免泛型字段误判
- [ ] 修复 `ProductQueryService.listProducts` 手动拼接 JSON 字符串
  - [ ] 使用 `ObjectMapper.writeValueAsString` 生成 JSON，避免非法转义
- [ ] 修复 `PriceEncryptionMigration` 日志泄露明文价格
  - [ ] 日志中仅打印记录 ID 与错误类型，不输出 `rawPrice`
- [x] 修复 `FactoryService.getFactoryCapableLevels` 对不存在工厂抛异常导致列表失败（2026-07-03 完成）
  - [x] 不存在时返回空列表，由调用方决定是否抛异常
- [x] Code Review P1：`selectBatchIds` 空列表保护（2026-07-03 完成）
  - [x] `RskuService` / `QuoteService` / `RetrievalService` / `RspuRelationService` 空列表时跳过批量查询
  - [x] 新增 `RskuServiceTest` 无 RSKU 场景覆盖
- [x] 前端新品录入页加固（2026-07-03 完成）
  - [x] `AbortController` 取消未完成请求
  - [x] 任务列表 `localStorage` 持久化
  - [x] `setInterval` 改为 chained `setTimeout`
  - [x] `beforeunload` 离开确认提示
- [ ] 修复 `AsyncTaskProcessor` 多任务竞争更新同一任务记录
  - [ ] 进入处理前使用数据库乐观锁（仅 `status = pending` 才更新为 `processing`）

### P1 — 本周处理（架构级治理 / 数据一致性 / 测试补齐）

- [ ] UUID 主键生成策略改为完整 UUID 或雪花算法
  - [ ] 替换所有 `UUID.randomUUID().toString().substring(0, 8)` 的短 ID 生成
  - [ ] 或在插入时检测主键冲突并重试
- [ ] 统一所有列表/预加载查询过滤 `deleted_at IS NULL`
  - [ ] `ProductQueryService.listProducts`、`RskuService.listByRspu`/`listByFactory`、`RspuVariantService.listVariantsByRspu`、`AiMatchingService.fetchCandidates`、`RspuRelationService.listByAnchor` 等
- [ ] 后端资源流关闭
  - [ ] `VisionService.recognizeImage`、`EmbeddingService.embedImage` 在 `finally` 中关闭输入流
- [ ] 异常处理规范化
  - [ ] `ChromaDbClient.getOrCreateCollectionId` 区分 404 与其他异常
  - [ ] `MinioStorageService.exists` 仅对“不存在”返回 false，其他异常抛出
  - [ ] `AuditLogService.insert` 异常通过事务同步/异步队列兜底，不再在业务事务内静默吞掉
  - [ ] `RskuImportService.parseExcel` / `ProductImportService.parseExcel` 将原始异常作为 cause 包装
- [ ] 前端路由参数响应式改造
  - [ ] `QuoteBuilderView.editSchemeId` 与 `SchemeDetailView.schemeId` 改为 `computed`，并 `watch` 重新加载
- [ ] 前端基础设施补齐
  - [ ] 路由增加 404 兜底页与全局导航守卫
  - [ ] API 层支持 `AbortSignal` 请求取消
  - [ ] `ProductListView` 筛选变化增加防抖
  - [ ] 表格 columns 统一添加 `DataTableColumns<T>` 泛型
  - [ ] 统一 `deleteRsku` API 路径
  - [ ] 确认 `vite.config.ts` proxy 与 axios baseURL 的 `/api` 前缀不重复
- [ ] 测试覆盖补齐
  - [ ] 为核心 Controller 增加 `@WebMvcTest` 测试
  - [ ] 为 `EmbeddingService`、`ChromaDbClient` 增加 WireMock / `MockRestServiceServer` 测试
  - [ ] 为 `MinioStorageService` 增加 Mockito 单元测试
  - [ ] 引入少量 `@SpringBootTest` 集成测试（可结合 H2 或 Testcontainers）
- [ ] 部署配置治理
  - [ ] 统一 `docker-compose.yml` 与 `.env.example` 变量名与来源
  - [ ] 将 ollama/chromadb/minio 镜像版本从 `latest` 锁定到具体版本
  - [ ] 为 postgres/redis/chromadb/minio/server 增加 `healthcheck`，`depends_on` 使用 `condition: service_healthy`
  - [ ] `reset_db.sql` 补充删除 `rspu_code_counter` 与 `variant_code_counter`
- [ ] 引入 Spring Boot Actuator
  - [ ] `pom.xml` 增加 `spring-boot-starter-actuator`
  - [ ] 暴露 `health`、`info`、`metrics` 端点

### P2 — 短期优化（可维护性、工程规范）

- [ ] 抽取业务常量枚举，消除魔法值
  - [ ] `"active"`、`"processing"`、`"待复核"`、`"low"/"mid"/"high"`、`"admin"` 等统一为枚举或常量类
- [ ] 完善审计日志体系
  - [ ] 覆盖 `scheme`、`factory_warehouse`、`factory_level_capability`、`rspu_style`、`rspu_scene` 等写操作
  - [ ] 审计失败时增加计数器/告警事件
- [ ] 建立 CI/CD 流水线
  - [ ] 新增 `.github/workflows`，执行 `make test`、构建 Docker 镜像
  - [ ] 增加依赖安全扫描（OWASP dependency-check / npm audit）
- [ ] 前端 Pinia Store 重构
  - [ ] 创建 `web/src/stores/`，将字典缓存、用户会话、全局 loading 等从视图组件提取
- [ ] 前端输入与下载体验优化
  - [ ] `ProductDetailView` JSON 字段解析后校验结构
  - [ ] `VisualSearchView` 增加文件类型/大小前端校验
  - [ ] `ProductEntryView` 任务结果增加具体类型与字段保护
  - [ ] `ProductImportView` / `RskuImportView` 模板下载改用 `<a download>` 或 fetch blob
  - [ ] `client.ts` 统一全局错误提示（结合 Naive UI `useMessage`）
- [ ] `RskuPriceUpdateRequest` 增加 `@Digits(integer=8, fraction=2)` 限制小数位
- [ ] 增加测试覆盖率统计
  - [ ] `pom.xml` 引入 `jacoco-maven-plugin`
  - [ ] `make test` 输出覆盖率报告

## 全项目 Code Review 补充（2026-07-03）

> 本次 Review 覆盖后端 Java、前端 Vue/TS、数据库/部署配置、核心业务规则四个维度。与上方 2026-07-02 条目重复的内容不再罗列，以下仅补充新发现的问题。

### P0 — 立即处理

- [x] 修复 `SecurityConfig` 对所有 GET 请求默认公开（2026-07-06 完成）
  - [x] 删除 `.requestMatchers(HttpMethod.GET, "/api/v1/**").permitAll()`
  - [x] 对价格历史、方案详情、工厂报价、任务状态等敏感 GET 接口显式授权
- [x] 把数据权限扩展到所有读价格链路（2026-07-06 完成）
  - [x] `QuoteService.generateQuote` 校验 RSKU 工厂访问权
  - [x] `SchemeService.getSchemeDetail` / `generateQuote` 校验 RSKU 工厂访问权
  - [x] `RspuRelationService.batchMinPrices` 过滤无权限工厂
  - [x] `PriceHistoryController` 查询前校验当前用户对 RSKU 所属工厂权限
- [x] 引入 JWT token_version 机制（2026-07-06 完成）
  - [x] `sys_user` 增加 `token_version` 字段
  - [x] 签发时写入 JWT，过滤器校验版本号一致
  - [x] 修改角色/权限、禁用用户时递增版本号，使旧 Token 失效
- [x] 禁止管理员禁用/修改自己（2026-07-06 完成）
  - [x] `UserService.updateStatus` 校验不能禁用当前登录用户
  - [x] `UserService.updateUser` 禁止修改当前用户自己的角色
- [x] 前端权限不要依赖 localStorage（2026-07-06 完成）
  - [x] `stores/user.ts` 的 `roles`/`permissions` 从 `/auth/me` 获取后放内存
  - [x] 路由守卫进入 `/admin/users` 等敏感页前调后端接口二次校验
- [x] 前端 JWT 改为 HttpOnly Cookie 存储（2026-07-06 完成）
  - [x] 后端登录接口 `Set-Cookie: token=...; HttpOnly; Secure; SameSite=Strict`
  - [x] 前端移除 localStorage 中的 `rsdp:token`/`rsdp:user` 持久化
- [x] 修复前端用户管理编辑保存失败（2026-07-06 完成）
  - [x] `UserManagementView.vue` 创建/编辑使用不同 rules，编辑时密码可选
- [x] 修复产品详情 JSON 字段类型不一致（2026-07-06 完成）
  - [x] 后端 `RspuMaster` 等字段改为 `@JsonRawValue` 或 Object/Map/List
  - [x] 或前端在 `getProductDetail` 返回后统一 `JSON.parse` 适配
- [x] 修复导入模板下载 401（2026-07-06 完成）
  - [x] `ProductImportView.vue` / `RskuImportView.vue` 用 Axios `responseType: 'blob'` 下载
- [x] DESIGNER 角色数据范围按 ALL 只读处理（2026-07-06 完成）
  - [x] `DataScopeContext` 对 `DESIGNER` 返回 `DataScope.ALL`
- [x] RSKU 新增 `material_code` 字段，落地材质版本码（2026-07-06 完成）
  - [x] `rsku_supply` 表已有 `material_code VARCHAR(8)`
  - [x] 实体、DTO、导入模板、Service 校验同步补充
  - [x] 前端 `ProductDetailView.vue` / `RskuDetailView.vue` 展示与录入
- [x] SSRF 图片下载防护加固（绕过点）（2026-07-06 完成）
  - [x] `ImageUrlValidator` 未解析域名默认拒绝
  - [x] 拒绝带 `userinfo` 的 URL（如 `http://10.0.0.1@example.com`）
  - [x] `ProductImportService.downloadSingleImage` 关闭自动重定向
- [x] 修复 `sys_user_factory.factory_code` 与 `factory_master.factory_code` 宽度不一致（2026-07-06 完成）
  - [x] 统一为 `VARCHAR(16)`
- [x] Docker Compose 无认证基础设施直接暴露宿主机（2026-07-06 完成）
  - [x] 仅 Nginx 暴露 80/443；Ollama/ChromaDB/Redis/MinIO 只在内部网络
  - [x] Redis 设置 `requirepass`；ChromaDB 预留 token 认证配置入口
- [x] 清理硬编码默认密码与密钥（2026-07-06 完成）
  - [x] `deploy/docker-compose.yml` 所有密码/密钥改为 `${VAR}` 引用
  - [x] `.env.example` 敏感字段使用 `<CHANGE_ME>`
  - [x] `scripts/start-local.sh` 读取 `.env`，缺失时退出
- [x] Nginx 生产配置启用 HTTPS 与安全头（2026-07-06 完成）
  - [x] 配置 443 SSL，80 强转 HTTPS
  - [x] 添加 HSTS、X-Frame-Options、X-Content-Type-Options、Referrer-Policy
- [x] Code Review 高风险项修复（2026-07-06 完成）
  - [x] JWT Filter 增加 HttpOnly Cookie 回读，修复 Cookie 认证链路
  - [x] `SecurityConfig` 修正 `/api/v1/products/import-template` 权限规则顺序
  - [x] `AiApiConfig` 支持 `rsdp.chromadb.auth-token`，启用认证时自动携带 Authorization 头
  - [x] `deploy/docker-compose.yml` Postgres 增加 `127.0.0.1:5433:5432` 本地开发映射
  - [x] `deploy/docker-compose.yml` `server` 服务端口限制为 `127.0.0.1:8081:8081`
  - [x] `scripts/start-local.sh` 强制 `RSDP_JWT_COOKIE_SECURE=false`
  - [x] `scripts/start-local.bat` 改为读取 `deploy/.env`、校验占位符、不再硬编码密钥
  - [x] `LoginView.vue` 登录成功后调用 `fetchUserInfo()` 拉取用户信息
  - [x] `deploy/nginx/nginx.conf` `/api` 增加 `proxy_read_timeout 300s` 等长请求超时
  - [x] `Makefile` `test` 目标在检查前先 `pnpm install`
- [x] DESIGNER 角色数据范围按 ALL 只读处理（2026-07-06 完成）
  - [x] `DataScopeContext` 对 `DESIGNER` 返回 `DataScope.ALL`，避免看不到任何 RSKU
- [ ] 工厂角色与产品库去重（2026-07-06 已确认方案，待实现）
  - [ ] 详见 `docs/08-roadmap/后续开发计划.md` 第 1 节
  - [ ] `FACTORY_SALES` 重命名为 `FACTORY_ADMIN`，权限增加单条录入/编辑自己产品
  - [ ] 新增 `factory_product_capability` 表与 `sys_user.view_full_catalog` 字段
  - [ ] 能力档案默认从已有 RSKU 自动同步，管理员只读查看，工厂不可见
  - [ ] 产品列表支持 `viewMode=own|full`，全库视图按能力去重且只读
  - [ ] 新增工厂单条录入接口，同时创建 RSPU + Variant + 第一条 RSKU
  - [ ] 编辑产品接口校验只能编辑“自己的产品”
  - [ ] 前端账号设置开关、产品库视图切换、工厂单条录入页、管理员能力表
- [ ] 吧椅（BS）分类语义完全未落地
  - [ ] 字典/字段增加座高档 6/7/8 与功能码（旋转/升降/固定/堆叠）
  - [ ] `RspuCodeService` 特殊处理 BS 编码规则

### P1 — 近期处理

- [ ] 权限与功能矩阵补全
  - [ ] 详见 `docs/08-roadmap/后续开发计划.md` 第 3 节
  - [ ] 修正 `EDITOR`/`VIEWER`/`USER` 的 RSKU 数据范围或明确禁用
  - [ ] 产品/工厂读接口强制 `product:read` / `factory:read`
  - [ ] 产品搭配关系、变体创建写接口补上权限校验
  - [ ] 修正 `/api/v1/schemes/{id}/quote` 权限为 `quote:generate`
  - [ ] 接入 `product:review`、`quote:export`、`admin:async-metrics`、`admin:vector-backfill`
  - [ ] 新增 `docs/06-reference/03-角色权限矩阵.md`
- [ ] 前端按钮级权限控制
  - [ ] 详见 `docs/08-roadmap/后续开发计划.md` 第 4 节
  - [ ] 路由 `meta.permissions` 落地
  - [ ] 页面内按钮按 `hasPermission` 隐藏
- [ ] 部署与运维优化
  - [ ] 详见 `docs/08-roadmap/后续开发计划.md` 第 5 节
  - [ ] `docker-compose.yml` 增加 healthcheck 与 depends_on 就绪条件
  - [ ] `make prod` 前校验 `.env` 无 `<CHANGE_ME>`
  - [ ] `scripts/stop-local.sh` 改用 PID 文件精确停止
  - [ ] Nginx 增加 `server_tokens off`、gzip、history 路由 fallback
- [ ] 异步 AI 识别审计日志操作人记录为 `anonymous`
  - [ ] 请求线程捕获操作人传入异步任务
- [ ] `AesEncryptionUtil.decrypt()` 删除明文兜底逻辑
  - [ ] 解密失败直接抛异常；迁移期单独处理
- [ ] `RetryInterceptor` 支持重试 Spring `ResourceAccessException`
  - [ ] 捕获 `ResourceAccessException`、`HttpServerErrorException` 等可重试异常
- [ ] `RskuImportService.importRskus` 长事务与数据权限 N+1
  - [ ] 按批次切分独立事务
  - [ ] 缓存当前用户 `DataScope` 与 `factoryCodes`
- [ ] 多个写接口仅认证未授权
  - [ ] `RspuVariantController`、`RspuRelationController`、`MatchingController`、`TaskController` 增加 `@PreAuthorize`
- [ ] `DefaultAdminInitializer` 硬编码默认管理员密码
  - [ ] 增加 profile/属性条件限制；或生成随机强密码
- [ ] `UserService.listUsers` N+1 查询
  - [ ] 批量查询角色与工厂关联
- [ ] `RskuService.toResponses` 对每个工厂单独查能力等级
  - [ ] 改用 `FactoryService.batchListCapableLevels`
- [ ] `DataScopeContext` 每次调用都查库
  - [ ] 请求级 ThreadLocal 缓存
- [ ] `ProductService.createEntry` 在事务内执行文件存储
  - [ ] 文件存储移到事务外或加回滚清理
- [ ] `VectorBackfillService` 先写 ChromaDB 再写 PG，PG 失败导致不一致
  - [ ] PG 更新失败后补偿删除 ChromaDB
- [ ] `ProductUpdateRequest` / `RspuRelationService.updateRelation` 增加字段与状态校验
- [x] `WebMvcConfig` 全局 CORS 与 `SecurityConfig` 冲突（2026-07-06 完成）
  - [x] 删除 `WebMvcConfig`，统一由 `SecurityConfig` 按环境变量管理
- [ ] 前端实现 Token 刷新或登录弹窗恢复
  - [ ] 避免 401 直接整页跳转丢失未保存数据
- [ ] 路由守卫进入受保护页前校验 Token 有效性
  - [ ] 调用 `/auth/me` 而非仅判断 localStorage 存在
- [ ] 用户管理重置密码改用密码输入弹窗
  - [ ] 替换 `window.prompt` 明文输入
- [ ] 前端 RSKU 出厂价校验拦截 `null`
- [ ] 变体创建类型补充 `productLevel`
- [ ] 前端图片字段 `isPrimary` 与后端 `primary` 对齐
- [ ] 报价单/方案数量校验 MOQ
- [ ] 前端总价计算精度与后端 BigDecimal 对齐
- [ ] AI 空间搭配到报价单丢失 RSKU 与数量
- [ ] 登录页支持 `redirect` 查询参数回跳
- [ ] 修复 `roles` 空数组回退逻辑
- [ ] 用户管理模块封装到 `src/api/user.ts`
- [ ] 报价单对 `productLevel` 为空的 RSKU 应视为工厂能力不匹配
- [ ] `SchemeService.create/update` 校验工厂能力等级
- [ ] 前后端统一密码复杂度策略（至少字母+数字，8 位以上）
- [ ] 产品导入主图下载失败时应阻断该行
- [ ] 以图搜图同款去重指纹加入 `six_dim_tags` 或向量距离阈值
- [ ] 以图搜图 `positioning_label` 过滤统一解析为字典码
- [ ] 报价/方案 BigDecimal 统一 `setScale(2, HALF_UP)`
- [ ] 图片访问按 `image_type` 区分公开/私有权限
- [ ] `price_history` 价格字段改为 AES 加密存储（若 07-02 条目未覆盖）

### P2 — 后续优化

- [ ] `ChromaDbClient` 集合 ID 缓存增加 TTL
- [ ] 核心业务 ID 改用完整 UUID 或雪花算法
  - [ ] 替换 `UUID.randomUUID().toString().substring(0, 8)`
- [ ] `SchemeService.deleteScheme` 软删除时同步标记子项
- [ ] 用户状态变更时清除权限缓存
- [ ] 增加用户物理/逻辑删除接口
- [ ] 评估删除 `user_operator` 冗余表
- [ ] `image_assets` / `ai_recognition` / `async_task` 补充 `updated_at`
- [ ] `async_task` 补充 `started_at`
- [ ] `review_status` 默认值改为英文状态码
- [ ] 开发环境 Redis 配置与容器启动状态一致
- [ ] `price_history.changed_by` 建立外键或索引
- [ ] 清理 `docs/06-reference/02-双层编码体系.md` 旧规则描述
- [ ] 办公家具材质码 3 位规则落地
- [ ] CSV 文件魔数校验增强（列名白名单）
- [ ] 产品导入拒绝 SVG 图片
- [ ] 常用大表增加 `deleted_at` 索引与 JSONB GIN 索引

## 历史归档

> 以下条目已在 2026-06-26 前完成，仅作追溯：

- [x] 图片上传接口：校验、MinIO/本地存储、写入 `image_assets`
- [x] 新品录入异步任务骨架：创建 RSPU 草稿 + 异步任务记录 + 状态轮询
- [x] 视觉识别结果持久化：`ai_recognition` 与 `rspu_master` 字段映射
- [x] 前端新品录入页面：拖拽上传 + 任务轮询 + 结果展示
- [x] 产品列表分页查询（支持品类、风格、场景、材质筛选）
- [x] 产品详情页（含 RSKU 比价表）
- [x] 工厂管理与 RSKU 报价录入
- [x] 审计日志自动记录
