## P0（近期必须完成）

- [x] 产品元数据编辑：`PUT /api/v1/products/{rspuId}`（2026-06-29 完成）
  - [x] 后端：支持部分字段更新（定位标签、颜色、材质、场景、六维标签、价格带、保修年限等）
  - [x] 后端：同步维护 `rspu_style` / `rspu_scene` 关联表
  - [x] 后端：写审计日志
  - [x] 前端：`ProductDetailView.vue` 增加「编辑元数据」弹窗
  - [x] 测试：`ProductQueryServiceTest` 补充更新用例
- [x] 更新本文档，清理过时条目（2026-06-29）
- [ ] 修复 `reset_db.sql` 与 `init_db.sql` 结构不一致（2026-06-30 Review 发现）
  - 补齐 `scheme`、`scheme_item`、`rspu_code_counter`、`variant_code_counter` 四张表
  - 补齐 `rspu_master.review_comment` 字段
  - 补齐 GIN 索引 `idx_rspu_material_tags_gin`
- [ ] 修复 `AsyncTaskProcessor.processProductEntry` 长事务问题
  - 移除方法级 `@Transactional`
  - 外部 HTTP（AI / Embedding / ChromaDB）与数据库写入分离事务
- [ ] 修复 `ProductQueryService.deleteProduct` 事务内调用 ChromaDB
  - ChromaDB 向量删除改为异步或事务外补偿执行
- [ ] `FactoryMaster` JSONB 字段 `certification` / `engineeringCases` 映射修复
  - Entity 加 `@TableField(typeHandler = JsonbTypeHandler.class)` 或 DDL 改为 `TEXT`
- [ ] 敏感价格字段加密：`rsku_supply.factory_price`、`scheme_item.factory_price`
  - 实现 `EncryptTypeHandler`
  - DDL 改为 `TEXT`/`BYTEA`，应用层统一加解密
- [ ] 前端修复 `ProductDetailView.vue` 同组件路由复用不刷新
  - 使用 `onBeforeRouteUpdate` 或 `watch(route.params.rspuId)` 重新加载
- [ ] 前端限制 `QuoteBuilderView.vue` URL 产品 ID 数量
  - 限制 `ids.length <= MAX_ITEMS`（如 50），超限报错并截断

## P1（下周优先）

- [x] 锚点搭配推荐：实现 `POST /api/v1/matching/recommend` 真正推荐逻辑（2026-06-29 完成）
  - [x] 以某个 RSPU 为锚点，AI 推荐目标品类产品
  - [x] 前端 `AnchorMatchingView.vue` 接入真实接口
- [x] 搭配方案管理：保存、查询、详情、删除手动搭配方案（2026-06-29 完成）
  - [x] 后端 `scheme` / `scheme_item` 表与 CRUD 接口
  - [x] 前端 `SchemeListView.vue` / `SchemeDetailView.vue`
  - [x] `QuoteBuilderView.vue` 增加「保存为方案」
- [x] 报价单导出：支持导出 Excel 报价单（2026-06-30 完成）
  - [x] 后端：引入 EasyExcel 生成 Excel
  - [x] 前端：`QuoteBuilderView.vue` / `SchemeDetailView.vue` 增加导出按钮
- [x] 删除/软删除能力（2026-06-30 完成）
  - [x] `DELETE /api/v1/products/{rspuId}`：RSPU 软删除
  - [x] `DELETE /api/v1/sku/{rskuId}`：报价记录软删除
  - [x] 前端：列表/详情页增加删除入口（带二次确认）
- [ ] 优化以图搜图性能：消除 `RetrievalService` 中的 N+1 查询
  - 批量查询图片 URL（`image_assets`）
  - 批量查询 RSPU 元数据（`rspu_master`）
- [ ] Controller 层补齐参数校验
  - 所有 Controller 类加 `@Validated`
  - `@PathVariable String` 参数加 `@NotBlank`
  - `ProductController.list` 加 `@Valid`，`ProductListRequest.size` 限制上限
  - `AdminController.backfillVectors` 的 `batchSize` 加 `@Min/@Max`
  - `RetrievalController.searchSimilar` 加 `@Valid`，`topK` 限制上限
  - 批量图片上传加空文件 / 类型 / 大小校验
- [ ] Service 层外部调用增强健壮性
  - `ChromaDbClient`、`EmbeddingService`、`VisionService` 配置超时与重试
  - 链式 API 响应判空，避免 NPE
  - 将直接 `throw new RuntimeException` 改为业务异常
- [ ] 治理列表页 N+1 查询
  - `ProductQueryService.toSummary` 批量查询主图
  - `ProductQueryService.listProducts` 风格/场景筛选改用 `EXISTS`
  - `RspuRelationService.toResponse` 批量查 RSPU / 图片 / 最低价
  - `RskuService.toResponse` 批量查工厂
  - `QuoteService.generateQuote` 批量查 RSKU / RSPU / 工厂 / 图片
  - `AiMatchingService` 批量查 candidate 最低价
- [ ] 数据库与 MyBatis-Plus 一致性加固
  - `RspuScene` / `RspuStyle` / `CategoryDict` 复合主键标注 `@TableId`
  - 全局启用 MP 逻辑删除 `deletedAt`
  - `scheme_item.factory_code` 加外键
  - `rsku_supply` 唯一索引处理可 NULL 的 `variant_id`
  - `factory_level_capability` 加“唯一主评级”部分唯一索引
  - `SchemeItemMapper.insertBatch` 空集合保护
  - `scheme_item.scheme_item_id` DDL 改为 `BIGSERIAL`
- [ ] 业务规则边界校验
  - `RspuCodeService` 校验 category/style/size 字典存在性，处理 seq 超过 999
  - `RspuVariantService` 确认 sequence 原子分配，增加唯一约束兜底
  - `SchemeService.create/update` 校验 `items` 非空
  - `RskuService.createRsku` 明确 `extendCapability` 事务传播行为
- [ ] 前端边界加固
  - `api/client.ts` 统一判断 `ApiResult.code`
  - 上传类接口覆盖更大超时
  - `ProductEntryView.vue` 校验文件大小与真实 MIME 类型
  - `VisualSearchView.vue` 修复 ObjectURL 泄漏，组件卸载时清理
  - `QuoteBuilderView.vue` 浮点价格精度、价格为 0 显示、`MAX_ITEMS` 真正阻止加载
  - 各详情页校验路由参数非空
  - `FactoryListView.vue` / `SchemeListView.vue` 增加分页

## P2（后续规划）

- [x] 以图搜图 / 同款判定：`POST /api/v1/retrieval/similar`（2026-06-30 完成）
  - [x] 接入 ChromaDB 向量写入与检索
  - [x] 图片向量化 + 三层检索（向量 + 元数据 + 规则）
  - [x] 前端 `VisualSearchView.vue` 支持以图/以文检索
- [ ] 空间校验：餐桌椅耦合、平面占位、产品组合冲突
- [ ] 工厂 Excel 报价单批量导入
- [ ] PPT / 方案导出
- [ ] 用户权限与登录
- [ ] 切换到本地 Ollama 推理（可选）
- [ ] 改进 `RetrievalServiceTest`：移除反射注入 `ObjectMapper`，改为构造函数注入
- [ ] 限制上传图片大小并在 Embedding 前做压缩/缩放（后端侧）
- [ ] `ChromaDbClient` 集合 ID 缓存增加失效与重试机制
- [ ] 统一 ChromaDB metadata 构建，消除 `AsyncTaskProcessor` 与 `VectorBackfillService` 重复代码
- [ ] 细化全局异常处理：区分 AI 服务异常、存储异常、系统异常
- [ ] `AdminController` 增加基础认证/授权控制（至少限制内网/管理员角色）
- [ ] 明确 `AuditLogService.insert` 事务策略（与业务同事务或独立 `REQUIRES_NEW`）
- [ ] `ImageAssets.qualityScore` 由 `Double` 改为 `BigDecimal`
- [ ] 统一 JSONB TypeHandler：`JsonbTypeHandler` 与 `PostgreSqlJsonbTypeHandler` 合并
- [ ] `AuditLog.oldValue/newValue` 类型由 `Object` 改为明确的 Map/快照类型
- [ ] 数据库时区治理：`TIMESTAMP` 升级为 `TIMESTAMPTZ`，Java 改用 `OffsetDateTime`
- [ ] 前端 DataTable `columns` 定义补充 TypeScript 泛型
- [ ] `App.vue` `router.push` 增加 NavigationFailure 处理
- [ ] `QuoteController.export` 文件名增加 UUID/随机后缀避免并发重复
- [ ] 前端 `NImage` 增加 404 fallback 处理
- [ ] 前端数字输入框补充业务上限（如 `budgetLimit` max）

## 历史归档

> 以下条目已在 2026-06-26 前完成，仅作追溯：

- [x] 图片上传接口：校验、MinIO/本地存储、写入 `image_assets`
- [x] 新品录入异步任务骨架：创建 RSPU 草稿 + 异步任务记录 + 状态轮询
- [x] 视觉识别结果持久化：`ai_recognition` 与 `rspu_master` 字段映射
- [x] 前端新品录入页面：拖拽上传 + 任务轮询 + 结果展示
- [x] 产品列表分页查询（支持品类、风格、场景、材质筛选）
- [x] 产品详情页（含 RSKU 比价表）
- [x] 工厂管理与 RSKU 报价录入
- [x] 审计日志自动记录
