## P0（近期必须完成）

- [x] 产品元数据编辑：`PUT /api/v1/products/{rspuId}`（2026-06-29 完成）
  - [x] 后端：支持部分字段更新（定位标签、颜色、材质、场景、六维标签、价格带、保修年限等）
  - [x] 后端：同步维护 `rspu_style` / `rspu_scene` 关联表
  - [x] 后端：写审计日志
  - [x] 前端：`ProductDetailView.vue` 增加「编辑元数据」弹窗
  - [x] 测试：`ProductQueryServiceTest` 补充更新用例
- [x] 更新本文档，清理过时条目（2026-06-29）

## P1（下周优先）

- [ ] 锚点搭配推荐：实现 `POST /api/v1/matching/recommend` 真正推荐逻辑
  - 以某个 RSPU 为锚点，AI 推荐目标品类产品
  - 前端 `AnchorMatchingView.vue` 接入真实接口
- [ ] 报价单导出：支持导出 Excel / PDF 报价单
  - 后端：引入 EasyExcel / Apache POI 生成 Excel
  - 前端：`QuoteBuilderView.vue` 增加导出按钮
- [ ] 删除/软删除能力
  - `DELETE /api/v1/products/{rspuId}`：RSPU 软删除
  - `DELETE /api/v1/sku/{rskuId}`：报价记录软删除
  - 前端：列表/详情页增加删除入口（带二次确认）

## P2（后续规划）

- [ ] 以图搜图 / 同款判定：`POST /api/v1/retrieval/similar`
  - 接入 ChromaDB 向量写入与检索
  - 图片向量化 + 三层检索（向量 + 元数据 + 规则）
- [ ] 空间校验：餐桌椅耦合、平面占位、产品组合冲突
- [ ] 工厂 Excel 报价单批量导入
- [ ] PPT / 方案导出
- [ ] 用户权限与登录
- [ ] 切换到本地 Ollama 推理（可选）

## 历史归档

> 以下条目已在 2026-06-26 前完成，仅作追溯：

- [x] 图片上传接口：校验、MinIO/本地存储、写入 `image_assets`
- [x] 新品录入异步任务骨架：创建 RSPU 草稿 + 异步任务记录 + 状态轮询
- [x] 视觉识别结果持久化：`ai_recognition` 与 `rspu_master` 字段映射
- [x] 前端新品录入页面：拖拽上传 + 任务轮询 + 结果展示
- [x] 产品列表分页查询（支持品类、风格、场景、材质筛选）
- [x] 产品详情页（含 RSKU 比价表）
- [x] 工厂管理与 RSKU 报价录入
- [x] 审计日志自动记录
