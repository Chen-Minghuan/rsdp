## 已完成

- [x] Excel 导入图片与价格录入修复（2026-07-22，沃高报价表实测问题）
  - [x] 根因 1：内嵌图片总量超 200MB 上限整体抛异常 → 98 个产品 0 图；`ExcelImageExtractor` 改为超限截断保留已提取部分（截断标记回传），上限改 `rsdp.excel-import.max-total-image-mb` 可配置（默认 500MB），整体失败/截断写入批次 failures 用户可见
  - [x] 根因 2：销售价列为多行单元格（`4700\n特惠价`）parsePrice 整串解析失败 → 0 条 RSKU；`parsePrice` 取首行提取首个数字（纯备注行返回 null），解析失败记入行 issues（批次 failures 可见）
  - [x] 根因 3（二次导入发现）：fuzzy 兜底把「出厂价」映射为 `referencePriceBand` → 201 行报「参考价格带必须是 low/mid/high」；改为价格类表头走 `__PRICE__:` 价格列通道（价格带/价格区间才映射 referencePriceBand）
  - [x] 合并「类别」单元格后续行品类为空：`forwardFillKeyColumns` 填充范围增加 categoryCode
  - [x] 测试：`ExcelImageExtractorTest` +1（截断保留部分图）、`ExcelAiImportServiceTest` +3（多行单元格价格/fuzzy 价格列/品类向下填充）；全量 602 测试零失败

- [x] Excel 导入品类中文名归一化（方案三：分层解析 + 用户确认 + 别名自学习，2026-07-22 完成）
  - [x] 背景：工厂 Excel 品类列为中文品名（茶桌/主椅/方凳），validateRow 只认字典码导致沃高报价表 216 行全失败；决策文档 `D:\文档\学习文档\项目中决策问题复盘\roomVip项目决策关键决策\2026-07-22-Excel导入品类中文名归一化方案选型.md`
  - [x] 数据层：V16 迁移 `dict_alias` 别名库（dict_type 通用化，唯一约束 (dict_type, alias_name)），V1/reset 同步（用户确认双轨制：新序号迁移 + V1 同步）
  - [x] 分层解析（优先级：用户确认映射 > 字典码 > 字典中文名 > 别名库 > 品类提示兜底）：`DictAliasService`（resolve/save 幂等）；`ExcelAiImportService.normalizeCategoryCode`
  - [x] 预览 AI 批量归一：品类列未解析词批量一次 chatText 注入品类枚举归一（AI 失败降级 source=none 不阻断预览）；`ExcelAiMappingResponse.categoryMappings`（rawValue/suggestedCode/source: dict|alias|ai|none）
  - [x] 确认回写：`ExcelAiMappingRequest.categoryMapping` 行级最高优先；导入后用户确认过的词条写回别名库（source=ai_confirmed，自学习）
  - [x] 前端：映射确认页新增「品类名归一」可编辑表格（来源标签：字典匹配/别名库/AI 建议/未归一；品类提示保留为兜底）
  - [x] 测试：DictAliasServiceTest 7 例、ExcelAiImportServiceTest 30 例、定向 44 例全绿；全量 598 测试零失败、前端三件套全过、对账零差异

- [x] Excel AI 导入链路完整 Review + 修复（2026-07-22 完成）
  - [x] 两路并行 Review（后端核心 / 图片与前端向导）：链路主干（物理行号对齐、归组共享、行级事务）健康，确认 3 P0 + 9 P1
  - [x] P0-1「规格/模块」列（variantDisplayName 标准字段）不再被丢弃，优先于 productName；复合「型号品名」拆分兼容
  - [x] P0-2 `ExcelAiImportResult` 新增 `tasks`（taskId↔rspuId 成对）与 `skippedCount`，旧字段保留；前端任务配对优先消费 tasks（修「查看产品」跳错产品）
  - [x] P1-1 归组模块行变体按 (rspuId,size,color,material) NULL 安全查重复用，避免唯一索引冲突整行回滚
  - [x] P1-2 RSKU/变体失败记入批次 failures（「工厂报价失败：xxx」），markSuccess 回填真实 rskuIds（治假成功）
  - [x] P1-3 updateIfExists 生效：true 复用并更新已有 RSPU（记审计），false 行标记 skipped
  - [x] P1-6 批次导入原子抢占（`ExcelImportBatchMapper.claimForImport`），防并发重复导入；前端 confirm 改 300s 通道 + 超时后用 getExcelAiImportStatus 恢复结果页（治卡死）
  - [x] P1-7 previewMapping 去掉方法级事务，批次写入改编程式事务
  - [x] P1-8 图片列关键字补充「图片/照片/效果图」；P1-9 EMF/WMF/TIFF 跳过并记失败明细
  - [x] P1-10 ExcelImageExtractor 单张图异常跳过，整体性失败才上抛
  - [x] P1-11 前端轮询连续失败 ≥5 次才标记失败（治网络抖动误报）
  - [x] P1-12 confirm/getStatus/listRows 增加批次归属校验（本人或 ADMIN，否则 ForbiddenException）
  - [x] P2 文件大小提示与实际 200MB 阈值统一；失败明细行号改用物理行号
  - [x] 验证：后端全量 585 测试零失败（新增/修改 31 例），前端 lint + type-check + build 全过

- [x] 场景图多产品拆分录入（2026-07-22 完成）
  - [x] 背景：整厅照片直传会把全屋家具混成一个 RSPU 产生脏数据；复用 PDF 导入「AI 区域检测 → 裁剪 → 批量建档」链路
  - [x] 后端：`VisionService.detectSceneProducts` 场景专用 prompt（找家具单品、排除装饰品、最多 12 件按显著度排序、`response_format: json_object`、bbox 合法性校验丢弃）；`SceneImportService`（上传校验 ≤10MB、长边 1024 压缩检测图、逐件 ImageCropper 裁剪 + `createEntryFromStream` 建档、单件失败不阻断、品类=AI检测>提示>FS）；`POST /api/v1/products/scene-import`（`product:create` 权限）；配置 `rsdp.scene-import.max-products/max-file-size-mb`
  - [x] 每件识别走既有异步链路，自动获得「OCR 品名 / 品类回退名」能力
  - [x] 前端：`SceneImportView`（`/products/scene-import`）：场景图上传预览 + 品类提示 + 拆分结果卡片网格（裁剪图预览/品类/识别进度轮询/失败原因/点击跳产品详情）；导航「录入中心」新增入口
  - [x] 测试：`VisionServiceTest` +2（json_object 请求格式 + 非法 bbox 丢弃/空结果）、`SceneImportServiceTest` 4 例（逐件建档/单件失败不阻断/品类兜底 FS/空结果）、`DocumentImportControllerTest` +1（scene-import 端点）；全量 565 测试零失败；前端 lint + type-check + build 全过

- [x] 2026-07-22 产品名称全链路补齐（AI OCR productName 断头路修复）
  - [x] 背景：VisionService prompt 已提取 `ocr.productName`，但 `rspu_master` 无产品名称列、识别落库从不消费，识别结果只留在 `ai_recognition` 原始 JSON
  - [x] 数据层：V15 迁移 `rspu_master.product_name VARCHAR(256)`（V1/reset 三处同步，5433 已应用）
  - [x] 识别落库：`AiRecognitionPersistenceService` 把 `ocr.productName` 写入 RSPU（只补空缺，不覆盖人工/Excel 已填值）；**图上无品名文字时回退为品类字典名**（如 FS→「座椅」），找不到品类名（返回原码）则不写
  - [x] 导入链路：Excel AI 导入品名（原只写变体 displayName）与人工 Excel 导入品名同步落 `product_name`（人工导入空单元格不覆盖）
  - [x] 接口：`ProductSummaryResponse` 增加 `productName`；`PUT /products/{rspuId}` 支持编辑 `productName`（≤256，空串清空）
  - [x] 前端：产品列表新增「产品名称」列；详情页展示；编辑元数据弹窗可修改
  - [x] 测试：`AiRecognitionPersistenceServiceTest` +2（OCR 品名补空缺/不覆盖已有），全量 557 测试零失败；前端 lint + type-check 通过；实体-DB 对账零差异

- [x] 2026-07-22 全项目 Code Review + 修补优化四批次（今日主线：修补、优化，不做新功能）
  - [x] 四路并行 Review（阶段5后端/阶段5前端/V14数据层脚本/既有核心链路遗留项）+ 后端全量测试基线确认
  - [x] 批次 A（后端成员模块）：
    - P0 审计日志落 BCrypt 密码哈希：`AuditLogService` 序列化前对 `passwordHash`/`password` 键（大小写不敏感、含嵌套）统一替换 `***`；`AuthService.register`、`MemberService.joinCompany/certifiedDesigner` 三处改传脱敏快照
    - P1 `CompanyService.deleteMyCompany` 补清成员 `company_name`/`group_name`；`MemberGroupService.deleteGroup` 补清 `group_name`（删除后展示层脏数据）
    - P1 `MemberService.removeMember` 审计 old==new：传参前置空 target 四个归属字段
    - P1 并发唯一冲突兜底：邀请码生成/懒生成捕获 `DataIntegrityViolationException` 重试（耗尽抛「邀请码生成失败」）；注册用户名冲突转 `BusinessException`
    - P1 `transferOwner` 校验新管理员账号为 active，停用账号拒绝转入
  - [x] 批次 B（数据库脚本一致性）：`reset_db.sql` 补 DROP `order_no_counter`；`V1__init_db.sql` 并入 V2 全部内容（6 张表 + 15 索引 + 16 个 ALTER 列扩展 + 8 条 setval），单独执行 V1 与迁移链建库结果一致；`V1__seed_data.sql` 补齐 V2 的 6 组字典；DROP/CREATE 清单程序化 diff 一致
  - [x] 批次 C（用户中心前端）：`stores/user.ts` `fetchUserInfo(force)` 并发下不再被在途请求吞掉、catch 区分 401/403 与网络错误（不再误踢登录态）；4 个新页面接入 `useRequestAbort` + api 层补可选 `signal`；`UserMemberView` 搜索防竞态；403 双重 toast 消除；`LoginView` 拦截 `//` 协议相对路径、watch inviteCode 回填、注册表单上限校验与后端 @Size 对齐、双 Tab 切换清空残留提示；改密/认证后 `/auth/logout` 401 不再硬跳转（redirect 保留）；`window.prompt` 改 Naive UI 弹窗；停用分组回显「名称(已停用)」
  - [x] 批次 D（导入/报价优化）：`RskuImportService.updateExistingRsku` 空单元格不覆盖已有字段、空价格不写 `old→null` 价格历史；`QuoteService` 能力校验改 `batchListCapableLevels` 批量查询消除 N+1；`RetrievalService` 同款去重指纹全空时回退 rspuId（不再误折叠候选）；抽 `util/CategoryPaths` 统一三处品类路径 switch（映射值未动）
  - [x] 验证：后端 `mvn test` 539 个测试零失败（基线 529 + 新增 10）；前端 lint + type-check + build 全过；实体-DB 对账零差异；33 个改动文件 diff 复核无超范围
  - [x] 需业务确认项（已记录待办，未动）：方案列表/详情可见性隔离与 totalPrice 口径、`price_band` 阈值两套（1000/5000 vs 3000/8000）统一口径、企业 `price_ratio=0` 语义、注册接口限流与无效邀请码降级

- [x] RSDP × rooom 全量复现阶段 5：用户中心 + 企业团队完整版（2026-07-21 完成）
  - [x] 数据层 V13：`company`（含 price_ratio 企业折扣率 CHECK [0,1]）、`member_group`、`invite_record` 三表；`sys_user` 扩 company_id/group_id/invite_code（唯一索引）/invited_by/certified_designer；历史 company_name/group_name 文本迁移为实体（同名企业合并、owner 取最早用户、分组按企业+名称合并，含 V6 未应用守卫）；V1/V1_seed/reset_db 三处同步，5433 已应用验证、幂等可重复执行
  - [x] 企业+分组后端：`CompanyService`/`MemberGroupService`/`MemberController`（企业 CRUD、变更管理员、分组 CRUD+启停，写操作仅企业管理员或平台 ADMIN，全量审计；企业/分组改名同步成员轻量文本字段）；`OrderService` 订单计价改为企业 price_ratio 优先于全局 price_rate
  - [x] 成员管理后端：`MemberService`（成员列表/搜索邀请加入/移出/调分组，owner 需先变更管理员才能移出；空值清空走 UpdateWrapper）；认证设计师 `PUT /api/v1/member/certified-designer`（挂标记，VIEWER/USER 补 DESIGNER 角色 + 递增 token_version）
  - [x] 邀请裂变后端：`InviteService`（8 位永久邀请码去易混淆字符、唯一冲突重试、注册归因绑定 + invite_record、我的邀请记录）；公开注册 `POST /api/v1/auth/register`（默认 VIEWER，SecurityConfig 放行）；`UserService.createUser` 同步生成邀请码；`/auth/me` 懒生成兜底 + 透传 inviteCode/certifiedDesigner/companyId
  - [x] 自服务后端（个人中心依赖）：`PUT /auth/me/profile`（改昵称）、`PUT /auth/me/password`（校验原密码 + 递增 token_version），审计打码
  - [x] 用户中心前端：`UserCenterView` 左菜单布局 + 4 子页——个人中心（账号信息/资料修改/改密/认证设计师入口，改密与认证成功后统一重新登录）、企业信息（创建/编辑/折扣率/变更管理员）、成员管理（部门 CRUD+启停、搜索邀请、调分组、移出）、邀请用户（永久邀请链接一键复制 + 邀请记录）；`types/member.ts` + `api/member.ts`；`userStore` 三级账号类型徽标（游客/设计师/企业账号）；顶栏徽标 + 个人中心入口；`LoginView` 登录/注册双 Tab + `?inviteCode=` 回填（命中直达注册 Tab）
  - [x] 测试：CompanyServiceTest 10 例、MemberGroupServiceTest 7 例、MemberServiceTest 12 例、InviteServiceTest 7 例、OrderServiceTest 企业折扣率用例、AuthServiceTest +3 注册用例、UserServiceTest +3 自服务用例；全量 529 测试零失败、实体-DB 对账零差异、前端 lint + type-check + build 全过
  - [x] 文档：API 设计补注册/自服务/企业团队端点；权限矩阵新增 2.9 节（/member/** 自服务归属校验模式，不新增权限码）

- [x] 2026-07-21 拉取远程 dev + 数据库脚本序号冲突修复
  - [x] 拉取远程 dev（经本机 7890 代理）：fast-forward 至 4bbd942，新增 3 份 rooom 整合文档（用户端/管理端功能清单、全量复现与 AI 整合方案），与本地未提交改动零冲突
  - [x] 序号冲突修复：`V7__variant_unique_index.sql` 与 `V7__order_price_rate_constraint.sql` 重号，前者 `git mv` 改号为 `V12__variant_unique_index.sql`（索引名 `uk_variant_attrs` 不变，幂等安全），`AGENTS.md` 文件引用同步修正

- [x] 2026-07-21 Code Review 后续批次 3：性能与工程卫生
  - [x] 前端路由守卫 `/auth/me` 会话级缓存：`stores/user.ts` `fetchUserInfo` 增加 60s TTL 缓存 + `force` 参数，消除每次导航阻塞请求；登录后/偏好变更后 3 处调用点改 `fetchUserInfo(true)` 强制刷新
  - [x] `SchemeListView` 标签下拉修复：后端分页后标签选项不再取自当前页，改为挂载时独立拉取全量模板（size=100）提取标签
  - [x] `RetryInterceptor` 支持 429 限流重试（与 5xx 共用 200ms→2s 指数退避）；新增 `RetryInterceptorTest` 4 例
  - [x] `CacheConfig` 实现 `CachingConfigurer` 注册 `CacheErrorHandler`：Redis 运行期故障降级回源查库 + ERROR 告警
  - [x] ChromaDB 向量维度固定：`ChromaDbProperties.dimension`（默认 1024）+ `createCollection` metadata 声明 + `upsert` 前置维度校验；`application.yml` 可配；`ChromaDbClientTest` +1 例（维度不匹配拦截）
  - [x] 验证：前端 lint + type-check + build 全过；后端定向 15 例全绿

- [x] 2026-07-21 Code Review 后续批次 2：AI 健壮性
  - [x] `OpenAiChatRequest` 增加 `max_tokens`（`@JsonProperty("max_tokens")`）与 `response_format`（`json_object`），`@JsonInclude(NON_NULL)` 不影响未设置场景
  - [x] `VisionService.recognizeImage`：请求加 `response_format: json_object` + `max_tokens=4096`；JSON 解析失败改抛「解析 AI 识别结果失败」独立异常（原被外层 `catch(IOException)` 误报为「读取图片流失败」）
  - [x] `VisionService.detectPageRegions`：页面图片流逐张 try-with-resources 关闭（P1 遗留项收尾）；请求加 `max_tokens=8192` 降低截断概率；`chatText` 加 `max_tokens=4096`
  - [x] 测试：`VisionServiceTest` +3（请求体含 response_format/max_tokens、非法 JSON 报错文案、页面流关闭验证），7 例全绿

- [x] 2026-07-21 Code Review 后续批次 1：部署阻断 + 数据一致性修复
  - [x] 全项目 Review（2 个 explore 子代理并行）：最近 5 提交（含 2 个 merge）冲突解决逐项核对无误；后端 `mvn test` 全绿
  - [x] 部署阻断修复：`deploy/docker-compose.yml` server 服务透传 `RSDP_ORDER_INVITE_SECRET` / `RSDP_ORDER_INVITE_EXPIRE_DAYS`（`OrderInviteService` 启动强校验，缺失会导致容器化部署启动失败）
  - [x] `VectorBackfillService` 双写一致性：PG `updateById` 失败时补偿删除 ChromaDB 刚写入的向量；新增 `ChromaDbClient.getExistingIds`（POST `/get`）批量核查，「PG 有向量 / Chroma 缺失」死区记录复用存量向量补偿 upsert（不调 embedding API），批量核查失败按缺失处理自愈
  - [x] `PriceEncryptionMigration` 泛化 `migrateTable` 支持任意价格列，新增 `design_order_item.original_price/final_price` 存量明文迁移（幂等跳过已加密）
  - [x] 文档核实勾掉：`DefaultAdminInitializer` 硬编码密码（已改环境变量 + 随机强密码）
  - [x] 测试：`VectorBackfillServiceTest` +2（Chroma 缺失补偿、PG 失败补偿删除）、`ChromaDbClientTest` +2（getExistingIds）、`PriceEncryptionMigrationTest` +1（订单明细两列），定向 20 例全绿

- [x] 2026-07-21 P1：变体创建唯一性校验 + 唯一索引兜底
  - [x] `RspuVariantService.createVariant` 前置校验同 RSPU 下 `(size_code, color_code, material_code)` 不重复（NULL 安全：`isNull`/`eq` 分列处理）
  - [x] 数据库兜底：部分唯一索引 `idx_variant_unique_dims`（`NULLS NOT DISTINCT WHERE deleted_at IS NULL`），新增 `V12__variant_unique_dims.sql`，V1/reset_db 同步
  - [x] 插入冲突按索引名区分：维度冲突 →「相同尺寸/颜色/材质的变体已存在」；主键冲突 → 保持「请重试」
  - [x] 测试：`RspuVariantServiceTest` +2 例（前置校验拦截、索引冲突兜底），8 例全过

- [x] 2026-07-21 P1：RSPU 软删除级联处理关联数据
  - [x] `ProductQueryService.deleteProduct` 新增 `cascadeDeleteAssociations`：变体/RSKU 报价/图片/搭配关系（anchor+related 双向）随产品软删除；`rspu_style`/`rspu_scene` 纯关联行物理删除
  - [x] 修复级联引入的顺序问题：图片 ID 改为级联前收集，直接随 `RspuDeletedEvent` 发布（否则软删后查不到图片、ChromaDB 向量清理失效）
  - [x] 测试：删除用例补充 6 个关联 Mapper 的级联验证，`ProductQueryServiceTest` 24 例全过

- [x] 2026-07-21 P1：受控字典强校验收尾（reviewStatus + quoteConfidence）
  - [x] `ProductQueryService.reviewProduct` 基于 `ReviewStatus` 枚举校验复核状态，非法值抛 `BusinessException`
  - [x] `RskuImportService.validateRow` 校验 `quoteConfidence` 必须为 `quote_confidence` 字典码（normalizeRow 中文映射之后判定）
  - [x] 补齐 `quote_confidence` 字典种子（high/mid/low ↔ 高/中/低）：V1__seed_data.sql、reset_db.sql、新增 `V11__quote_confidence_dict.sql`（此前字典为空导致中文映射从不生效）
  - [x] 测试：`ProductQueryServiceTest` 非法复核状态用例、`RskuImportServiceTest` 非法置信度用例

- [x] 2026-07-21 P1：异常处理规范化
  - [x] `ChromaDbClient.getOrCreateCollectionId`：仅 404 判定为集合不存在并创建，5xx/连接异常抛 `ExternalServiceException`（不再误创建）
  - [x] `MinioStorageService.exists`：仅 `NoSuchKey` 返回 false，权限/连接等异常抛 `IOException`；`StorageService.exists` 接口声明 `throws IOException`（全库无业务调用方，零影响面）
  - [x] `BusinessException` 新增带 cause 构造器；两个导入服务 `parseExcel` 包装原始异常为 cause
  - [x] 复核确认审计写入失败兜底已由 `AuditLogWriter`（异步 + ERROR 日志）覆盖，无需再改
  - [x] 新增 `MinioStorageServiceTest` 4 例、`ChromaDbClientTest` 2 例（MockRestServiceServer）

- [x] 2026-07-21 P0：修复 `AsyncTaskProcessor` 多任务竞争更新同一任务记录
  - [x] `AsyncTaskMapper` 新增 `claimPendingTask`：`UPDATE async_task SET status='processing', progress=10 WHERE task_id=? AND status='pending'` 原子认领
  - [x] `processProductEntry` 入口改为认领失败（已 processing/终态）直接跳过，替代原先无条件覆盖状态
  - [x] 三个调用方（ProductService ×2、ExcelAiImportService）均以 `pending` 创建任务，语义兼容
  - [x] 测试：既有 5 用例适配认领语义（`updateById` 次数 3→2），新增「任务已被认领则跳过」用例

- [x] 2026-07-21 Code Review + 昨日批次收尾提交
  - [x] 复核 07-20 下午/晚间未提交批次（57 文件 + 5 新文件），实现与进度记录一致
  - [x] 修复 `OrderNoGeneratorTest` 硬编码日期 `"20260720"` 导致跨日运行 `PotentialStubbingProblem`，改为动态当天日期
  - [x] 工程卫生：`.gitignore` 排除 `cookies*.txt`/`query`/`test_image.png`，并将已跟踪的 `cookies.txt`/`query`/`test_image.png` 移出版本控制（本地文件保留）
  - [x] 分批提交：`feat(id)` 完整 UUID 主键 + 列宽拓宽；`fix(p1)` 收藏夹权限/审计/统计上限/SSRF/JSONB 加固；`fix(web)` 前端修复与文档
  - [x] 验证：后端 `mvn test` 440 个测试全绿；前端 `pnpm lint && pnpm type-check` 通过

- [x] 2026-07-20 晚间 Code Review 复核 + 遗留 P0/P1 收尾
  - [x] Review 未提交批次发现 2 个阻断问题并修复：
    - 回退 `application-dev.yml` 硬编码 AES/JWT/订单邀请密钥回归（与 f61037f P0 安全修复冲突，恢复为环境变量注入）
    - 完整 UUID 主键（最长 42 字符）超出 `batch_id VARCHAR(32)`、`favorite_id`/`project_id`/`order_id` 等 `VARCHAR(40)` 列宽：新增 `database/V10__widen_id_columns.sql`，并同步 V1/V2/V4/V5/reset_db.sql 全部拓宽至 VARCHAR(64)
  - [x] 前端 P0：`QuoteBuilderView` 的 `selectedRskuMap`/`quantityMap` 改 `reactive` 修复响应式丢失；`ProductDetailView` 表单初始字段与 DTO 对齐（`productLevel` 经核实后端支持，保留）
  - [x] 后端 P0/P1：`JsonbTypeHandler` null 显式 `setNull(Types.OTHER)` + `isStringType` 泛型误判修复（新增 5 个测试用例）；`ImageUrlValidator` SSRF 加固（scheme 小写、显式端口限 80/443、`getAllByName` 全量 A 记录 DNS 复检）；`VisionService`/`EmbeddingService` 输入流改 try-with-resources；`RskuService.updateRskuPrice` 归属不一致改抛 `BusinessException`
  - [x] 复核不属实项：`deleted_at IS NULL` 过滤（`@TableLogic` 已自动追加）、`ProductQueryService` 手动拼接 JSON（历史提交 520e533 已参数化）
  - [x] 验证：后端 `mvn test` 440 个测试全绿；前端 `pnpm lint && pnpm type-check` 通过

- [x] 2026-07-20 下午 P1 修复：业务主键完整 UUID + 统计接口时间窗/上限 + 更新快速登录文档等
  - [x] 业务主键：新增 `server/src/main/java/com/rsdp/util/IdGenerator.java`，统一生成 `PREFIX-<完整 UUID>`；替换 13 个文件共 27 处 8 位截断 UUID 生成，删除未使用的 `java.util.UUID` import
  - [x] 统计接口：`OrderStatisticsService` 强制默认 90 天/最大 365 天、结果前 50；`OrderController.statistics` 增加 `dim` 枚举校验；`StatisticsController.factories` 新增 `months` 参数（默认 12，最大 24）；`StatisticsService.factories(int months)` 使用传入月数、结果前 20
  - [x] 文档：更新 `docs/02-architecture/04-API设计.md` 订单统计与工厂统计接口说明；更新快速登录相关文档
  - [x] 验证：`mvn test -q` 后端测试全绿；`pnpm lint && pnpm type-check` 前端检查通过

- [x] 2026-07-20 批量修复（P0/P1/P2）
  - [x] 07-16 Code Review 安全与数据硬伤收尾：RSPU-工厂映射/工厂交期规则写端点接入 `DataScope`；批量插入 price 字段声明 `EncryptTypeHandler`；`favorite.ts` 数组参数契约修复；`AesEncryptionUtil` 兜底日志脱敏
  - [x] 登录防爆破：`LoginAttemptService` + `AuthService`/`AuthController` 接入 + `application.yml` 配置
  - [x] JWT 测试密钥随机化
  - [x] 移除默认管理员与订单邀请的 fallback 弱口令/硬编码密钥；开发种子账号密码改为 `rsdp-dev-2026!`
  - [x] 订单全局折扣率 `[0,1]` 范围校验
  - [x] 修复 RSKU 导入自调用导致的事务失效
  - [x] Excel 解析增加 zip bomb 与资源上限防护
  - [x] 图片公开访问增加归属校验
  - [x] 分页 `size` 上限与 HikariCP 连接池配置
  - [x] 性能与安全治理：`V8__missing_indexes` 补齐索引；`AuditLogService` 写入拆分为 `AuditLogWriter`；补齐 project / template / config 等写操作审计；治理 `DesignerProfileService` N+1 等
  - [x] 安全硬编码与权限缺口：`SecurityConfig` 默认密码场景加固；`App.vue` / 路由增加 `public` / `hideHeader` meta
  - [x] 订单模块并发与性能：`OrderNoGenerator` 改 `INSERT ... ON CONFLICT DO UPDATE` 发号；状态机改条件更新避免 read-check-write；统计接口 SQL 聚合优化
  - [x] 前端体验：axios `CanceledError` 透传修复；`useEcharts` 按需注册；`VisualSearchView` 旧 ObjectURL 释放
  - [x] 收藏夹接口显式权限控制：`favorite:read` / `favorite:write` 权限码 + `SecurityConfig` 显式规则 + `FavoriteController` `@PreAuthorize` + 种子数据/迁移 + 测试
  - [x] 新模块写操作补审计：`ProjectService` / `SchemeService` / `ConfigService` 接入 `AuditLogService`
  - [x] 部署与工程化：`Makefile` / `reset_db.sql` 环境守卫 / `docker-compose.yml` / `server/Dockerfile` jar 通配符修复
  - [x] 验证：后端 `mvn test` 405 个测试全部通过，前端 `pnpm lint && pnpm type-check` 通过

- [x] P0 安全/数据硬伤集中修复（2026-07-20 完成）
  - [x] 敏感价格明文落库：`DesignOrderItemMapper.xml` / `SchemeItemMapper.xml` / `RskuSupplyMapper.xml` 批量插入声明 `EncryptTypeHandler`，`original_price`/`final_price`/`factory_price` 加密写入
  - [x] 收藏状态查询契约修复：`web/src/api/favorite.ts` `checkFavorites` 数组参数改为 `rspuIds.join(',')`，匹配后端 `@RequestParam List<String>`
  - [x] 两个写端点授权缺口修复：`SecurityConfig` 显式授权 `POST /api/v1/products/*/factories`（`PRODUCT_UPDATE`）与 `POST /api/v1/factories/*/lead-time-rules`（`FACTORY_UPDATE`）；`RspuFactoryMappingService` / `FactoryLeadTimeRuleService` 接入 `DataScopeHelper` 数据权限校验
  - [x] `AesEncryptionUtil` 解密兜底日志脱敏：`fallbackPlainPrice` 不再输出明文，仅记录长度与 SHA-256 哈希
  - [x] 登录防爆破：`LoginAttemptService` 基于内存实现 IP/用户名失败计数与短期锁定；`AuthService`/`AuthController` 接入；`application.yml` 增加 `rsdp.security.login.*` 配置
  - [x] JWT 测试密钥随机化：`JwtUtilTest` 不再硬编码密钥
  - [x] 测试：`RspuFactoryMappingServiceTest` / `FactoryLeadTimeRuleServiceTest` 各补 1 例无数据权限场景；新增 `LoginAttemptServiceTest`；后端 405 测试全绿，前端 lint + type-check 通过

- [x] P2 技术债第三批（2026-07-21 完成）
  - [x] Excel 重复行检测增强：`ProductImportService` 预加载「外部编码 → RSPU ID」映射，同一产品在 Excel 中分别用 rspuId / externalCode 引用时识别为重复行（原只按单一 key 去重）；ProductImportServiceTest +1 用例
  - [x] 逻辑删除遗留防御代码清理（子代理执行）：14 个 Service 删除 46 处冗余 `getDeletedAt()` 判断（实体均有 @TableLogic，MP 标准查询与自定义 XML 均已过滤软删）；3 个测试用例适配（2 个删除、1 个改为空列表语义）
  - [x] 审计日志增强：`AuditLogService` 记录客户端 IP（RequestContextHolder + X-Forwarded-For 解析，异步线程为 null）；写入失败增加 `AtomicLong` 计数器（`getInsertFailureCount`）+ 带累计次数的 ERROR 日志；操作人本就走 SecurityContext（核实已在）
  - [x] 方案列表后端分页（子代理执行）：`GET /api/v1/schemes` 改 `PageResult` 响应 + page/size 参数（上限 100），tag 筛选从内存过滤下沉 SQL；前端 SchemeListView 改服务端分页，ProjectDetailView 模板弹窗 size=100 适配；API 文档已同步
  - [x] 细化全局异常处理核实已完成（ExternalServiceException 返回真实 503，上一批落地），勾掉遗留待办
  - [x] 全量验证：后端 434 测试零失败，前端 lint + type-check + build 全过

- [x] P2 技术债第二批（2026-07-20 完成）
  - [x] 数据库 V7 迁移：`database/V12__variant_unique_index.sql`（原文件名 V7，2026-07-21 因序号冲突改号 V12）新增 `rspu_variant(rspu_id, COALESCE(size/color/material_code,''))` 部分唯一索引（仅约束未软删除记录，防并发导入重复变体），V1/reset 三处同步；`sys_user_factory.factory_code` 核实已是 VARCHAR(16)（勾掉遗留待办）
  - [x] 审计日志治理：`AuditLog.oldValue/newValue` 由 `Object` 改为 `Map<String, Object>`；`AuditLogService` 四个入口明确 `REQUIRES_NEW` 独立事务（PG 事务内语句失败会中止全事务，同事务下审计失败会连带业务回滚）；`AuditLogServiceTest` 改构造函数注入真实 ObjectMapper（原 @InjectMocks 注入 null，一直在走降级路径）
  - [x] `ImageAssets.qualityScore`：`Double` → `BigDecimal`（DDL 本就 DECIMAL(5,4)）
  - [x] ChromaDB 治理（子代理执行）：新增 `ChromaMetadataBuilder` 统一 metadata 构建（消除 AsyncTaskProcessor/VectorBackfillService 重复）；`ChromaDbClient` 集合 ID 缓存增加「集合不存在」失效检测 + 清缓存重试一次（upsert/query/delete 统一走 `executeWithCollectionRetry`）
  - [x] 图片 Embedding 前缩放（子代理执行）：新增 `ImageResizer`（长边 >1024 等比缩放、透明 PNG 转 RGB 白底、JPEG 0.85），在 `EmbeddingService.embedImage` 单点接入，覆盖录入/回填/以图搜图三链路，失败降级用原图
  - [x] 前端 DataTable columns 泛型（子代理执行）：13 个 View 的 columns 补行类型泛型（全部复用 types/ 已有类型，未暴露真实 bug）
  - [x] 测试：新增 ChromaDbClientTest（WireMock 6 用例）/ChromaMetadataBuilderTest/ImageResizerTest（6 用例）/EmbeddingServiceTest（WireMock）；定向 46 用例零失败
  - [x] 全量验证：后端 433 测试零失败，前端 lint + type-check + build 全过

- [x] P2 技术债第一批（2026-07-20 完成）
  - [x] 后端健壮性：`GlobalExceptionHandler` 对 `ExternalServiceException` 返回真实 503；`RetryInterceptor` 支持 5xx 重试 + 指数退避（200ms 倍增至 2s）；`MinioStorageService.exists` 区分「不存在（NoSuchKey→false）」与「存储故障（抛 IOException）」，接口签名加 `throws IOException`；`StorageProperties` MinIO 密钥默认值改空 + `MinioConfig` 启动校验快速失败（prod yml 同步去 minioadmin 默认）
  - [x] 后端小修：`QuoteController.export` 文件名加 8 位随机后缀防并发重复；`RskuService.createRsku` 细化约束冲突提示（唯一/外键/其他）；`AdminController` 清理过时 TODO（`/api/v1/admin/**` 已由 SecurityConfig 限定 ADMIN 角色）
  - [x] scheme 软删除级联：`deleteScheme` 级联软删 `scheme_item` 明细（@TableLogic），`SchemeServiceTest` 补级联验证
  - [x] 测试改进：`RetrievalServiceTest` 移除反射注入，改构造函数注入真实 ObjectMapper
  - [x] 前端小修（子代理执行）：`VisualSearchView` 切换图片先释放旧 Object URL；`App.vue` 导航统一忽略 NavigationFailure；`RoomSchemeView` 预算上限补 min/max；产品列表/详情主图加统一 SVG 占位 fallback（`IMAGE_FALLBACK_SRC`）；`types/matching.ts` 的 `SchemeItem` 重命名为 `AiSchemeItem` 消除与 `types/scheme.ts` 的同名歧义
  - [x] 核实已完成（历史遗留，勾掉待办）：`RspuRelationService.createRelation` 排除软删（@TableLogic 自动过滤 + 无唯一索引冲突）；`DictService` 缓存（@Cacheable/@CacheEvict 已在）；`RoomSchemeView` 报价单携带 RSKU（rspuIds&rskuIds&quantities 参数协议已在）
  - [x] 全量验证：后端 416 测试零失败，前端 lint + type-check + build 全过

- [x] P1 技术债清理（2026-07-20 完成）
  - [x] `AiMatchingService` 全表加载治理：风格筛选从「先全量查 `rspu_style` 再 IN 两步查询」改为 `EXISTS` 关联子查询单条 SQL 下推，候选 LIMIT 30 不变；移除不再使用的 `RspuStyleMapper` 依赖并同步清理测试桩
  - [x] `RskuImportService` 文件魔数校验：核实已完成（`ExcelFileValidator.isExcelOrCsv`，PK/OLE2 魔数 + CSV 可打印文本判定，测试覆盖非法文件拒绝），勾掉遗留待办
  - [x] JWT token_version（方案 A）：核实机制已落地（`sys_user.token_version` + JWT claim + 过滤器逐请求比对 + 角色变更/重置密码/禁用三处递增），本次补齐 `UserServiceTest` 4 用例（角色变更递增、未变不递增、重置密码递增、禁用递增）
  - [x] 全量验证：后端 416 测试零失败（无前端改动）

- [x] Excel AI 导入全链路修复与增强（2026-07-17 完成）
  - [x] 浮动图片错位根治：旧代码按「表头 1 行」假设换算行号，双行表头/标题行场景全错；改为导入时重解析原始文件重建数据行物理行号（`loadPhysicalLayout`），POI 图片锚点行精确对齐数据行，10 个真实工厂文件验证命中率 ~100%
  - [x] 图片列物理索引：previewRows 经 PostgreSQL jsonb 存储后键序被打乱，曾致图片列误判、150 张图全被过滤；改为从合并表头取真实物理列索引 + 数据区域边界，根治全量丢图
  - [x] 产品归组：型号/品名列按纵向合并单元格语义向下填充，同型号连续行归入同一 RSPU（共享主图与 AI 识别任务），规格模块行不再成为独立产品；实测 149 行 → 51 个产品
  - [x] 图片分层归属（复用 `image_assets.variant_id`，无库表变更）：产品图样列图=主图候选；规格/模块列图=模块样式图挂本行变体；数据区域外图（logo/二维码）忽略；严格主图模式（表格有图片列时模块示例图不兜底升主图，无产品图样图则产品无主图）
  - [x] 行级交期：AI 映射 prompt / fuzzy 关键字 / 前端下拉新增 `leadTimeDays` 标准字段；`parseDaysValue` 容忍「30天」「25-30天」；优先级 Excel 行级值 > 工厂交期规则 > 请求默认值；删除从未接线的死代码 `parseLeadTimeDays`
  - [x] 容错三连：空价格列 NPE（`getOrDefault` 对 null 值返回 null）修复；文件中间重复表头行自动识别跳过（值与列名高度自指判定）；「更新日期/修订日期」等尾注行并入说明行过滤
  - [x] 前端状态保持：导入向导全部状态 + 上传/导入请求 + 识别轮询迁入 `stores/excelImport.ts`（Pinia），组件卸载不再取消请求/停止轮询，切换页面后回来进度不丢失，返回时自动恢复轮询
  - [x] 测试：ExcelAiImportServiceTest 从 10 → 14 用例（物理行号挂图、行级交期优先、合并单元格+重复表头+空价格综合、组内图片共享+列过滤、模块示例图不升主图）；全量 405 测试零失败，前端三件套全过

- [x] RSDP × rooom 整合阶段 4：订单统计 + 企业团队轻量版（2026-07-16 完成）
  - [x] 数据层：V6 迁移 `sys_user` 扩展 `company_name`/`group_name`（幂等 + 列注释）；V1/seed/reset 三处同步，6 个演示账号补企业/分组，5432 已应用验证、重复执行幂等安全
  - [x] 订单统计后端：`OrderStatisticsService` + `GET /api/v1/orders/statistics?dim=product|factory&from=&to=`（声明在 `/{orderId}` 之前，复用 `order:read`）；排除 CANCELLED、from/to 含当日边界、非 ADMIN 按 `created_by`(user_id) 归属隔离；AES 加密到手价实体级解密后内存聚合；产品维度按 RSPU 聚合（商品名/图取订单快照）、工厂维度订单数 distinct 聚合 + 工厂名批量补齐；`OrderStatisticsServiceTest` 5 用例
  - [x] 企业团队后端：`SysUser`/`UserResponse`/创建/编辑请求 DTO 加两字段（@Size 128/64）；`UserService` 创建/更新写入（null 不覆盖）+ 列表透传 + keyword 扩展企业/分组搜索；`ProjectService.create` 企业名称留空回退当前用户企业；UserServiceTest/ProjectServiceTest 各 +3 用例
  - [x] 订单统计前端：`types/orderStatistics.ts` + `api/orderStatistics.ts`；`OrderStatisticsView`（`/orders/statistics`）：维度切换 + 日期范围筛选 + echarts 横向柱状图 TOP10 + 明细表格（产品维度含图片列）；路由静态路径优先于 `:orderId`；导航「订单」组加「订单统计」项并防双高亮
  - [x] 用户管理前端：抽离 `types/user.ts` + `api/user.ts`（消化待办 P1「用户管理模块封装」）；列表新增企业/分组列、搜索框提示扩展、创建/编辑表单加两字段
  - [x] 全量验证：后端 400 测试零失败、实体-DB 对账零差异、前端三件套全过；API 文档补订单统计/用户字段/项目默认企业；权限矩阵补齐 project:*/order:* 行

- [x] RSDP × rooom 整合阶段 3A：订单化（方案转订单 + 状态机 + 客户邀请确认）（2026-07-15 完成）
  - [x] 数据层：V5 迁移 `design_order`（ORD- 主键、order_no 唯一、AES 加密总价、price_rate 折扣率、invite_token_hash/invite_expire_at/invite_confirmed_at、软删除）+ `design_order_item`（价格快照 + snapshot_json）+ `sys_config`（price_rate='1' 种子）+ `design_order_status` 字典 + `order:*` 权限（ADMIN/DESIGNER 显式映射）；V1/seed/reset 三处同步，5432 已应用验证
  - [x] 订单后端：`OrderService`/`OrderController` 五接口（由方案生成订单：价格快照 = RSKU 当前出厂价 × 全局折扣率保留两位、五态状态机 PENDING→CONFIRMED→PRODUCING→COMPLETED+CANCELLED、归属隔离、order_no 唯一冲突重试 3 次、列表带 statusCounts）；`OrderNoGenerator`（DO-yyyyMMdd-三位序号）；`ConfigService`/`ConfigController`（price_rate 读写，写仅 ADMIN）；`OrderServiceTest` 8 用例
  - [x] 邀请后端：`OrderInviteService`（HMAC-SHA256 签名 token = base64url(orderId.过期时间戳.nonce).base64url(签名)，库存 SHA-256 哈希，7 天有效，常量时间比对，重新生成旧链接失效，确认一次性）；`PublicOrderController` 免登录查看/确认（公开视图只含到手价，绝不泄露出厂价/工厂/RSKU）；`ContractTemplateService` POI 生成 docx 采购合同模板；`OrderInviteServiceTest` 6 用例
  - [x] 依赖治理：pom 新增 `poi-ooxml 5.3.0`，从 easyexcel 排除其传递的旧版 POI 4.1.2（解决 ooxml-schemas 类冲突导致的 Excel 测试全挂）
  - [x] 配置：`rsdp.order.invite-secret`（生产走 `RSDP_ORDER_INVITE_SECRET` 注入，留空用开发默认值并告警）+ `invite-expire-days: 7`；SecurityConfig `/api/v1/public/**` permitAll + `POST /orders/*/invite` → `order:update`
  - [x] 订单前端：`types/order.ts` + `api/order.ts` + `api/orderInvite.ts`；`OrderListView`（状态计数 RadioGroup 筛选 + 服务端分页表格）；`OrderDetailView`（三级价格明细快照、状态机操作按钮、编辑收件信息弹窗仅 PENDING、生成邀请链接一键复制、下载合同模板）；`OrderInviteView` 客户免登录确认页（`/invite/order/:token` public 路由）；方案详情页「转订单」入口（`order:create` + 收件信息弹窗，创建成功跳订单详情）；导航「订单」入口 + `ORDER_*` 权限常量
  - [x] 全量验证：后端 389 测试零失败、实体-DB 对账零差异、前端三件套全过；API 文档补订单/邀请/配置接口

- [x] RSDP × rooom 整合阶段 3：运营统计 + 项目运营视角（2026-07-15 完成）
  - [x] 统计后端：`StatisticsService`/`StatisticsController` 三接口（overview 总览 / trends 月度趋势补零 / factories 工厂金额 TOP10），复用 `scheme:read` 权限，非 ADMIN 按归属隔离；`StatisticsServiceTest` 4 用例
  - [x] 统计前端：引入 echarts（已批准依赖）；`useEcharts` 通用组合式函数；`StatisticsView`（数字卡片 + 金额/方案数双轴趋势图 + 工厂横向柱状图）；导航「统计」入口
  - [x] 项目运营视角：项目列表 scope 参数（mine/all，all 仅 ADMIN 生效）；前端 ADMIN「全部项目/我的项目」切换 + 全部模式显示负责人；ProjectServiceTest +2 用例
  - [x] 全量验证：后端 370 测试零失败、前端三件套全过；API 文档补统计接口与 scope 参数

- [x] RSDP × rooom 整合阶段 2：设计项目 Project + 方案模板化（2026-07-15 完成）
  - [x] 数据层：V4 迁移追加 `project` 表（PROJ- 主键、软删除）+ `scheme` 扩 `project_id`/`is_template`/`template_tags` 三列 + `project_type` 字典 3 条 + `project:*` 权限种子（DESIGNER 全量、VIEWER/USER 只读、ADMIN/EDITOR 显式补插）；V1/reset 三处同步，5432 已应用验证
  - [x] 项目后端：`ProjectService`/`ProjectController` 五接口（分页列表按归属隔离、批量聚合方案统计无 N+1、详情含方案列表与总价、软删解绑方案）；`ForbiddenException` 403 语义；`ProjectServiceTest` 7 用例
  - [x] 方案模板化后端：创建/更新支持 projectId 归属校验；`setTemplate`；`copyFromTemplate`（复制方案项、价格取 RSKU 最新价、priceChanges 对比、失效 RSKU 跳过回报、模板只读、名称自动避让）；列表支持 isTemplate/tag 筛选；SchemeServiceTest +5 用例
  - [x] 项目前端：`ProjectListView`（卡片网格 + 搜索 + 创建弹窗 + 删除确认）、`ProjectDetailView` 项目画布（信息头 + 新建方案/从模板创建/AI 搭配三入口 + 方案卡片 + 编辑弹窗 + 模板套用价格变动对比弹窗）；导航「项目」入口；QuoteBuilder 支持 ?projectId= 归属回跳
  - [x] 模板前端：SchemeListView 模板开关 + 标签筛选 + 模板列；SchemeDetailView 设模板（标签录入）/取消模板/套用此模板（选项目 + 价格对比）
  - [x] 全量验证：后端 364 测试零失败、对账脚本零差异、前端三件套全过；API 文档补项目与方案模板接口

- [x] RSDP × rooom 整合阶段 1：品牌首页 + 收藏夹 + 产品库筛选（2026-07-14 完成）
  - [x] 收藏夹后端：`user_favorite` 表（V4 迁移 + V1/reset 同步，5432 已应用）；`FavoriteService`/`FavoriteController` 四接口（收藏/取消/列表/批量 check，按用户隔离）；`FavoriteServiceTest` 8 用例全过，全量 352 测试零失败
  - [x] 收藏夹前端：`types/favorite.ts` + `api/favorite.ts`；产品库新增收藏列（心形按钮，加载后批量回填状态）；产品详情页操作行收藏按钮
  - [x] `FavoritesView.vue`（`/favorites`）：分组筛选 + 4 列卡片网格 + 多选批量「生成报价单」（复用 `/quotes/build?rspuIds=`）；导航「产品库 ▾」新增「我的收藏」
  - [x] 品牌首页重做：Hero 区（品牌蓝渐变 + 衬线大标题）+ 按空间/风格/品类/材质四维导航卡（点击带筛选参数跳产品库）+ 五步使用导览 + 快捷入口（按权限显隐）
  - [x] 产品库体验：支持 URL 筛选参数回填（`keyword`/`positioningLabel`/`sceneCode`/`materialTag`）；5 个下拉筛选迁移为左侧分组面板（单选高亮 + 重置），顶栏精简为搜索
  - [x] API 文档补充收藏夹接口；前端 lint + type-check + build 全部通过

- [x] RSDP × rooom 整合阶段 0：设计系统与导航重构（2026-07-14 完成）
  - [x] 新增 `web/src/styles/tokens.css` 设计 token：品牌色 `#2453FC` 四档、中性色、语义色、display/body 字体、圆角、页面留白、阴影、`html.dark` 暗色变量预留
  - [x] `App.vue` 接入 Naive UI `themeOverrides`：主色/圆角/字体与 token 对齐（Card 12px、Button 8px）
  - [x] 新增 `web/src/config/navigation.ts`：12 个入口抽取为声明式分组配置（permission/role 可见性、exact/prefix 高亮、activeExcludes 排除）
  - [x] 顶栏从 12 个平铺按钮收敛为 7 个分组：录入中心 ▾ / 产品库 ▾ / 搭配方案 ▾ / 报价 / 工厂 / 管理 ▾，单项组直达、多项组 hover 下拉
  - [x] 品牌顶栏视觉：RSDP 衬线品牌字（品牌蓝）+「家居全案平台」副标题，用户菜单改 quaternary 轻量样式
  - [x] 新增 `web/src/components/PageContainer.vue` 布局组件（标题区 display 字体 + 操作区插槽 + 1440px 限宽居中），首页试点套用
  - [x] 6 角色导航显隐矩阵与数据库权限种子核对一致；`pnpm lint` + `type-check` + `build` 全部通过

- [x] 前端体验与数据一致性闭环（2026-07-09 完成）
  - [x] `RskuDetailView.vue` 更新价格弹窗校验出厂价必须大于 0；详情页出厂价 null 保护并格式化为 `¥x.xx`
  - [x] `ProductDetailView.vue` RSKU 表格出厂价列 null 保护并格式化为 `¥x.xx`
  - [x] `QuoteBuilderView.vue` 小计使用 `Math.round(value * 100) / 100`，与后端 `BigDecimal.setScale(2, HALF_UP)` 对齐
  - [x] `QuoteBuilderView.vue` 数量输入框 `:min` 绑定当前 RSKU 的 MOQ；生成/导出/保存方案前统一校验数量 >= MOQ
  - [x] `RoomSchemeView.vue` 跳转生成报价单时携带 AI 推荐的 `rskuIds` 与 `quantities`
  - [x] `QuoteBuilderView.vue` 解析 URL 中的 `rskuIds`/`quantities` 并回填预选 RSKU 与数量
  - [x] `LoginView.vue` 支持 `redirect` 查询参数回跳，并做站内路径校验防止开放重定向
  - [x] 新增 `useRequestAbort` composable；ProductDetailView/FactoryDetailView/RskuDetailView/QuoteBuilderView/SchemeDetailView/ProductListView/RoomSchemeView 接入 AbortSignal，组件卸载时取消进行中的异步请求
  - [x] API 层（product/factory/rsku/scheme/quote/matching/dict/variant）统一支持可选 `signal` 参数
  - [x] `api/client.ts` 错误拦截器忽略 axios 取消错误
  - [x] `types/matching.ts` 的 `SchemeItem` 补充 `quantity` 字段
  - [x] 前端 `pnpm lint` + `pnpm type-check` + `pnpm run build` 全部通过

- [x] RSKU 导入业务规则治理三连击（2026-07-09 完成）
  - [x] `RskuImportService.preloadExisting` 增加 `deleted_at IS NULL` 过滤，导入时排除已软删除报价
  - [x] 复核确认 `RskuImportService.updateSingleRsku` 更新报价时已同步写入 `price_history`
  - [x] `RskuCreateRequest` / `RskuPriceUpdateRequest` / `RskuBatchFactoryQuote.factoryPrice` 将 `@PositiveOrZero` 改为 `@Positive`，强制出厂价大于 0
  - [x] 新增 `RskuControllerTest` 价格为 0 / 负数时的 400 校验测试
  - [x] 新增 `RskuImportServiceTest` 验证 `preloadExisting` 查询条件包含 `deleted_at IS NULL`
  - [x] 后端 285 个测试全部通过，前端 `pnpm run build` 通过

- [x] 实现 AI 识别后自动创建默认变体 + 审核页批量新增报价（2026-07-08 完成）
  - [x] `RspuVariantService` 新增 `initializeDefaultVariant`：AI 识别成功后，若 RSPU 无变体则自动创建默认变体
  - [x] `AsyncTaskProcessor` 在 `saveSuccess` 后调用默认变体初始化
  - [x] 后端新增 `POST /api/v1/products/{rspuId}/rsku/batch` 批量创建 RSKU 接口
  - [x] `RskuBatchCreateRequest` 改为「多选变体 + 多家工厂独立报价」结构：`variantIds` + `factoryQuotes`
  - [x] `RskuService.batchCreateRskus` 按（变体，工厂）组合创建，每个组合独立事务，失败不影响其他组合
  - [x] `SecurityConfig` 增加 `/api/v1/products/*/rsku/batch` 权限规则
  - [x] `ProductDetailView.vue` 增加「批量新增报价」弹窗，支持多选变体、多选工厂；提供「统一报价模板」，可快速为所有已选工厂填入相同报价，也支持单独调整每家工厂的报价
  - [x] 单条/批量新增报价弹窗在产品/变体等级缺失时提供等级选择
  - [x] 更新 `AsyncTaskProcessorTest` 以适配新增的变体初始化调用
  - [x] 后端 282 个测试全部通过，前端 `pnpm run build` 通过

- [x] 修复新品录入 AI API 401 失败（2026-07-08 完成）
  - [x] 诊断根因：DASHSCOPE_API_KEY 未配置或为占位符导致 DashScope 返回 401 Unauthorized
  - [x] `AiApiConfig` 增加启动时 API Key 校验，空值/占位符直接抛异常并给出明确提示
  - [x] `scripts/restart-backend.sh` 将 `DASHSCOPE_API_KEY` 列为必填环境变量
  - [x] `server/src/test/resources/application.yml` 补充测试环境 fallback Key，确保单元测试通过
  - [x] 后端 282 个测试全部通过

- [x] 角色可见性复核与前端权限闭环（2026-07-08 完成）
  - [x] `ProductDetailView.vue`：AI 识别记录卡片仅对平台运营人员（ADMIN/EDITOR）显示；AI 识别标签保持对所有可见用户可见
  - [x] `FactoryListView.vue`：工厂列表「编辑」按钮叠加 `isMyFactory` 判断，工厂管理员只能进入本厂详情
  - [x] `RskuDetailView.vue`：全库只读视图下隐藏「更新价格」「删除报价」按钮
  - [x] `HomeView.vue`：首页「产品库」入口按 `product:read` 权限显隐
  - [x] 后端 `ProductQueryService`：非平台运营人员不再返回完整 `ai_recognition` 历史，只返回空列表
  - [x] 后端 `TaskService`：异步任务查询增加归属校验，平台运营可看任意任务，其他用户只能看自己的任务
  - [x] `async_task` 表增加 `created_by` 字段；`ProductService` 创建任务时写入创建人
  - [x] 后端 282 个测试全部通过，前端 `pnpm run build` 通过

- [x] 修复产品库 → 报价单生成器链路 bug（2026-07-08 完成）
  - [x] `ProductListView.vue`：设计师在全库视图下也能点击「生成报价单」；返回产品库时清空已选项
  - [x] `QuoteBuilderView.vue`：`factoryPrice` 为 null 时禁用选项并做空值保护，避免 `toFixed` 报错；默认选中有报价的最低价 RSKU
  - [x] `RoomSchemeView.vue` / `SchemeListView.vue` / `SchemeDetailView.vue`：统一对可能为 null 的价格字段做 `?? 0` 兜底
  - [x] `scripts/seed-demo-data.sql`：为演示 RSKU 补充 `factory_price` 与复核状态，为 TEST 工厂补充 B 级能力
  - [x] 设计师/编辑员登录后，可完整走通「产品库全库视图 → 选中产品 → 生成报价单」流程
  - [x] 后端 282 个测试全部通过，前端 `pnpm run build` 通过

- [x] 本地演示数据补充脚本（2026-07-08 完成）
  - [x] 新增 `scripts/seed-demo-data.sh` / `scripts/seed-demo-data.sql`
  - [x] 从 `C:\develop\roomVip\测试案例图\案例图` 复制 10 个风格样例图 + 2 张工厂展示图到 `server/data/uploads/images/`
  - [x] 新增/补充演示数据：F001/F002 工厂、TEST 工厂完整资料、10 条 RSPU（覆盖 10 风格）、10 条变体、10 张图片、12 条 RSKU 报价
  - [x] 验证产品列表、工厂详情、RSKU 列表、图片访问均正常

- [x] 补齐权限矩阵文档与方法级授权（2026-07-08 完成）
  - [x] 新增 `docs/06-reference/03-角色权限矩阵.md`
  - [x] `RspuVariantController` / `RspuRelationController` / `MatchingController` / `RetrievalController` / `TaskController` / `SkuController` 补充 `@PreAuthorize`
  - [x] 前端 `api/client.ts` 403 时统一 `createDiscreteApi` toast 提示
  - [x] 用户管理页「关联工厂」改为从后端工厂列表下拉多选
  - [x] 工厂详情页编辑权限判定改为显式 `ADMIN/EDITOR` 角色校验，避免依赖 `isPlatformStaff` 语义歧义
  - [x] 修复 `FACTORY_ADMIN` 缺少 `factory:update` 权限导致无法编辑本厂资料的问题
  - [x] `UserService.updateUser` 角色变更后清除用户权限缓存，避免旧权限残留
  - [x] 新增 `scripts/restart-backend.sh` / `restart-backend.bat`，支持只重启后端而不影响前端 dev server
  - [x] 工厂列表页增加「编辑」操作列（有 `factory:update` 权限可见），方便直接进入工厂详情编辑
  - [x] 修复工厂详情编辑弹窗：多选字段清空时保存为空数组，而不是忽略该字段
  - [x] 优化退出登录逻辑：使用 `async/await` + `router.replace` 确保跳转稳定
  - [x] 后端测试通过，前端 `pnpm run build` 通过

- [x] 产品删除修复与数据范围校验闭环（2026-07-08 完成）
  - [x] 修复 RSPU 软删除使用 MyBatis-Plus `deleteById`，前端删除后列表不再展示
  - [x] 后端 `DataScopeHelper` 扩展 RSPU/工厂级校验：`canAccessRspu` / `assertCanAccessRspu` / `isOnlyAssociatedFactoryForRspu` / `canAccessFactory`
  - [x] `ProductQueryService.reviewProduct` / `updateProduct` / `deleteProduct` 接入数据范围校验
  - [x] `RspuVariantService.createVariant`、`RspuRelationService.create/update/deleteRelation`、`FactoryService`、`FactoryCapabilityService` 接入数据范围校验
  - [x] `SecurityConfig` 修正 `/admin/users/*/reset-password` 权限；`/products/factory-entry` 由 Controller 层要求 `FACTORY_ADMIN` 角色
  - [x] `ProductSummaryResponse` 返回 `factoryCodes`，支持列表页删除按钮按本厂关联显隐
  - [x] 前端 `ProductDetailView.vue`：RSPU 写操作按钮按本厂 RSKU 关联显隐；新增报价工厂下拉仅展示当前用户关联工厂
  - [x] 前端 `ProductListView.vue`：删除按钮叠加 `factoryCodes` 本厂关联判断
  - [x] 前端 `FactoryDetailView.vue`：编辑工厂资料/变更主等级/编辑兼做等级/同步能力档案仅本厂管理员可见
  - [x] 前端 `RskuDetailView.vue`：删除报价/更新价格仅本厂管理员可见
  - [x] 新增 `ForbiddenView.vue` / `NotFoundView.vue`，路由无权限跳转 `/403`，未知路由匹配 `/404`
  - [x] `/quotes/build?editSchemeId=` 路由守卫额外校验 `SCHEME_UPDATE` 权限
  - [x] 修复 `FactoryCapabilityServiceTest`、`FactoryServiceTest`、`ProductQueryServiceTest`、`RspuRelationServiceTest`、`RspuVariantServiceTest` 因 `DataScopeHelper` 引入导致的编译/运行错误
  - [x] 更新 `docs/07-issues/前端权限与业务目的复核报告-2026-07-08.md`
  - [x] 后端测试 282 个全部通过，前端 `pnpm lint` + `pnpm run build` 通过

- [x] 工厂与导入模块 V2：RSPU-工厂关联、交期规则、Excel 行级记录（2026-07-09 完成）
  - [x] 数据库脚本规范化：`init_db.sql` / `seed_data.sql` 重命名为 `V1__init_db.sql` / `V1__seed_data.sql`，新增 `V2__factory_module.sql`
  - [x] V2 数据库：新增 `rspu_factory_mapping`、`factory_lead_time_rule`、`excel_import_row`、`factory_capacity_assessment`、`rspu_price_column_mapping`、`excel_import_price_column` 等表
  - [x] V2 数据库：扩展 `excel_import_batch`（工厂/发货地/交期/MOQ 等字段）、`factory_master`（产能评分字段）；补充字典数据
  - [x] 后端实体与 Mapper：`RspuFactoryMapping`、`FactoryLeadTimeRule`、`ExcelImportRow`、`FactoryWarehouse`
  - [x] 后端 Service：`RspuFactoryMappingService`（按发货地筛选、主供工厂唯一性）、`FactoryLeadTimeRuleService`（动态交期计算）、`ExcelImportRowService`（行级记录）
  - [x] 后端 Controller：`RspuFactoryMappingController`、`FactoryLeadTimeRuleController`、扩展 `ExcelAiImportController` 增加行记录查询
  - [x] Excel AI 导入集成：导入时创建 RSPU-工厂映射、按规则计算动态交期、记录每行处理状态与生成实体
  - [x] 修复 `RspuFactoryMappingService.enrichAndConvert` 空仓库导致的 NPE
  - [x] 调整 `RspuFactoryMappingRequest` / `FactoryLeadTimeRuleRequest` 校验注解，避免路径变量与 `@Valid` 冲突
  - [x] API 文档：`docs/02-architecture/04-API设计.md` 补充工厂交期规则、RSPU-工厂关联、Excel 导入行记录接口
  - [x] 单元测试：`RspuFactoryMappingServiceTest`、`FactoryLeadTimeRuleServiceTest`、`ExcelImportRowServiceTest`、`RspuFactoryMappingControllerTest`、`FactoryLeadTimeRuleControllerTest`、`ExcelAiImportControllerTest`
  - [x] 更新现有测试：`ExcelAiImportServiceTest` 补充新依赖 mock；后端全部测试通过

- [x] Excel AI 辅助导入（含内嵌图片提取 + 多材质价格列）（2026-07-08 完成）
  - [x] 数据库脚本：新增 `excel_import_batch` 表及 `storage_path`、`price_columns` 字段
  - [x] 后端：`ExcelImportBatch` 实体 / Mapper、`ExcelAiImportService` / `ExcelAiImportController`
  - [x] 后端 DTO：`ExcelAiMappingRequest/Response`、`ExcelAiImportResult`、`ExcelAiImportFailure`、`ExcelAiImportStatusResponse`、`PriceColumnInfo`
  - [x] 后端工具：`ExcelImageExtractor`（Apache POI 内嵌图片提取，按 sheet+行分组）、`ExcelHeaderNormalizer`（表头清洗 + 多行父子表头合并）
  - [x] 后端接口：`POST /api/v1/products/excel-ai-import/preview`、`POST /api/v1/products/excel-ai-import/import`、`GET /api/v1/products/excel-ai-import/{batchId}`
  - [x] 后端逻辑：AI 字段映射预览 → 用户确认 → 逐行独立事务创建 RSPU/变体/图片/RSKU → 触发异步 AI 识别
  - [x] 多材质价格列：识别「价格-A级布」「价格-AA级布」等父子表头，同一 RSPU 生成多个变体 + 多 RSKU
  - [x] 复合表头：「型号品名」自动拆分为型号与品名；多行表头合并为「价格-材质名」
  - [x] 前端：`types/product.ts` 增加 `ExcelAiMappingResponse` / `ExcelAiImportResult` / `PriceColumnInfo` 类型
  - [x] 前端：`api/product.ts` 增加 `previewExcelAiMapping`、`confirmExcelAiImport`、`getExcelAiImportStatus`
  - [x] 前端：新增 `ProductExcelAiImportView.vue`（上传 → 确认映射 → 选择价格列 → 执行导入 → 轮询 AI 任务）
  - [x] 前端：更新 `router/index.ts` 与 `App.vue` 顶部导航，新增「Excel AI 导入」入口
  - [x] 单元测试：`ExcelImageExtractorTest`、`ExcelAiImportServiceTest`、`ExcelHeaderNormalizerTest` 覆盖内嵌图片提取、表头合并、多价格列导入
  - [x] 后端测试通过，前端 lint + type-check + build 通过
  - [x] 还原 `application-dev.yml` / `application.yml` 中的本地调试配置与敏感信息

- [x] 项目骨架搭建
  - [x] `AGENTS.md` 项目级指令
  - [x] `.ai-rules` AI 协作补充规则
  - [x] `README.md` 项目入口
  - [x] `Makefile` 统一命令入口
  - [x] `.gitignore` Git 忽略规则
- [x] 文档整理
  - [x] 按 `docs/01-*` ~ `07-*` 编号结构组织文档
  - [x] `docs/02-architecture/` 架构、数据库、API、实现说明文档
  - [x] `docs/03-guides/` 本地开发、编码规范、测试指南
  - [x] `docs/04-decisions/` 关键决策记录
  - [x] `docs/05-status/` 当前进度、待办事项
  - [x] `docs/06-reference/` 双层编码体系参考资料
  - [x] `docs/07-issues/` 问题排障日志
- [x] 数据库脚本
  - [x] `database/init_db.sql` PostgreSQL 建表 DDL
  - [x] `database/seed_data.sql` PostgreSQL 字典种子数据
  - [x] `database/reset_db.sql` 数据库重置脚本
- [x] 后端骨架
  - [x] SpringBoot `pom.xml`（Java 21 + Spring Boot 3.4 + MyBatis-Plus + PostgreSQL）
  - [x] 启动类 `RsdpApplication`
  - [x] 配置文件 `application.yml` / `application-dev.yml` / `application-prod.yml`
  - [x] `config/` 配置类（RestClient、WebMvc、MyBatis-Plus 分页等）
  - [x] `common/` 统一响应 `Result<T>`（含错误码）、`PageResult<T>`
  - [x] `exception/` 全局异常处理 `GlobalExceptionHandler`
  - [x] `entity/` / `mapper/`：RspuMaster、ImageAssets、AiRecognition、AsyncTask
  - [x] `controller/ImageController`：按 imageId 返回图片文件流
  - [x] `controller/ProductController`：新品录入、列表、详情、复核
  - [x] `controller/TaskController`：任务状态轮询
  - [x] `service/ImageService`：加载本地图片资源
  - [x] `service/ProductService`：保存图片、写 RSPU 草稿、创建异步任务
  - [x] `service/ProductQueryService`：产品列表、详情、复核
  - [x] `service/AsyncTaskProcessor`：后台 AI 识别处理
  - [x] `service/TaskService`：任务状态查询
  - [x] `service/VisionService`：调用 DashScope `qwen3-vl-plus`
  - [x] `util/ImageUploadValidator`：图片上传校验
  - [x] `dto/request/ProductListRequest`、`ProductReviewRequest`
  - [x] `dto/response/ProductSummaryResponse`、`ProductDetailResponse`
  - [x] `Dockerfile`
- [x] 测试补充
  - [x] `ProductServiceTest`（Mockito）
  - [x] `ProductQueryServiceTest`（Mockito）
  - [x] `ImageServiceTest`（Mockito）
  - [x] `VisionServiceTest`（WireMock）
  - [x] `ImageUploadValidatorTest`
- [x] 前端骨架
  - [x] `package.json` / `vite.config.ts` / `tsconfig.json`
  - [x] `main.ts` / `App.vue`
  - [x] `router/index.ts`：首页 `/`、录入页 `/entry`、产品库 `/products`、详情 `/products/:rspuId`
  - [x] `views/HomeView.vue`、`views/ProductEntryView.vue`、`views/ProductListView.vue`、`views/ProductDetailView.vue`
  - [x] `api/client.ts`：Axios 实例与统一错误处理
  - [x] `api/product.ts`、`api/task.ts`：API 封装
  - [x] `types/task.ts`：任务相关类型
  - [x] `types/product.ts`：产品相关类型
  - [x] 图片上传并调用 `POST /api/v1/products/entry`
  - [x] 前端任务状态轮询展示
  - [x] 产品列表分页查询与详情展示
  - [x] 产品库/详情页图片通过 `/api/v1/images/{imageId}` 正常显示
  - [x] 人工复核确认/存疑操作
  - [x] 顶部全局导航栏
  - [x] `Dockerfile`
- [x] 存储抽象层
  - [x] `StorageService` 接口：`store` / `get` / `exists` / `delete`
  - [x] `LocalStorageService`：本地磁盘实现，兼容旧版绝对路径存储记录
  - [x] `MinioStorageService` + `MinioConfig`：MinIO 实现（`storage.type=minio` 激活）
  - [x] `StorageProperties`：统一 `storage.type` / `storage.local-path` / `storage.minio.*` 配置
  - [x] `ProductService` / `ImageService` / `AsyncTaskProcessor` / `VisionService` 接入 `StorageService`
  - [x] `LocalStorageServiceTest` 单元测试
  - [x] 配置更新：`application.yml` / `application-dev.yml` / `application-prod.yml`
- [x] 审计日志自动记录
  - [x] `AuditLog` 实体 + `AuditLogMapper` + `AuditLogService`
  - [x] `PostgreSqlJsonbTypeHandler`：支持 Java 对象写入 PostgreSQL jsonb
  - [x] `MybatisTypeHandlerConfig`：让 MyBatis-Plus JacksonTypeHandler 使用 Spring 的 ObjectMapper
  - [x] 接入 `ProductService.createEntry`（rspu_master / image_assets CREATE）
  - [x] 接入 `ProductQueryService.reviewProduct`（rspu_master REVIEW）
  - [x] 接入 `FactoryService.createFactory`（factory_master CREATE）
  - [x] 接入 `RskuService.createRsku`（rsku_supply CREATE）
  - [x] 接入 `AsyncTaskProcessor` AI 识别后的 RSPU 字段更新（rspu_master UPDATE）
  - [x] `AuditLogServiceTest` 单元测试
- [x] B 模块：工厂管理 + RSKU 报价录入
  - [x] 后端 `FactoryMaster` / `RskuSupply` 实体与 Mapper
  - [x] 后端 `FactoryService` / `FactoryController`（工厂增删改查）
  - [x] 后端 `RskuService` / `RskuController`（按 RSPU 报价列表、新增报价）
  - [x] 后端 `FactoryServiceTest`、`RskuServiceTest` 单元测试
  - [x] 前端 `FactoryListView.vue`（新增工厂 + 列表）
  - [x] 前端 `types/factory.ts`、`types/rsku.ts`
  - [x] 前端 `api/factory.ts`、`api/rsku.ts`
  - [x] 前端 `ProductDetailView.vue` 增加 RSKU 报价列表与新增报价弹窗
  - [x] 路由与导航：新增 `/factories` 路由和顶部"工厂管理"入口
- [x] 部署配置
  - [x] `deploy/docker-compose.yml`
  - [x] `deploy/nginx/nginx.conf`
  - [x] `deploy/.env.example`
- [x] 运维脚本
  - [x] `scripts/setup.sh`
  - [x] `scripts/test.sh`
  - [x] `scripts/backup_db.sh`
  - [x] `ops/anchor_encode.sh`
- [x] 「风格数据库 Skill」方案与导入数据格式文档
  - [x] `docs/04-decisions/005-风格数据库Skill方案与可行性分析.md`
  - [x] `docs/03-guides/04-风格数据库导入数据格式.md`
  - [x] 数据库设计文档已补充风格库表结构
  - [x] API 设计文档已补充风格库接口
- [x] 风格数据库种子数据（2026-07-03）
  - [x] 新增 `database/seed_style_knowledge.sql`：基于 `data/style-knowledge/raw/*.json` 自动生成
  - [x] 正向案例 11 个（每个风格百科 1 个）+ 反向案例 9 个（来自失败案例分析）
  - [x] 元素拆解 247 条 + 搭配公式 11 条
  - [x] ⚠️ **当前数据仅覆盖客厅（LIVING_ROOM）**：书房、卧室、餐厅等空间尚未建立案例与公式
  - [x] 新增生成脚本 `scripts/generate_style_knowledge_seed.js`
  - [x] `Makefile` 增加 `seed-style` 命令
  - [x] 本地数据库已重置并验证导入
- [x] 环境升级（2026-06-25）
  - [x] JDK 从 17 升级到 21 LTS（`D:\develop\Java\jdk-21`）
  - [x] `pom.xml` `<java.version>` 改为 21
  - [x] Docker PostgreSQL 端口因 Windows Docker Desktop 5432 冲突，改为映射 5433→5432
  - [x] `application-dev.yml` JDBC URL 端口同步改为 5433
  - [x] 本地启动后端需显式指定 `--spring.profiles.active=dev` 或 `-Dspring-boot.run.profiles=dev`
  - [x] 创建远程分支 `hha-create-a-branch` 并推送
  - [x] 前端启动验证通过（Vite 6.4.3，http://localhost:5173）
  - [x] 后端启动验证通过（Java 21.0.11，Tomcat :8081）
  - [x] 6 条产品录入测试通过（DashScope `qwen3-vl-plus` AI 识别正常）
- [x] 前端 ESLint 配置（2026-06-26）
  - [x] 新增 `web/eslint.config.js`：Vue 3 + TypeScript 5 + 浏览器全局变量
  - [x] 安装依赖：`eslint` `prettier` `@eslint/js` `typescript-eslint` `eslint-plugin-vue` `globals`
  - [x] 替换 `package.json` 的 lint no-op：`eslint src --ext .ts,.vue`
  - [x] `pnpm lint` 0 errors、`pnpm type-check` 通过
  - [x] 关闭强格式类规则（max-attributes-per-line、singleline-content-newline、attributes-order）
  - [x] 将 `types/task.ts` 中 `Record<string, any>` 改为 `Record<string, unknown>`
- [x] 产品录入支持多图/非主图场景（2026-06-26）
  - [x] 后端 `ProductController.entry` 改为接收 `List<MultipartFile> images`
  - [x] 后端 `ProductService.createEntry`：一个 RSPU + 多张图片，第一张为主图并触发 AI 识别
  - [x] 非主图 `image_type=detail`、`is_primary=false`，仅存档不参与 AI 识别
  - [x] 返回 `imageIds` 列表，所有图片记录写入审计日志
  - [x] 更新 `ProductServiceTest`：单图、多图、空列表测试
  - [x] 前端 `api/product.ts` 新增 `uploadProductImages(files)`
  - [x] 前端 `types/task.ts` 增加 `imageIds`
  - [x] 前端 `ProductEntryView.vue`：一次上传多图为一个产品，任务卡片展示多图缩略图
- [x] 工厂详情页 / RSKU 详情页（2026-06-26）
  - [x] 后端 `RskuService.getRsku(rspuId, rskuId)` + `GET /api/v1/products/{rspuId}/rsku/{rskuId}`
  - [x] 后端 `RskuService.listByFactory(factoryCode)` + `GET /api/v1/factories/{factoryCode}/rsku`
  - [x] 更新 `RskuServiceTest`
  - [x] 前端新增 `FactoryDetailView.vue`（`/factories/:factoryCode`）
  - [x] 前端新增 `RskuDetailView.vue`（`/products/:rspuId/rsku/:rskuId`）
  - [x] 前端 `FactoryListView.vue` 表格行可点击进入详情
  - [x] 前端 `ProductDetailView.vue` RSKU 表格行可点击进入详情
  - [x] 更新路由
- [x] 字典基础设施 + 报价单 + AI 搭配方案（2026-06-26）
  - [x] 后端 `CategoryDict` 实体 + Mapper + `DictService` + `GET /api/v1/dicts/{dictType}`
  - [x] `database/seed_data.sql` 新增 `room_type`（客厅/卧室/餐厅/书房/办公室）和 `quote_confidence`（高/中/低）
  - [x] 后端 `QuoteService` + `QuoteController` + `POST /api/v1/quotes/generate`
  - [x] 后端 `QuoteServiceTest`
  - [x] 后端扩展 `VisionService.chatText` 支持纯文本对话
  - [x] 后端 `AiMatchingService` + `MatchingController` + `POST /api/v1/matching/room-scheme`
  - [x] 后端 `AiMatchingServiceTest`（WireMock 模拟 AI 返回）
  - [x] 后端 `POST /api/v1/matching/recommend` 预留接口
  - [x] 前端 `dict.ts` / `types/dict.ts`，字典下拉从接口动态获取
  - [x] 前端 `ProductListView.vue` 改为从字典获取复核状态，增加产品多选 +「生成报价单」
  - [x] 前端 `ProductDetailView.vue` 报价置信度改为从字典获取
  - [x] 前端新增 `QuoteBuilderView.vue`（`/quotes/build`）
  - [x] 前端新增 `RoomSchemeView.vue`（`/matching/room-scheme`）
  - [x] 前端新增 `AnchorMatchingView.vue`（`/matching/anchor`）骨架
  - [x] 前端 `App.vue` 新增「报价单生成器」「AI 搭配方案」导航入口
  - [x] 更新路由
  - [x] 后端测试 42 个全部通过，前端 lint + type-check 通过
  - [x] 手动验证：字典接口、报价单生成、AI 空间搭配方案接口均正常
- [x] 业务规则完善（2026-06-26）
  - [x] 阶段 1：字典表补齐（category 增加 DT/CB/BD，新增 product_status）+ 启用 `rspu_variant` 变体体系
  - [x] 阶段 2：新品录入支持品类选择 + AI 识别后写入 `rspu_style` / `rspu_scene` 关联表
  - [x] 阶段 3：RSKU 必须关联变体 + 变体合法性校验 + `price_history` 价格历史
  - [x] 阶段 4：产品列表按风格码/场景码（`rspu_style` / `rspu_scene`）精确查询
  - [x] 阶段 5：`material_tags` 存字典码 + 材质标签 JSONB 精确查询
  - [x] 阶段 6：工厂等级变更不改工厂代码（`PUT /factories/{code}/level`）
  - [x] 阶段 7：RSPU 业务编码服务 `RspuCodeService` + `rspu_code_counter` 表（可选能力，未默认启用）
  - [x] 前端：变体管理、价格历史、工厂等级变更、风格/场景/材质筛选
  - [x] 更新 `API设计.md` 与 `seed_data.sql`
  - [x] 后端测试 47 个全部通过，前端 lint + type-check 通过
  - [x] Code Review 修复（2026-06-26）
    - [x] 修复 `RspuVariantService` 变体 ID 并发竞态：新增 `variant_code_counter` 表 + `VariantCodeMapper` 原子递增
    - [x] 修复多写服务缺少 `@Transactional`：`RskuService`、`FactoryService`、`ProductQueryService`
    - [x] 配置 `@Async` 有界线程池：`AsyncConfig` + `taskExecutor` + `application.yml` 参数
    - [x] 修复 RSKU 重复报价检查竞态：移除先查后插，捕获 `DataIntegrityViolationException` 转换为业务异常
    - [x] 统一 `scene_tags` 也存储字典码（与 `material_tags` 一致）
    - [x] 修复 `DictResolverService.resolveCodesByNames` 同时匹配 `dictName` 与 `dictNameEn`
    - [x] 修复复核备注 `reviewComment` 未持久化：`rspu_master.review_comment` 字段 + 实体 + 接口写入
    - [x] 增加工厂等级字典校验：`FactoryService` 创建/更新时检查 `factory_level` 字典
    - [x] 增加变体字典码校验：`RspuVariantService` 创建时校验 size/color/material 字典码
    - [x] 启用 `material_tags` JSONB GIN 索引
    - [x] 更新 `FactoryServiceTest`、`RspuVariantServiceTest`、`RskuServiceTest`
    - [x] 后端测试 57 个全部通过，前端 lint + type-check 通过
- [x] 搭配方案管理完善（2026-06-29）
  - [x] 修复 `SchemeItem` 自增主键溢出：`@TableId(type = IdType.AUTO)`
  - [x] 修复请求/响应 UTF-8 编码：`application.yml` 强制编码 + 前端 `Content-Type` 带 charset
  - [x] 编辑方案：`PUT /api/v1/schemes/{schemeId}` + `QuoteBuilderView` 编辑模式
  - [x] RSKU 失效防御：`QuoteService.generateQuote` 批量校验失效 RSKU
  - [x] 方案快照与价格变动提示：`QuoteResponse.priceChanges` 对比保存时价格与当前价
  - [x] 同一 RSPU 多 RSKU 提示：`QuoteBuilderView` / `SchemeDetailView` 去重警告
  - [x] 方案名称长度与唯一性校验：`@Size(max=128)` + 同创建人下 active 方案同名校验
  - [x] 方案项批量插入与数量上限：`SchemeItemMapper.insertBatch` + `@Size(max=50)`
  - [x] 后端测试 71 个全部通过，前端 lint + type-check 通过
- [x] 工厂兼做等级能力（2026-06-29）
  - [x] 新增 `factory_level_capability` 表：`id` + `factory_code` + `level_code` + `is_primary`
  - [x] 新增 `FactoryLevelCapability` 实体与 `FactoryLevelCapabilityMapper`
  - [x] `FactoryCreateRequest` / `FactoryResponse` 增加 `capableLevels`
  - [x] 新增 `PUT /api/v1/factories/{code}/capable-levels` 更新兼做等级
  - [x] `FactoryService` 创建/更新时同步维护能力等级表，主等级自动标记 `is_primary`
  - [x] 前端 `FactoryListView.vue` 增加兼做等级多选
  - [x] 前端 `FactoryDetailView.vue` 展示主等级/兼做等级，支持编辑兼做等级
  - [x] 更新 `FactoryServiceTest` 覆盖能力等级场景
  - [x] 后端测试 71 个全部通过，前端 lint + type-check 通过
- [x] 原厂搭配关系（2026-06-29）
  - [x] 新增 `rspu_relation` 表：表达 RSPU → RSPU 的 official / ai_verified / exclude 关系
  - [x] 新增 `RspuRelation` 实体、`RspuRelationMapper`、`RspuRelationService`、`RspuRelationController`
  - [x] 新增 `GET/POST/PUT/DELETE /api/v1/products/{rspuId}/relations` 接口
  - [x] `ProductDetailResponse` 增加 `officialMatches` / `matchedBy`
  - [x] `ProductDetailView.vue` 展示「官方搭配」与「适配来源」，支持添加/删除关系
  - [x] 前端新增 `types/relation.ts`、`api/relation.ts`
  - [x] 新增 `RspuRelationServiceTest`、`ProductQueryServiceTest` 覆盖关系查询与详情组装
  - [x] 后端测试 93 个全部通过，前端 lint + type-check 通过
  - [x] 更新 `docs/02-architecture/04-API设计.md`
- [x] 报价单 Excel 导出（2026-06-30 完成）
  - [x] 后端 `server/pom.xml` 引入 EasyExcel 3.3.4
  - [x] 后端新增 `QuoteExportService`，生成含「报价明细」和「汇总」两个 sheet 的 `.xlsx`
  - [x] 后端 `QuoteController` 增加 `POST /api/v1/quotes/export` 文件下载接口
  - [x] 前端 `api/quote.ts` 新增 `exportQuote`，通过 Blob 触发浏览器下载
  - [x] 前端 `QuoteBuilderView.vue` / `SchemeDetailView.vue` 增加「导出 Excel」按钮
  - [x] 后端测试 `QuoteExportServiceTest` 覆盖 Excel 字节输出与空列表异常
  - [x] 更新 `docs/02-architecture/04-API设计.md`
- [x] RSPU / RSKU 软删除（2026-06-30 完成）
  - [x] 后端 `ProductQueryService.deleteProduct` + `DELETE /api/v1/products/{rspuId}`
  - [x] 后端 `RskuService.deleteRsku` + 新增 `SkuController`：`DELETE /api/v1/sku/{rskuId}`
  - [x] 软删除操作写入审计日志（`logDelete`）
  - [x] 后端测试 `ProductQueryServiceTest` / `RskuServiceTest` 覆盖删除成功、重复删除、不存在场景
  - [x] 前端 `api/product.ts` 新增 `deleteProduct`，`api/rsku.ts` 新增 `deleteRsku`
  - [x] 前端 `ProductListView.vue` / `ProductDetailView.vue` 增加 RSPU 删除入口（二次确认）
  - [x] 前端 `ProductDetailView.vue` / `RskuDetailView.vue` 增加 RSKU 删除入口（二次确认）
  - [x] 更新 `docs/02-architecture/04-API设计.md`
- [x] 以图搜图 / 以文搜图（2026-06-30 完成）
  - [x] 后端 `EmbeddingService`：接入 DashScope `multimodal-embedding-v1` 生成图片/文本 embedding
  - [x] 后端 `ChromaDbClient` + `ChromaDbProperties`：封装 ChromaDB REST API（集合、upsert、query、delete），已升级为 v2 API
  - [x] 后端 `AiApiConfig`：新增 `embeddingRestClient` / `chromaRestClient`
  - [x] 后端 `AsyncTaskProcessor`：新品录入 AI 识别成功后生成 embedding，写入 `rspu_master.style_vector` 与 ChromaDB
  - [x] 后端 `RetrievalService` + `RetrievalController`：`POST /api/v1/retrieval/similar` 三层检索（向量召回 → 元数据过滤 → 规则重排）
  - [x] 后端 `VectorBackfillService` + `AdminController`：`POST /api/v1/admin/vectors/backfill` 存量图片向量回填
  - [x] 后端 `ProductQueryService.deleteProduct`：软删除时同步删除 ChromaDB 向量
  - [x] 后端测试 `AsyncTaskProcessorTest`、`RetrievalServiceTest`、`RetrievalControllerTest` 覆盖核心流程
  - [x] 前端 `api/retrieval.ts`、`types/retrieval.ts`
  - [x] 前端新增 `VisualSearchView.vue`（`/visual-search`）：支持图片上传/文本输入、类别/风格过滤、结果展示
  - [x] 前端 `App.vue` 顶部导航新增「以图搜图」入口
  - [x] 更新 `docs/02-architecture/04-API设计.md` 与 `docs/05-status/当前进度.md`
- [x] P0 修复与边界加固（2026-07-01 完成）
  - [x] 修复 `database/reset_db.sql` 与 `database/init_db.sql` 结构不一致
  - [x] 拆分 `AsyncTaskProcessor` 长事务：新增 `AiRecognitionPersistenceService`
  - [x] `ProductQueryService.deleteProduct` 改为发布 `RspuDeletedEvent` 异步清理 ChromaDB 向量
  - [x] 修复 `FactoryMaster` JSONB 字段映射并扩展工厂 API 透传 `certification` / `engineeringCases`
  - [x] 修复 `ProductDetailView.vue` 同组件路由复用不刷新
  - [x] 限制 `QuoteBuilderView.vue` URL 产品 ID 数量（去重后截断至 50）
  - [x] 敏感价格字段加密：`RskuSupply.factoryPrice` / `SchemeItem.factoryPrice` 使用 AES-256-GCM + TypeHandler 自动加解密
  - [x] 后端测试 113 个全部通过，前端 lint + type-check 通过
- [x] 以图搜图性能优化（2026-07-01 完成）
  - [x] 消除 `RetrievalService` N+1 查询：图片 URL 与 RSPU 元数据均改为批量查询
  - [x] 更新 `RetrievalServiceTest` 以覆盖批量查询路径
  - [x] 后端测试 121 个全部通过，前端 lint + type-check 通过
- [x] Controller 层补齐参数校验（2026-07-01 完成）
  - [x] 所有 Controller 类加 `@Validated`
  - [x] 所有 `@PathVariable String` 参数加 `@NotBlank`
  - [x] `ProductController.list` 加 `@Valid`，`ProductListRequest.size` 限制 1~100
  - [x] `AdminController.backfillVectors` 的 `batchSize` 限制 1~1000
  - [x] `RetrievalController.searchSimilar` 加 `@Valid`，`SimilarProductRequest.topK` 限制 1~100
  - [x] `ProductController.entry` 批量图片上传加空文件 / 类型 / 大小校验（单图上限 10MB）
  - [x] `GlobalExceptionHandler` 增加 `ConstraintViolationException` 统一处理
  - [x] 后端测试 121 个全部通过，前端 lint + type-check 通过
- [x] Service 层外部调用增强健壮性（2026-07-01 完成）
  - [x] 新增 `ExternalServiceException` 与全局异常处理
  - [x] `AiApiConfig` 为 AI / Embedding / ChromaDB 三个 RestClient 配置连接/读取超时与 IO 重试拦截器
  - [x] `application.yml` 增加 `timeout-seconds`、`max-retries`、`embedding-timeout-seconds`、`embedding-max-retries`、`chromadb.timeout-seconds`、`chromadb.max-retries`
  - [x] `ChromaDbClient` 增强集合查询/创建/upsert/query/delete 的判空与异常包装
  - [x] `EmbeddingService` 增强图片流判空、API 响应判空，使用 `ExternalServiceException`
  - [x] `VisionService` 增强图片流判空、AI 响应逐层判空，使用 `ExternalServiceException`
  - [x] 后端测试 121 个全部通过，前端 lint + type-check 通过
- [x] 治理列表页 N+1 查询（2026-07-01 完成）
  - [x] `ProductQueryService.listProducts` 风格/场景筛选改用 `EXISTS` 子查询
  - [x] `ProductQueryService.toSummary` 批量查询主图
  - [x] `RspuRelationService.toResponse` 批量查 RSPU / 图片 / 最低价
  - [x] `RskuService.toResponse` 批量查工厂
  - [x] `QuoteService.generateQuote` 批量查 RSKU / RSPU / 工厂 / 图片
  - [x] `AiMatchingService` 批量查 candidate 最低价 / cheapest RSKU / 主图 / 工厂
  - [x] 同步更新 `ProductQueryServiceTest`、`RspuRelationServiceTest`、`RskuServiceTest`、`QuoteServiceTest`、`AiMatchingServiceTest`
  - [x] 后端测试 121 个全部通过，前端 lint + type-check 通过
- [x] 数据库与 MyBatis-Plus 一致性加固（2026-07-01 完成）
  - [x] `RspuStyle` / `RspuScene` / `CategoryDict` 复合主键字段标注 `@TableId` / `@TableField`，补充复合主键说明
  - [x] `RspuMaster` / `RskuSupply` / `RspuRelation` / `RspuVariant` / `FactoryMaster` / `Scheme` 启用 `@TableLogic` 逻辑删除
  - [x] 移除 Service 层手动的 `isNull("deleted_at")` 条件，交由 MyBatis-Plus 自动过滤
  - [x] `scheme_item.factory_code` 增加外键约束
  - [x] `rsku_supply` 唯一索引拆分为部分唯一索引（variant_id IS NULL / IS NOT NULL），并排除已软删除记录
  - [x] `factory_level_capability` 增加“唯一主评级”部分唯一索引
  - [x] `SchemeItemMapper.insertBatch` 增加空集合保护 `insertBatchSafe`
  - [x] `scheme_item.scheme_item_id` DDL 改为 `BIGSERIAL`
  - [x] 同步更新 `SchemeServiceTest`
  - [x] 后端测试 121 个全部通过，前端 lint + type-check 通过
- [x] 业务规则边界校验（2026-07-01 完成）
  - [x] `RspuCodeService` 注入 `DictService` 校验 category/style/size 字典存在性，seq 超过 999 抛业务异常
  - [x] `RspuVariantService` 确认 `variantCodeMapper.allocateSequence` 原子分配，插入时捕获 `DataIntegrityViolationException` 兜底
  - [x] `SchemeService.create/update` 校验 `items` 非空、方案名称非空
  - [x] `FactoryService.extendCapability` 显式标注 `@Transactional(propagation = Propagation.REQUIRED)` 并补充事务语义说明
  - [x] 同步更新 `RspuCodeServiceTest`
  - [x] 后端测试 125 个全部通过，前端 lint + type-check 通过
- [x] 前端边界加固（2026-07-01 完成）
  - [x] `api/client.ts` 统一判断 `ApiResult.code`（非 200 抛 `ApiError`）
  - [x] 上传接口使用独立 `uploadClient`（120s 超时）
  - [x] `ProductEntryView.vue` 校验文件数量、单文件 ≤10MB、真实图片 MIME 类型
  - [x] `VisualSearchView.vue` 组件卸载时清理 ObjectURL
  - [x] `QuoteBuilderView.vue` 价格改用「分」累加避免浮点精度，价格为 0 显示 `¥0.00`
  - [x] `QuoteBuilderView.vue` `MAX_ITEMS=50` 在 `loadData` 中真正截断
  - [x] 产品/工厂/方案/RSKU 详情页校验路由参数非空
  - [x] `FactoryListView.vue` / `SchemeListView.vue` 增加前端本地分页
  - [x] 后端测试 125 个全部通过，前端 lint + type-check 通过
- [x] 历史价格数据加密迁移（2026-07-01 完成）
  - [x] 新增 `MigrationProperties` 配置类，开关 `rsdp.migration.encrypt-prices.enabled` 默认关闭
  - [x] 新增 `PriceEncryptionMigration` CommandLineRunner，迁移 `rsku_supply` 与 `scheme_item` 的明文 `factory_price`
  - [x] 幂等执行：已加密记录自动跳过，明文记录加密更新，非法值记录失败日志
  - [x] `application.yml` / `application-dev.yml` 增加迁移配置示例
  - [x] `PriceEncryptionMigrationTest` 覆盖关闭、加密、跳过、非法值、scheme_item 场景
  - [x] 后端测试 129 个全部通过
- [x] 工厂能力等级在报价/推荐中落地（2026-07-01 完成）
  - [x] `QuoteService.generateQuote` 增加能力等级校验：产品等级不在工厂 `capableLevels` 中的 RSKU 批量拒绝
  - [x] `AiMatchingService` 的 `batchMinPrices` / `batchCheapestRskus` 按能力等级过滤，推荐结果只选具备能力的工厂
  - [x] `RskuResponse` 增加 `factoryCapableLevels`，`RskuService` 批量填充
  - [x] 前端 `QuoteBuilderView.vue` RSKU 选项与已选标签显示能力匹配状态（正常/警告）
  - [x] 更新 `QuoteServiceTest`、`AiMatchingServiceTest`、`RskuServiceTest`
  - [x] 后端测试 131 个全部通过，前端 lint + type-check 通过
- [x] 以图搜图效果优化（2026-07-01 完成）
  - [x] `RetrievalService` 图片查询时调用 `VisionService.recognizeImage` 提取查询图标签
  - [x] 将查询图风格、主色、材质、场景、六维标签与候选 RSPU 标签对比，按规则重排加分
  - [x] 实现跨 RSPU 同款去重：按 `category + style + color + material` 指纹保留向量分最高的代表
  - [x] `SimilarProductResponse` 增加 `aestheticsConfidence`，前端 `VisualSearchView.vue` 展示置信度与向量/综合得分
  - [x] 向量召回候选池从 `topK * 3` 扩大到 `topK * 5`
  - [x] `VisionService` 识别失败时降级为空标签，不影响向量检索
  - [x] 更新 `RetrievalServiceTest`，覆盖文本查询、图片标签加成、同款去重、AI 识别失败降级
  - [x] 后端测试 132 个全部通过，前端 lint + type-check 通过
- [x] 工厂 Excel 报价单批量导入（2026-07-01 完成）
  - [x] 新增 `RskuImportService`，使用 EasyExcel 解析 Excel
  - [x] 新增 `RskuImportRow` / `RskuImportResult` / `RskuImportFailure` DTO
  - [x] 校验工厂/RSPU/变体存在性、出厂价正数、产品等级有效、工厂能力等级匹配
  - [x] 支持「存在时跳过」与「存在时更新」两种模式
  - [x] `RskuSupplyMapper` 新增 `insertBatch` / `insertBatchSafe` 批量插入
  - [x] 新增 `RskuImportController`：`GET /api/v1/rsku/import-template` 下载模板，`POST /api/v1/rsku/import` 上传导入
  - [x] 前端新增 `RskuImportView.vue`：下载模板、上传 Excel、展示导入结果与失败明细
  - [x] 更新 `RskuImportServiceTest`，覆盖成功导入、跳过、更新、校验失败、空文件场景
  - [x] 后端测试 137 个全部通过，前端 lint + type-check 通过
- [x] 工厂 Excel 导入代码 Review 与隐藏问题修复（2026-07-01 完成）
  - [x] 修复审计日志 `logUpdate` 在更新后才快照，导致 oldValue == newValue 的问题
  - [x] 修复 `isValidProductLevel(null)` 可能触发 NPE 的问题
  - [x] 预加载工厂能力等级并缓存 `factory_level` 字典，避免每行重复查询
  - [x] 增加 Excel 内部重复行检测（同一工厂+变体），防止重复插入触发唯一索引冲突
  - [x] 批量插入失败时自动回退到逐行插入并捕获 `DataIntegrityViolationException`，提升并发导入容错
  - [x] 增加文件大小（≤10MB）与 MIME 类型（Excel/CSV）校验
  - [x] 扩展 `parseExcel` 异常处理，捕获格式错误并给出友好提示
  - [x] 更新 `RskuImportServiceTest`：新增重复行、审计快照、文件类型校验测试
  - [x] 后端测试 140 个全部通过
- [x] RSKU 报价导入模板中文化与业务边界校验增强（2026-07-01 完成）
  - [x] `RskuImportRow` 所有 `@ExcelProperty` 改为中文表头（RSPU编码、工厂编码、变体编码、出厂价等）
  - [x] `RskuImportService` 增加字段长度校验（RSPU/变体 ≤64、工厂编码 ≤16、工厂SKU ≤64 等）
  - [x] 增加数值边界校验：出厂价 ≤99,999,999.99、交期 0~999、MOQ 0~999,999、质保 0~50
  - [x] 支持中文值自动映射：报价置信度 `高/中/低` → `high/mid/low`，产品等级 `S级/S级战略厂` → `S`
  - [x] 导入前自动 trim 所有字符串字段
  - [x] 更新 `RskuImportServiceTest`：中文表头、中文值映射、边界校验测试
  - [x] 后端测试 142 个全部通过，前端 lint + type-check 通过
  - [x] 重启后端并验证 `/api/v1/rsku/import-template` 下载成功
- [x] 冒烟测试环境启动（2026-07-01 完成）
  - [x] 启动基础设施：PostgreSQL（5433）、ChromaDB（8000）、Redis（6379）、MinIO（9000/9001）
  - [x] 修复启动阻塞：`RskuSupplyMapper.xml` 的 DTD system ID 错误（`mybatis-3.0.dtd` → `mybatis-3-mapper.dtd`）
  - [x] 后端 `mvn spring-boot:run` 在 `:8081` 启动成功
  - [x] 前端 `pnpm dev` 在 `http://localhost:5173/` 启动成功
  - [x] `GET /api/v1/dicts/factory_level` 接口响应正常
  - [x] 补充工厂列表页入口：`FactoryListView.vue` 新增「批量导入 RSKU 报价」按钮，跳转 `/rsku/import
  - [x] 前端 lint + type-check 通过
- [x] Code Review P0 问题集中修复（2026-07-02 完成）
  - [x] 前端 FormData 上传：移除 `web/src/api/product.ts` / `rsku.ts` / `retrieval.ts` 中手动设置的 `Content-Type: multipart/form-data`，由 Axios 自动生成带 boundary 的 header
  - [x] 前端详情页路由刷新：`FactoryDetailView.vue` / `RskuDetailView.vue` 将路由参数改为 `computed`，并配合 `onBeforeRouteUpdate` 重新加载
  - [x] AI 搭配推荐风格过滤：`AiMatchingService.fetchCandidates` 改为按 `rspu_style.style_code` 过滤，语义正确
  - [x] 生产配置统一：`application-prod.yml` 统一使用 `rsdp.ai.*` / `rsdp.chromadb.*` 层级；`application.yml` 移除默认激活的 `spring.profiles.active: dev`
  - [x] 价格解密兜底：`AesEncryptionUtil.decrypt` 对历史遗留明文价格兜底返回并告警，避免未迁移数据导致查询崩溃
  - [x] 后端测试 143 个全部通过，前端 lint + type-check 通过
- [x] RSPU/RSKU 主键与业务编码分离决策（2026-07-02 完成）
  - [x] 明确风格、尺寸、材质、工厂均为可变属性，不适合写入主键
  - [x] 决策：`rspu_id` / `rsku_id` 继续使用 UUID 主键；业务编码仅作为展示字段（未来可新增 `rspu_code` / `rsku_code`）
  - [x] 多风格/多尺寸/多材质通过 `rspu_style` / `rspu_scene` / `rspu_variant` / `rsku_supply` 关联表表达
  - [x] 更新 `AGENTS.md`、`docs/06-reference/01-业务规则说明.md`、新增 ADR `004-为什么RSPU和RSKU使用UUID主键而非业务编码作为主键.md`
  - [x] 关闭「用业务编码替代 UUID 主键」的 P0 目标，`RspuCodeService` 作为可选能力保留
- [x] 产品人工批量导入（Excel）（2026-07-02 完成）
  - [x] 数据库：`rspu_master` 增加 `external_code` 字段及唯一索引，同步更新实体与 DDL/重置脚本
  - [x] 后端：`ProductImportRow` / `ProductImportResult` / `ProductImportFailure` DTO
  - [x] 后端：`ProductImportService`：EasyExcel 解析、字典预加载校验、RSPU/变体/图片写入、跳过/更新模式、图片下载失败仅记录明细
  - [x] 后端：`ProductImportController`：`GET /api/v1/products/import-template` 下载模板、`POST /api/v1/products/import` 上传导入
  - [x] 后端：扩展 `StorageService.store(InputStream, ...)`，本地与 MinIO 实现已补齐
  - [x] 后端：`ProductImportServiceTest` 覆盖创建、更新、跳过、重复行、字典校验、图片成功/失败、空文件、超大文件场景
  - [x] 前端：`types/product.ts` 增加 `ProductImportResult` / `ProductImportFailure`
  - [x] 前端：`api/product.ts` 增加 `downloadProductImportTemplate` / `importProducts`
  - [x] 前端：新增 `ProductImportView.vue`（`/products/import`），支持下载模板、上传 Excel、切换更新/跳过、展示结果与失败明细
  - [x] 前端：`ProductListView.vue` 增加「批量导入」按钮跳转导入页
  - [x] 后端测试 153 个全部通过，前端 lint + type-check 通过
  - [x] 更新 `docs/02-architecture/04-API设计.md`
- [x] 产品导入收尾：安全与健壮性加固（2026-07-02 完成）
  - [x] 文件类型校验改用文件魔数：新增 `ExcelFileValidator`，`ProductImportService.validateFile` 不再依赖 `Content-Type`
  - [x] 图片 URL SSRF 防护：新增 `ImageUrlValidator`，默认禁止 local / 内网 / 保留地址，支持 `rsdp.import.allowed-image-hosts` 白名单
  - [x] 图片 URL 不安全时单独记录「不安全的图片 URL」失败明细
  - [x] 修复 Vue Router 中 `/products/import` 与 `/products/:rspuId` 的路由顺序冲突
  - [x] 修复 `processRowInTransaction` 未捕获 `DataIntegrityViolationException` 的问题
  - [x] 修复图片存储失败未记录到导入结果的问题
  - [x] 消除 `normalizeDictCode` 中 `dictName.startsWith` 模糊匹配歧义
  - [x] 补充 `ExcelFileValidatorTest`、`ImageUrlValidatorTest`，扩展 `ProductImportServiceTest`
  - [x] 后端测试 167 个全部通过，前端 lint + type-check 通过
- [x] 搭配方案/报价单支持产品数量选择（2026-07-02 完成）
    - [x] 数据库 `scheme_item` 已存在 `quantity` 字段，补充 `SchemeItemMapper.xml` 批量插入
    - [x] 修复 `SchemeService.generateQuote` 按数量生成报价单；`buildItemResponse` 回填 quantity/subtotal
    - [x] `SchemeService.create/update` 对重复 `rskuId` 聚合数量，与 `QuoteService` 行为一致
    - [x] `AiMatchingService` AI 推荐项默认 quantity=1、subtotal=factoryPrice
    - [x] 更新 `QuoteServiceTest`、`QuoteExportServiceTest`、`SchemeServiceTest` 覆盖数量/小计/总数量
    - [x] 前端 `QuoteBuilderView.vue` 增加数量输入、小计显示、数量参与报价/导出/保存方案
    - [x] 前端 `SchemeDetailView.vue` 展示数量/小计/总数量，导出报价单携带数量
    - [x] 更新 `docs/02-architecture/04-API设计.md`
    - [x] 后端测试通过，前端 lint + type-check 通过
- [x] Code Review 遗留问题集中修复（2026-07-02 完成）
  - [x] `RskuImportService.validateFile` 改用文件魔数校验，复用 `ExcelFileValidator`
  - [x] 合并 JSONB TypeHandler：`JsonbTypeHandler` 与 `PostgreSqlJsonbTypeHandler` 统一为 `com.rsdp.config.typehandler.JsonbTypeHandler`，同时支持 `String` 与 `Object` 字段
  - [x] Redis 接入 Spring Cache：新增 `CacheConfig`，生产环境使用 Redis、开发环境回退 ConcurrentMap；`DictService.listByType` 增加 `@Cacheable("dicts")`
  - [x] 风格数据库表 DDL 同步：`init_db.sql` / `reset_db.sql` 补充 `style_case` / `style_element` / `style_matching_formula` / `product_style_match` / `matching_feedback`
  - [x] 统一 `init_db.sql` 文档引用路径为 `docs/02-architecture/02-数据库设计.md`
  - [x] 后端测试 170 个全部通过，前端 lint + type-check 通过
- [x] Code Review P1 治理项集中修复（2026-07-03 完成）
  - [x] 修复 `LocalStorageService` 目录遍历风险：绝对路径也校验不越界
  - [x] 治理 `FactoryService.listFactories` N+1 能力等级查询：批量查询后组装响应
  - [x] 治理 `SchemeService.getSchemeDetail` N+1 查询：批量查询 RSPU / RSKU / 工厂 / 主图
  - [x] `RskuImportService` 更新报价时同步写入 `price_history`
  - [x] `PriceEncryptionMigration` 改为 LIMIT/OFFSET 分批查询，避免 OOM
  - [x] 修复 `RskuController.updatePrice` 未校验路径 `rspuId`：`RskuService.updateRskuPrice` 增加 `rspuId` 归属校验，移除 Controller 重复查询
  - [x] 修复 `AsyncTaskProcessor` 向量写入失败仍标记任务成功：新增 `partial_success` 状态，区分 AI 识别成功但向量缺失场景；前端同步支持状态展示
  - [x] 修复 `VectorBackfillService` PG 与 ChromaDB 不一致：调整写入顺序为先写 ChromaDB、成功后写 PG
  - [x] 后端测试 182 个全部通过，前端 lint + type-check 通过
- [x] Code Review P1：新品录入与元数据更新增加字典码校验（2026-07-03 完成）
  - [x] `ProductService.createEntry` 注入 `DictService`，校验 `categoryCode` 存在于 `category` 字典
  - [x] `ProductQueryService.updateProduct` 统一校验 `positioningLabel`（style）、`materialTags`（material）、`sceneTags`（scene）、`productLevel`（factory_level）字典码
  - [x] 优化字典校验错误提示：风格/场景/材质/产品等级/品类使用业务语义文案
  - [x] 新增 `ProductServiceTest.createEntry_shouldRejectInvalidCategoryCode`、`ProductQueryServiceTest.updateProduct_shouldRejectInvalidPositioningLabel` / `updateProduct_shouldRejectInvalidSceneTag`
  - [x] 后端测试 185 个全部通过，前端 lint + type-check 通过
- [x] Code Review P1：`selectBatchIds` 空列表保护（2026-07-03 完成）
  - [x] `RskuService.toResponses`：无 RSKU 或无有效 `factory_code` 时跳过批量工厂查询
  - [x] `QuoteService.generateQuote` / `batchRspuMap` / `batchFactoryMap`：空 `rskuId` / `rspuId` / `factoryCode` 列表时返回空 Map 或抛业务异常
  - [x] `RetrievalService.searchSimilar` / `rerank`：空 `imageIds` / `rspuIds` 时跳过批量查询
  - [x] `RspuRelationService.toResponses`：空目标 `rspuId` 列表时跳过批量查询
  - [x] 新增 `RskuServiceTest.listByRspu_shouldReturnEmptyListWhenNoRsku`
  - [x] 后端测试 188 个全部通过
- [x] Code Review P1：`FactoryService.getFactoryCapableLevels` 对缺失工厂不再抛异常（2026-07-03 完成）
  - [x] 工厂不存在、已删除或 `factoryCode` 为空时返回空列表
  - [x] 调用方（`RskuService` / `QuoteService` / `AiMatchingService` / `RskuImportService`）按空列表语义处理
  - [x] 新增 `FactoryServiceTest` 工厂不存在 / 编码为空场景覆盖
  - [x] 后端测试 190 个全部通过
- [x] 前端新品录入页加固（2026-07-03 完成）
  - [x] `ProductEntryView.vue`：`AbortController` 取消未完成的图片上传和任务轮询请求
  - [x] 任务列表持久化到 `localStorage`，页面切换/刷新后可恢复并继续轮询
  - [x] `setInterval` 改为 chained `setTimeout`，避免轮询请求并发
  - [x] `beforeunload` 事件监听，上传/识别未结束时关闭页面弹出确认提示
  - [x] 前端 lint + type-check 通过
- [x] 用户自助扩展 material / scene 字典标签（2026-07-03 完成）
  - [x] 后端新增 `POST /api/v1/dicts`，仅允许扩展 `material` / `scene` 两类标签字典
  - [x] 后端校验 `dictCode`（字母数字，自动大写）、`dictName` 非空；重复时返回「字典项已存在」
  - [x] 创建成功后 `@CacheEvict("dicts")` 清除字典缓存，保证前端选项即时刷新
  - [x] 修复 `CategoryDict` 复合主键插入：将 `@TableId` 设置为 `IdType.INPUT`，避免 MyBatis-Plus 默认雪花算法覆盖 `dict_type`
  - [x] 后端新增 `DictServiceTest`（8 例）与 `DictControllerTest`（4 例）
  - [x] 前端 `api/dict.ts` 新增 `createDict`
  - [x] 前端 `ProductDetailView.vue` 编辑弹窗：材质标签 / 场景标签选择器旁增加「+ 新增」按钮
  - [x] 新增字典项弹窗，创建成功后自动刷新对应字典并选中新标签
  - [x] 更新 `docs/02-architecture/04-API设计.md`
  - [x] 后端测试 202 个全部通过，前端 lint + type-check 通过
- [x] Code Review P1：`AiMatchingService` 候选加载 SQL 化（2026-07-03 完成）
  - [x] `fetchCandidatesByCategory` 改为 SQL 层 `LIMIT 30` 并排除锚点产品，避免按品类全量加载
  - [x] 新增 `RskuSupplyMapper.selectCapableByRspuIds`：在 SQL 层过滤未删除工厂 + 工厂能力等级匹配
  - [x] `batchMinPrices` / `batchCheapestRskus` 直接消费 SQL 过滤后的 capable RSKU，移除内存里的 `filterCapableRskus`
  - [x] 移除 `AiMatchingService` 对 `FactoryService` 的依赖
  - [x] 更新 `AiMatchingServiceTest` 以覆盖新 Mapper 调用路径
  - [x] 后端测试 202 个全部通过
- [x] Code Review P1：`AsyncConfig` 线程池队列满策略优化（2026-07-03 完成）
  - [x] 默认拒绝策略由 `CallerRunsPolicy` 改为 `AbortPolicy`，避免队列满时阻塞 HTTP 线程
  - [x] 新增 `LoggingRejectedExecutionHandler`，任务被拒绝时记录 ERROR 日志与线程池状态
  - [x] 支持通过 `rsdp.async.rejected-policy` 切换策略（abort / discard / discard-oldest / caller-runs）
  - [x] `AdminController` 新增 `GET /api/v1/admin/async/metrics` 暴露线程池实时指标
  - [x] 新增 `AsyncConfigTest` 覆盖默认策略、可配置策略、拒绝行为
  - [x] 后端测试 205 个全部通过
- [x] P0 登录认证体系（2026-07-03 完成）
  - [x] 后端引入 `spring-boot-starter-security` + `jjwt`，实现 JWT 无状态认证
  - [x] 新增 `sys_user` 表与默认管理员初始化（开发/演示账号密码统一为 `rsdp-dev-2026!`，生产环境由 `DefaultAdminInitializer` 生成随机强密码）
  - [x] 新增 `SysUser` / `SysUserMapper` / `SecurityUserDetailsService` / `AuthService` / `AuthController`
  - [x] 新增 `JwtUtil` / `JwtAuthenticationFilter` / `SecurityConfig`，权限规则按角色控制写接口
  - [x] `AuditLogService` 自动从 `SecurityOperatorContext` 获取当前操作人，替代硬编码 `"admin"`
  - [x] 前端 `LoginView.vue` 登录页 + `useUserStore` Pinia 状态管理 + Axios 自动携带 token
  - [x] `App.vue` 显示当前用户/退出登录，登录页隐藏导航栏
  - [x] 新增 `AuthControllerTest` / `AuthServiceTest` / `SecurityUserDetailsServiceTest` / `JwtUtilTest`
  - [x] 更新 `WebMvcTest` 以兼容 Spring Security（`@AutoConfigureMockMvc(addFilters = false)` + mock filter）
  - [x] 更新 `docs/02-architecture/04-API设计.md`、`.env.example`、`docker-compose.yml`
  - [x] 后端测试 215 个全部通过，前端 lint + type-check 通过
- [x] RBAC 权限抽象层 + 最小用户管理后台（第一批基础改造，2026-07-03 完成）
  - [x] 数据库扩展：`sys_role`、`sys_permission`、`sys_role_permission`、`sys_user_role`、`sys_user_factory`
  - [x] `sys_user` 移除 `role` 字段，通过 `sys_user_role` 关联；`seed_data.sql` 初始化角色/权限/默认 admin
  - [x] 新增 `Permission` 常量类与 `PermissionService`（Spring Cache 缓存权限列表）
  - [x] `SecurityConfig` 由 `hasRole` 改为 `hasAuthority`，URL 按权限字符串控制
  - [x] 数据权限框架：`DataScope` / `DataScopeContext` / `DataScopeHelper`，RSKU 查询/更新/导入接入工厂数据权限
  - [x] 用户管理后端接口：`GET/POST/PUT /api/v1/admin/users`、`PUT /api/v1/admin/users/{userId}/reset-password` / `status`
  - [x] 后端 `LoginResponse` 返回 `roles` / `permissions`，`AuthService` 填充完整权限列表
  - [x] 前端 `useUserStore` 升级：`roles` / `permissions`、`hasRole` / `hasAnyRole` / `hasPermission` / `hasAnyPermission`
  - [x] 前端路由守卫支持 `meta.roles` 与 `meta.permissions` 双维度鉴权
  - [x] 新增 `UserManagementView.vue`（`/admin/users`）：用户列表、新增/编辑、禁用/启用、重置密码
  - [x] `App.vue` 仅 ADMIN 可见「用户管理」入口，用户下拉显示多角色
  - [x] 前端 `types/router.d.ts` 扩展 `RouteMeta` 类型
  - [x] 适配 `AuthControllerTest` / `AuthServiceTest`
  - [x] 后端测试 215 个全部通过，前端 lint + type-check 通过
  - [x] 本地启动后端需设置 `RSDP_JWT_SECRET` / `RSDP_ENCRYPTION_KEY`（`scripts/start-local.sh` 已注入 dev 默认值）
- [x] Step 2：设计师/管理员扩展能力建表与基础 CRUD（2026-07-06 完成）
  - [x] 数据库：`designer_profile`、`recommendation_score_config`、`scheme_candidate` 建表；`factory_product_capability`、`product_collection`/`product_collection_item` 已存在同步完善
  - [x] 新增权限常量：`collection:*`、`capability:*`、`designer:profile:*`、`recommendation:score:config:*`、`scheme:candidate:*`
  - [x] `seed_data.sql` 补充新权限与角色权限映射（ADMIN 全部、EDITOR 除推荐配置外、DESIGNER 产品集/能力只读 + 画像 + 候选清单）
  - [x] 后端实体/MyBatis Mapper/Service/Controller：
    - `FactoryProductCapabilityController`：`GET/POST/PUT/DELETE /api/v1/factories/{code}/capabilities`、`POST .../sync`（替换原 `/api/v1/admin/...`）
    - `ProductCollectionController`：`GET/POST/PUT/DELETE /api/v1/collections`
    - `DesignerProfileController`：`GET/PUT/POST /api/v1/designer-profiles/me`、`GET /api/v1/designer-profiles/{userId}`
    - `RecommendationScoreConfigController`：`GET/POST/PUT/DELETE /api/v1/recommendation-score-configs`、`GET /default`
    - `SchemeCandidateController`：`GET/POST/PUT/DELETE /api/v1/scheme-candidates`、`POST /batch`、`GET /mine`
  - [x] `SecurityConfig` 接入新权限规则，并处理 `/batch`、`/sync` 等子路径
  - [x] 前端 `web/src/utils/constants.ts` 同步新增权限常量
  - [x] 新增 `RecommendationScoreConfigServiceTest`、`FactoryCapabilityServiceTest` 共 11 例
  - [x] 后端测试 228 个全部通过，前端 lint + type-check 通过
- [x] RSKU 报价新增 `material_code` 字段（2026-07-06 完成）
  - [x] `rsku_supply` 表已含 `material_code VARCHAR(8)`，实体/DTO/响应/导入模板同步补充
  - [x] `RskuService` / `RskuImportService` 校验长度 ≤16、trim 后持久化
  - [x] `RskuSupplyMapper.xml` 批量插入包含 `material_code`
  - [x] 前端 `types/rsku.ts` 补充 `materialCode`
  - [x] 前端 `ProductDetailView.vue` 报价表格与新增弹窗增加材质编码输入/展示
  - [x] 前端 `RskuDetailView.vue` 详情展示材质编码
  - [x] 后端测试 217 个全部通过，前端 lint + type-check 通过
- [x] Code Review 高风险项修复（2026-07-06 完成）
  - [x] JWT Filter 增加 HttpOnly Cookie 回读，修复 Cookie 认证链路无法使用的问题
  - [x] `SecurityConfig` 调整 `/api/v1/products/import-template` 规则顺序，避免被 `/*` 通配覆盖
  - [x] `AiApiConfig` 支持 `rsdp.chromadb.auth-token`，启用 ChromaDB Token 认证时自动携带 Authorization 头
  - [x] `deploy/docker-compose.yml` Postgres 增加 `127.0.0.1:5433:5432` 映射，本地开发后端可连接
  - [x] `deploy/docker-compose.yml` `server` 端口限制为 `127.0.0.1:8081:8081`
  - [x] `scripts/start-local.sh` 强制 `RSDP_JWT_COOKIE_SECURE=false`；增加 `DASHSCOPE_API_KEY` 占位符校验
  - [x] `scripts/start-local.bat` 改为读取 `deploy/.env`、校验密钥占位符、设置 `RSDP_JWT_COOKIE_SECURE=false`
  - [x] `LoginView.vue` 登录成功后改为调用 `fetchUserInfo()` 拉取用户信息
  - [x] `web/src/api/auth.ts` 移除已弃用的 `token?: string` 字段
  - [x] `deploy/nginx/nginx.conf` `/api` 增加 `proxy_read_timeout 300s` 等长请求超时
  - [x] `Makefile` `test` 目标在检查前先 `pnpm install`
  - [x] 后端测试 217 个全部通过，前端 lint + type-check 通过
- [x] PDF 产品目录批量导入（2026-07-07 完成）
  - [x] 后端 `server/pom.xml` 引入 Apache PDFBox 3.0.2
  - [x] 后端新增 DTO：`DocumentImportResult`、`DocumentProductRegion`、`ProductBoundingBox`、`DocumentImportFailure`
  - [x] 后端新增工具类：`PdfFileValidator`、`PdfRenderer`、`ImageCropper`
  - [x] 后端 `VisionService` 扩展 `detectPageRegions`，支持多页 PDF 图片批量检测产品 bbox
  - [x] 后端 `ProductService` 新增 `createEntryFromStream`，支持从图片流创建单产品录入
  - [x] 后端新增 `PdfImportService`：PDF 校验 → 渲染 → 页面检测 → 裁剪 → 批量创建 RSPU + 异步 AI 识别
  - [x] 后端新增 `DocumentImportController`：`POST /api/v1/products/document-import`
  - [x] 后端 `SecurityConfig` 接入 `product:import` 权限
  - [x] 后端 `application.yml` 新增 `rsdp.document-import.pdf` 配置
  - [x] 修复：AI 多页区域检测返回 JSON 截断问题，批次大小由 20 调整为 5，并增加流式解析容错
  - [x] 后端新增测试：`PdfFileValidatorTest`、`PdfRendererTest`、`ImageCropperTest`、`PdfImportServiceTest`、`DocumentImportControllerTest`、`VisionServiceTest.detectPageRegions_*`
  - [x] 后端全量测试通过
  - [x] 前端新增 `ProductDocumentImportView.vue`（`/products/document-import`）：PDF 上传、品类提示、批次结果、任务轮询
  - [x] 前端 `api/product.ts` 新增 `importProductsFromDocument`，`types/product.ts` 新增 `DocumentImportResult`
  - [x] 前端路由与导航栏新增「PDF 导入」入口
  - [x] 前端 lint + type-check 通过
  - [x] 更新 `docs/02-architecture/04-API设计.md` 与 `docs/05-status/当前进度.md`

- [x] Step 2 Code Review Critical/High 问题修复（2026-07-06 完成）
  - [x] 修复 `created_by` 外键错配：新增 `SecurityUser` 携带 `user_id`；`SecurityOperatorContext` 增加 `currentUserId()`；`ProductCollectionService` / `RecommendationScoreConfigService` 写入 `sys_user.user_id`
  - [x] 修复中文风格名无法解析：`StyleMatchingService` 通过 `DictResolverService` 将中文风格名（如"中古风"）映射为风格编码
  - [x] 修复 `ProductQueryService.listProducts` 非管理员带 `reviewStatus` 时的双重过滤
  - [x] 修复 `SchemeCandidateService.listByRequestId` 未按创建人隔离
  - [x] 修复 `DesignerProfileController` `/me` 写接口权限未显式覆盖：`SecurityConfig` 放行 GET/POST/PUT `/api/v1/designer-profiles/me` 给已认证用户
  - [x] 修复 `VisionService` 字典为空时 prompt 无兜底枚举
  - [x] 修复 `AsyncTaskProcessor` 后端无文件大小校验：增加 `rsdp.ai.max-image-size`（默认 20MB）
  - [x] 补充 `StyleMatchingServiceTest` 中文风格名解析测试
  - [x] 后端测试 234 个全部通过，前端 lint + type-check 通过

- [x] P0 工厂角色与产品库去重（2026-07-07 完成）
  - [x] 角色重命名：`FACTORY_SALES` → `FACTORY_ADMIN`，`DataScopeContext` / 前端常量 / 用户管理页同步更新
  - [x] 新增 `view_full_catalog`：DDL、seed/reset、`SysUser` / DTO / `userStore` 同步透传
  - [x] 产品列表 `viewMode=own|full`：`own` 仅展示本厂 RSKU 关联的 RSPU；`full` 在全平台按 `factory_product_capability` 能力覆盖去重
  - [x] 编辑所有权校验：`ProductQueryService.updateProduct` 对非本厂 RSKU 关联的 RSPU 返回 403
  - [x] 工厂单条录入：`POST /api/v1/products/factory-entry` 原子创建 RSPU + 变体 + 图片 + RSKU
  - [x] 测试：新增 `DataScopeContextTest`，扩展 `ProductQueryServiceTest` / `AuthServiceTest`；后端 243 个测试通过
  - [x] 前端：`ProductListView.vue` 增加 `viewMode` 切换与工厂筛选；`pnpm lint` + `pnpm type-check` 通过

- [x] P0 前端闭环：工厂单条录入页 + 账号设置页（2026-07-07 完成）
  - [x] 后端新增 `PUT /api/v1/auth/me/preferences`，支持当前用户自更新 `view_full_catalog`
  - [x] 新增 `ProductFactoryEntryView.vue`：工厂管理员纯手动录入 RSPU + 变体 + RSKU
  - [x] 新增 `UserSettingsView.vue`：工厂管理员持久化控制「显示全产品库（去重）」开关
  - [x] `App.vue` 导航增加「工厂录入」与「账号设置」入口（仅 `FACTORY_ADMIN`）
  - [x] `ProductListView.vue` 全库开关改为读取/持久化 `userStore.viewFullCatalog`
  - [x] `UserManagementView.vue` 用户表单增加「全库视图」开关
  - [x] 后端测试 243 个全部通过，前端 `pnpm lint` + `pnpm type-check` 通过

- [x] 前端显示与预期使用对齐：P0/P1 体验补齐（2026-07-07 完成）
  - [x] 全库视图只读：`ProductListView.vue` / `ProductDetailView.vue` 在 `viewMode=full` 下隐藏所有写操作按钮并显示提示
  - [x] 工厂单条录入优化：`ProductFactoryEntryView.vue` 增加图片大小/类型/数量校验、尺寸格式校验、提交成功后弹窗选择继续/返回/查看
  - [x] 按钮级权限补全：`QuoteBuilderView.vue` 导出/保存方案按钮、`RoomSchemeView.vue` 生成报价按钮按权限显隐
  - [x] 用户管理增强：`UserManagementView.vue` 增加「全库视图」列表列、操作按钮按权限显隐、新增删除按钮、重置密码改为弹窗密码输入
  - [x] 后端测试 243 个全部通过，前端 `pnpm lint` + `pnpm type-check` 通过

- [x] 工厂能力档案页 + 同组件路由切换刷新修复（2026-07-07 完成）
  - [x] 前端类型：`web/src/types/factory.ts` 新增 `FactoryProductCapability`
  - [x] 前端 API：`web/src/api/factory.ts` 新增 `listFactoryCapabilities`、`syncFactoryCapabilities`
  - [x] 工厂详情页：`FactoryDetailView.vue` 增加「产品能力档案」卡片，按 `capability:read` 展示能力表，按 `capability:create` 显示「重新同步」按钮
  - [x] 方案详情页：`SchemeDetailView.vue` 将 `schemeId` 改为 `computed`，并添加 `onBeforeRouteUpdate` 重新加载
  - [x] 报价单生成器：`QuoteBuilderView.vue` 将 `editSchemeId` 改为 `computed`，同组件切换编辑方案时重置状态并重新加载
  - [x] 修复此前引入的 `FactoryDetailView.vue` / `SchemeDetailView.vue` / `api/factory.ts` 中残留行首 `>` 导致的解析错误
  - [x] 后端测试 243 个全部通过，前端 `pnpm lint` + `pnpm type-check` 通过

- [x] Code Review P0 问题集中修复（2026-07-07 完成）
  - [x] 补齐 DDL：`database/init_db.sql` / `reset_db.sql` 新增 `scheme`、`scheme_item`、`rspu_relation`、`factory_level_capability` 表
  - [x] `rspu_variant` DDL 补充 `product_level` 字段；`image_assets` 启用逻辑删除（新增 `deleted_at` 与 `@TableLogic`）
  - [x] `rsku_supply` 唯一索引改为部分索引 `WHERE deleted_at IS NULL`，避免软删除后无法重建同厂同变体报价
  - [x] `price_history.old_price/new_price` 改为加密 `TEXT`，`PriceHistory` 实体增加 `@TableField(typeHandler = EncryptTypeHandler.class)`
  - [x] `AuditLogService` 写入快照前递归加密 `factoryPrice`/`oldPrice`/`newPrice` 字段，防止审计日志明文泄露价格
  - [x] `JwtAuthenticationFilter` principal 改为 `SecurityUser`，修复 `SecurityOperatorContext.currentUserId()` 取不到用户 ID 的问题
  - [x] `JwtAuthenticationFilter` Cookie 名称改为从 `rsdp.jwt.cookie-name` 读取，避免硬编码与配置不一致
  - [x] `ProductQueryService.applyOwnProductFilter` 改为参数化查询（`selectObjs` + `wrapper.in`），消除 SQL 注入风险
  - [x] `ProductQueryService.listProducts` 材质标签 JSON 改用 `ObjectMapper.writeValueAsString` 生成，避免特殊字符破坏 JSONB 语法
  - [x] `SchemeResponse` / `SchemeService.getSchemeDetail` 返回 `createdBy`，修复前端普通用户无法编辑自己方案的问题
  - [x] 前端 `ProductDetailView.vue` 新增 RSKU 出厂价校验拦截 `null` 值
  - [x] 启用 `idx_rspu_six_dim_gin` 与 `idx_variant_dimensions_gin` GIN 索引
  - [x] 后端测试 243 个全部通过，前端 `pnpm lint` + `pnpm type-check` 通过

- [x] 联调修复：登录后获取用户信息失败 / 产品库为空（2026-07-07 完成）
  - [x] `AuthController.me` 参数改为 `@AuthenticationPrincipal SecurityUser`，修复 principal 变更后 `/auth/me` 报「用户未登录」
  - [x] 补齐缺失 DDL：`rspu_code_counter`、`variant_code_counter` 表
  - [x] `rsku_supply.factory_price` 列类型由 `DECIMAL` 改为 `TEXT`，与 `EncryptTypeHandler` 一致
  - [x] `ProductQueryService.listProducts` 修复：非管理员在 `viewMode=own` 下不过滤复核状态，工厂管理员可见自己录入的待复核产品
  - [x] 通过 `/api/v1/products/factory-entry` 录入 2 条示例产品
  - [x] 验证管理员与工厂管理员产品列表均返回 2 条数据
  - [x] 后端测试 243 个全部通过

- [x] 二次联调修复：产品详情 404 / 新品录入 AI 401（2026-07-07 完成）
  - [x] `ProductQueryService.getProductDetail` 增加 `canViewProduct` 权限校验：工厂管理员可查看自己工厂已报价的待复核产品
  - [x] `ProductQueryService.assertProductOwnership` 的 RSKU 计数增加 `deleted_at IS NULL` 条件
  - [x] `VisionService` 增加 `rsdp.ai.mock.enabled` 开关，开发环境返回固定模拟识别结果，避免无 DashScope Key 导致新品录入失败
  - [x] `application.yml` 增加 Mock 配置（默认关闭），`application-dev.yml` 默认开启
  - [x] 前端 `ProductListView.vue` 无图产品显示「暂无」占位，避免空白单元格
  - [x] 验证工厂管理员可查看待复核产品详情；验证 AI 新品录入异步任务完成（partial_success，向量因无 embedding Key 未生成）
  - [x] 后端测试 243 个全部通过

- [x] 真实 AI 识图全流程验证（2026-07-07 完成）
  - [x] 关闭 `application-dev.yml` 中的 AI Mock（`rsdp.ai.mock.enabled=false`）
  - [x] 重启后端，从系统环境变量读取真实 `DASHSCOPE_API_KEY`（避免被 `deploy/.env` 占位符覆盖）
  - [x] 调用 `/api/v1/products/entry` 上传测试图片，任务创建成功
  - [x] 真实 `qwen3-vl-plus` 识别成功（耗时约 8.1s）
  - [x] `multimodal-embedding-v1` 生成向量成功（耗时 335ms）
  - [x] 向量成功写入 ChromaDB，异步任务状态 `done`
  - [x] 产品详情可正常查询，`styleVector` 已持久化

- [x] 工厂资料模块 P1：工厂表信息补充 + 前端详情查看（2026-07-08 完成）
  - [x] 数据库：`factory_master` 扩展 20+ 字段（规模/设备/原料/品控/物流/验厂/图片）
  - [x] 数据库：新增 `equipment_type` / `logistics_method` / `packaging_type` / `wood_type` 字典
  - [x] 后端：`FactoryMaster` / `FactoryCreateRequest` / `FactoryResponse` / 新增 `FactoryUpdateRequest` 扩展字段
  - [x] 后端：`FactoryService.updateFactory` + `FactoryController.PUT /api/v1/factories/{code}`
  - [x] 后端：字段校验（面积/人数/产能/年份非负合理）+ 审计日志
  - [x] 后端测试：扩展 `FactoryServiceTest`，全量 270 个测试通过
  - [x] 前端：`FactoryDetailView.vue` 多信息卡片布局 + 编辑弹窗（按 `factory:update` 权限）
  - [x] 前端：`FactoryListView.vue` 增加「地区」列与名称/地区搜索
  - [x] 前端：`types/factory.ts` / `api/factory.ts` 扩展 `FactoryUpdateRequest`
  - [x] 前端 `pnpm lint` + `pnpm type-check` 通过
  - [x] 更新 `docs/02-architecture/02-数据库设计.md` / `04-API设计.md`

- [x] 第 2 批 数据一致性与事务边界治理（2026-07-08 完成）
  - [x] 改造 `RskuImportService.importRskus`
    - [x] 移除入口方法 `@Transactional`，解析/校验/预加载无事务执行
    - [x] 新增 `insertSingleRsku` / `updateSingleRsku` 私有方法，每行独立事务提交/回滚
    - [x] `preloadCapableLevels` 改为调用 `FactoryService.batchListCapableLevels` 批量查询，消除 N+1
    - [x] 单行 `DataIntegrityViolationException` 捕获并记录失败明细，不阻断其他行
  - [x] 修复 `ProductService.createEntry` 事务回滚孤儿文件问题
    - [x] 新增 `registerStorageRollbackCleanup`，文件存储成功后注册事务同步器
    - [x] 事务回滚时自动调用 `storageService.delete` 清理已上传文件
    - [x] `createEntry` / `createEntryFromStream` / `createFactoryEntry` 均接入清理逻辑
  - [x] 测试补齐
    - [x] `RskuImportServiceTest` 已覆盖批量查询、行级事务、唯一冲突容错
    - [x] `ProductServiceTest` 新增回滚触发文件删除 / 正常提交不删除 4 个用例
  - [x] 后端测试 276 个全部通过，前端 lint + type-check 通过
  - [x] 更新 `docs/05-status/待办事项.md`

- [x] 治理 `UserService.listUsers` N+1 查询（2026-07-08 完成）
  - [x] `UserService.listUsers` 改为按当前页 `user_id` 集合批量查询 `sys_user_role` / `sys_role` / `sys_user_factory`
  - [x] 新增 `batchGetRoleMap` / `batchGetFactoryCodeMap` 私有批量组装方法
  - [x] `toResponse` 复用新的 `toListResponse`，减少重复代码
  - [x] 新增 `UserServiceTest` 覆盖多用户角色/工厂映射、无角色/无工厂场景
  - [x] 后端测试 272 个全部通过，前端 `pnpm lint` + `pnpm type-check` 通过

- [x] 第 1 批 N+1 与性能治理（2026-07-08 完成）
  - [x] 在 `docs/05-status/待办事项.md` 中标记 `UserService.listUsers` N+1 已完成
  - [x] 治理 `RskuService.toResponses` 能力等级 N+1：改用 `FactoryService.batchListCapableLevels` 批量查询，保留老数据主等级 fallback
  - [x] 治理 `DataScopeContext` 重复查库：引入请求级 ThreadLocal 缓存，在 `JwtAuthenticationFilter` 的 `finally` 中清理
  - [x] 扩展 `RskuServiceTest` / `DataScopeContextTest` 覆盖批量查询与缓存场景
  - [x] 后端测试 276 个全部通过，前端 `pnpm lint` + `pnpm type-check` 通过

- [x] Excel 导入品类中文名归一化（方案三：分层解析 + 用户确认 + 别名自学习，2026-07-22 完成）
  - [x] 新增 `dict_alias` 数据层：`entity/DictAlias`、`mapper/DictAliasMapper`、`service/DictAliasService`（resolveAlias / resolveAliases 批量 / saveAlias 幂等 + 并发唯一约束兜底）
  - [x] V16 同步进 `V1__init_db.sql` 与 `reset_db.sql`（建表 + 索引 + 序列对齐 + DROP）
  - [x] `ExcelAiImportService.normalizeCategoryCode` 分层解析：用户确认映射 > 字典码 > 字典中文名（DictResolverService）> 别名库；未命中保留原值由 validateRow 报错（行为兼容）
  - [x] 预览接口返回 `categoryMappings`（rawValue/suggestedCode/source=dict|alias|ai|none），未解析词批量一次 AI 归一，失败降级不阻断预览
  - [x] 确认请求新增 `categoryMapping`；导入完成后经 `saveAlias` 写回别名库自学习
  - [x] 新增 `DictAliasServiceTest`（7 个）+ `ExcelAiImportServiceTest` 品类用例（6 个），定向测试 44 个全部通过

## 进行中

- [ ] 无


## 阻塞项

- 暂无

## 下一步

1. 多模态录入扩展：PPT 产品目录导入（复用 PDF 流程，POI 渲染幻灯片为图片）
2. Code Review P1 治理项按优先级逐步推进
3. 风格数据库 Skill：表结构、导入接口、产品匹配与推荐集成
   - 优先补充书房、卧室、餐厅等非客厅空间的风格案例与搭配公式
4. 工厂后台工作台 UI 细化（可选）
5. 工厂资料模块 P2（交期规则 + 面料材质组 + 验厂表单，P1 完成后再启动）
6. 其他功能优先级需结合业务方向再确定
