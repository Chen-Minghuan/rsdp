## 已完成

- [x] 项目骨架搭建
  - [x] `AGENTS.md` 项目级指令
  - [x] `.ai-rules` AI 协作补充规则
  - [x] `README.md` 项目入口
  - [x] `Makefile` 统一命令入口
  - [x] `.gitignore` Git 忽略规则
- [x] 文档整理
  - [x] 按 `docs/01-*` ~ `07-*` 编号结构组织文档
  - [x] `docs/02-architecture/` 架构、数据库、API、实现说明文档
  - [x] `docs/03-guides/` 本地开发、编码规范、测试指南
  - [x] `docs/04-decisions/` 关键决策记录
  - [x] `docs/05-status/` 当前进度、待办事项
  - [x] `docs/06-reference/` 双层编码体系参考资料
  - [x] `docs/07-issues/` 问题排障日志
- [x] 数据库脚本
  - [x] `database/init_db.sql` PostgreSQL 建表 DDL
  - [x] `database/seed_data.sql` PostgreSQL 字典种子数据
  - [x] `database/reset_db.sql` 数据库重置脚本
- [x] 后端骨架
  - [x] SpringBoot `pom.xml`（Java 21 + Spring Boot 3.4 + MyBatis-Plus + PostgreSQL）
  - [x] 启动类 `RsdpApplication`
  - [x] 配置文件 `application.yml` / `application-dev.yml` / `application-prod.yml`
  - [x] `config/` 配置类（RestClient、WebMvc、MyBatis-Plus 分页等）
  - [x] `common/` 统一响应 `Result<T>`（含错误码）、`PageResult<T>`
  - [x] `exception/` 全局异常处理 `GlobalExceptionHandler`
  - [x] `entity/` / `mapper/`：RspuMaster、ImageAssets、AiRecognition、AsyncTask
  - [x] `controller/ImageController`：按 imageId 返回图片文件流
  - [x] `controller/ProductController`：新品录入、列表、详情、复核
  - [x] `controller/TaskController`：任务状态轮询
  - [x] `service/ImageService`：加载本地图片资源
  - [x] `service/ProductService`：保存图片、写 RSPU 草稿、创建异步任务
  - [x] `service/ProductQueryService`：产品列表、详情、复核
  - [x] `service/AsyncTaskProcessor`：后台 AI 识别处理
  - [x] `service/TaskService`：任务状态查询
  - [x] `service/VisionService`：调用 DashScope `qwen3-vl-plus`
  - [x] `util/ImageUploadValidator`：图片上传校验
  - [x] `dto/request/ProductListRequest`、`ProductReviewRequest`
  - [x] `dto/response/ProductSummaryResponse`、`ProductDetailResponse`
  - [x] `Dockerfile`
- [x] 测试补充
  - [x] `ProductServiceTest`（Mockito）
  - [x] `ProductQueryServiceTest`（Mockito）
  - [x] `ImageServiceTest`（Mockito）
  - [x] `VisionServiceTest`（WireMock）
  - [x] `ImageUploadValidatorTest`
- [x] 前端骨架
  - [x] `package.json` / `vite.config.ts` / `tsconfig.json`
  - [x] `main.ts` / `App.vue`
  - [x] `router/index.ts`：首页 `/`、录入页 `/entry`、产品库 `/products`、详情 `/products/:rspuId`
  - [x] `views/HomeView.vue`、`views/ProductEntryView.vue`、`views/ProductListView.vue`、`views/ProductDetailView.vue`
  - [x] `api/client.ts`：Axios 实例与统一错误处理
  - [x] `api/product.ts`、`api/task.ts`：API 封装
  - [x] `types/task.ts`：任务相关类型
  - [x] `types/product.ts`：产品相关类型
  - [x] 图片上传并调用 `POST /api/v1/products/entry`
  - [x] 前端任务状态轮询展示
  - [x] 产品列表分页查询与详情展示
  - [x] 产品库/详情页图片通过 `/api/v1/images/{imageId}` 正常显示
  - [x] 人工复核确认/存疑操作
  - [x] 顶部全局导航栏
  - [x] `Dockerfile`
- [x] 存储抽象层
  - [x] `StorageService` 接口：`store` / `get` / `exists` / `delete`
  - [x] `LocalStorageService`：本地磁盘实现，兼容旧版绝对路径存储记录
  - [x] `MinioStorageService` + `MinioConfig`：MinIO 实现（`storage.type=minio` 激活）
  - [x] `StorageProperties`：统一 `storage.type` / `storage.local-path` / `storage.minio.*` 配置
  - [x] `ProductService` / `ImageService` / `AsyncTaskProcessor` / `VisionService` 接入 `StorageService`
  - [x] `LocalStorageServiceTest` 单元测试
  - [x] 配置更新：`application.yml` / `application-dev.yml` / `application-prod.yml`
- [x] 审计日志自动记录
  - [x] `AuditLog` 实体 + `AuditLogMapper` + `AuditLogService`
  - [x] `PostgreSqlJsonbTypeHandler`：支持 Java 对象写入 PostgreSQL jsonb
  - [x] `MybatisTypeHandlerConfig`：让 MyBatis-Plus JacksonTypeHandler 使用 Spring 的 ObjectMapper
  - [x] 接入 `ProductService.createEntry`（rspu_master / image_assets CREATE）
  - [x] 接入 `ProductQueryService.reviewProduct`（rspu_master REVIEW）
  - [x] 接入 `FactoryService.createFactory`（factory_master CREATE）
  - [x] 接入 `RskuService.createRsku`（rsku_supply CREATE）
  - [x] 接入 `AsyncTaskProcessor` AI 识别后的 RSPU 字段更新（rspu_master UPDATE）
  - [x] `AuditLogServiceTest` 单元测试
- [x] B 模块：工厂管理 + RSKU 报价录入
  - [x] 后端 `FactoryMaster` / `RskuSupply` 实体与 Mapper
  - [x] 后端 `FactoryService` / `FactoryController`（工厂增删改查）
  - [x] 后端 `RskuService` / `RskuController`（按 RSPU 报价列表、新增报价）
  - [x] 后端 `FactoryServiceTest`、`RskuServiceTest` 单元测试
  - [x] 前端 `FactoryListView.vue`（新增工厂 + 列表）
  - [x] 前端 `types/factory.ts`、`types/rsku.ts`
  - [x] 前端 `api/factory.ts`、`api/rsku.ts`
  - [x] 前端 `ProductDetailView.vue` 增加 RSKU 报价列表与新增报价弹窗
  - [x] 路由与导航：新增 `/factories` 路由和顶部"工厂管理"入口
- [x] 部署配置
  - [x] `deploy/docker-compose.yml`
  - [x] `deploy/nginx/nginx.conf`
  - [x] `deploy/.env.example`
- [x] 运维脚本
  - [x] `scripts/setup.sh`
  - [x] `scripts/test.sh`
  - [x] `scripts/backup_db.sh`
  - [x] `ops/anchor_encode.sh`
- [x] 「风格数据库 Skill」方案与导入数据格式文档
  - [x] `docs/04-decisions/005-风格数据库Skill方案与可行性分析.md`
  - [x] `docs/03-guides/04-风格数据库导入数据格式.md`
  - [x] 数据库设计文档已补充风格库表结构
  - [x] API 设计文档已补充风格库接口
- [x] 环境升级（2026-06-25）
  - [x] JDK 从 17 升级到 21 LTS（`D:\develop\Java\jdk-21`）
  - [x] `pom.xml` `<java.version>` 改为 21
  - [x] Docker PostgreSQL 端口因 Windows Docker Desktop 5432 冲突，改为映射 5433→5432
  - [x] `application-dev.yml` JDBC URL 端口同步改为 5433
  - [x] 本地启动后端需显式指定 `--spring.profiles.active=dev` 或 `-Dspring-boot.run.profiles=dev`
  - [x] 创建远程分支 `hha-create-a-branch` 并推送
  - [x] 前端启动验证通过（Vite 6.4.3，http://localhost:5173）
  - [x] 后端启动验证通过（Java 21.0.11，Tomcat :8081）
  - [x] 6 条产品录入测试通过（DashScope `qwen3-vl-plus` AI 识别正常）
- [x] 前端 ESLint 配置（2026-06-26）
  - [x] 新增 `web/eslint.config.js`：Vue 3 + TypeScript 5 + 浏览器全局变量
  - [x] 安装依赖：`eslint` `prettier` `@eslint/js` `typescript-eslint` `eslint-plugin-vue` `globals`
  - [x] 替换 `package.json` 的 lint no-op：`eslint src --ext .ts,.vue`
  - [x] `pnpm lint` 0 errors、`pnpm type-check` 通过
  - [x] 关闭强格式类规则（max-attributes-per-line、singleline-content-newline、attributes-order）
  - [x] 将 `types/task.ts` 中 `Record<string, any>` 改为 `Record<string, unknown>`
- [x] 产品录入支持多图/非主图场景（2026-06-26）
  - [x] 后端 `ProductController.entry` 改为接收 `List<MultipartFile> images`
  - [x] 后端 `ProductService.createEntry`：一个 RSPU + 多张图片，第一张为主图并触发 AI 识别
  - [x] 非主图 `image_type=detail`、`is_primary=false`，仅存档不参与 AI 识别
  - [x] 返回 `imageIds` 列表，所有图片记录写入审计日志
  - [x] 更新 `ProductServiceTest`：单图、多图、空列表测试
  - [x] 前端 `api/product.ts` 新增 `uploadProductImages(files)`
  - [x] 前端 `types/task.ts` 增加 `imageIds`
  - [x] 前端 `ProductEntryView.vue`：一次上传多图为一个产品，任务卡片展示多图缩略图
- [x] 工厂详情页 / RSKU 详情页（2026-06-26）
  - [x] 后端 `RskuService.getRsku(rspuId, rskuId)` + `GET /api/v1/products/{rspuId}/rsku/{rskuId}`
  - [x] 后端 `RskuService.listByFactory(factoryCode)` + `GET /api/v1/factories/{factoryCode}/rsku`
  - [x] 更新 `RskuServiceTest`
  - [x] 前端新增 `FactoryDetailView.vue`（`/factories/:factoryCode`）
  - [x] 前端新增 `RskuDetailView.vue`（`/products/:rspuId/rsku/:rskuId`）
  - [x] 前端 `FactoryListView.vue` 表格行可点击进入详情
  - [x] 前端 `ProductDetailView.vue` RSKU 表格行可点击进入详情
  - [x] 更新路由
- [x] 字典基础设施 + 报价单 + AI 搭配方案（2026-06-26）
  - [x] 后端 `CategoryDict` 实体 + Mapper + `DictService` + `GET /api/v1/dicts/{dictType}`
  - [x] `database/seed_data.sql` 新增 `room_type`（客厅/卧室/餐厅/书房/办公室）和 `quote_confidence`（高/中/低）
  - [x] 后端 `QuoteService` + `QuoteController` + `POST /api/v1/quotes/generate`
  - [x] 后端 `QuoteServiceTest`
  - [x] 后端扩展 `VisionService.chatText` 支持纯文本对话
  - [x] 后端 `AiMatchingService` + `MatchingController` + `POST /api/v1/matching/room-scheme`
  - [x] 后端 `AiMatchingServiceTest`（WireMock 模拟 AI 返回）
  - [x] 后端 `POST /api/v1/matching/recommend` 预留接口
  - [x] 前端 `dict.ts` / `types/dict.ts`，字典下拉从接口动态获取
  - [x] 前端 `ProductListView.vue` 改为从字典获取复核状态，增加产品多选 +「生成报价单」
  - [x] 前端 `ProductDetailView.vue` 报价置信度改为从字典获取
  - [x] 前端新增 `QuoteBuilderView.vue`（`/quotes/build`）
  - [x] 前端新增 `RoomSchemeView.vue`（`/matching/room-scheme`）
  - [x] 前端新增 `AnchorMatchingView.vue`（`/matching/anchor`）骨架
  - [x] 前端 `App.vue` 新增「报价单生成器」「AI 搭配方案」导航入口
  - [x] 更新路由
  - [x] 后端测试 42 个全部通过，前端 lint + type-check 通过
  - [x] 手动验证：字典接口、报价单生成、AI 空间搭配方案接口均正常
- [x] 业务规则完善（2026-06-26）
  - [x] 阶段 1：字典表补齐（category 增加 DT/CB/BD，新增 product_status）+ 启用 `rspu_variant` 变体体系
  - [x] 阶段 2：新品录入支持品类选择 + AI 识别后写入 `rspu_style` / `rspu_scene` 关联表
  - [x] 阶段 3：RSKU 必须关联变体 + 变体合法性校验 + `price_history` 价格历史
  - [x] 阶段 4：产品列表按风格码/场景码（`rspu_style` / `rspu_scene`）精确查询
  - [x] 阶段 5：`material_tags` 存字典码 + 材质标签 JSONB 精确查询
  - [x] 阶段 6：工厂等级变更不改工厂代码（`PUT /factories/{code}/level`）
  - [x] 阶段 7：RSPU 业务编码服务 `RspuCodeService` + `rspu_code_counter` 表（可选能力，未默认启用）
  - [x] 前端：变体管理、价格历史、工厂等级变更、风格/场景/材质筛选
  - [x] 更新 `API设计.md` 与 `seed_data.sql`
  - [x] 后端测试 47 个全部通过，前端 lint + type-check 通过
  - [x] Code Review 修复（2026-06-26）
    - [x] 修复 `RspuVariantService` 变体 ID 并发竞态：新增 `variant_code_counter` 表 + `VariantCodeMapper` 原子递增
    - [x] 修复多写服务缺少 `@Transactional`：`RskuService`、`FactoryService`、`ProductQueryService`
    - [x] 配置 `@Async` 有界线程池：`AsyncConfig` + `taskExecutor` + `application.yml` 参数
    - [x] 修复 RSKU 重复报价检查竞态：移除先查后插，捕获 `DataIntegrityViolationException` 转换为业务异常
    - [x] 统一 `scene_tags` 也存储字典码（与 `material_tags` 一致）
    - [x] 修复 `DictResolverService.resolveCodesByNames` 同时匹配 `dictName` 与 `dictNameEn`
    - [x] 修复复核备注 `reviewComment` 未持久化：`rspu_master.review_comment` 字段 + 实体 + 接口写入
    - [x] 增加工厂等级字典校验：`FactoryService` 创建/更新时检查 `factory_level` 字典
    - [x] 增加变体字典码校验：`RspuVariantService` 创建时校验 size/color/material 字典码
    - [x] 启用 `material_tags` JSONB GIN 索引
    - [x] 更新 `FactoryServiceTest`、`RspuVariantServiceTest`、`RskuServiceTest`
    - [x] 后端测试 57 个全部通过，前端 lint + type-check 通过
- [x] 搭配方案管理完善（2026-06-29）
  - [x] 修复 `SchemeItem` 自增主键溢出：`@TableId(type = IdType.AUTO)`
  - [x] 修复请求/响应 UTF-8 编码：`application.yml` 强制编码 + 前端 `Content-Type` 带 charset
  - [x] 编辑方案：`PUT /api/v1/schemes/{schemeId}` + `QuoteBuilderView` 编辑模式
  - [x] RSKU 失效防御：`QuoteService.generateQuote` 批量校验失效 RSKU
  - [x] 方案快照与价格变动提示：`QuoteResponse.priceChanges` 对比保存时价格与当前价
  - [x] 同一 RSPU 多 RSKU 提示：`QuoteBuilderView` / `SchemeDetailView` 去重警告
  - [x] 方案名称长度与唯一性校验：`@Size(max=128)` + 同创建人下 active 方案同名校验
  - [x] 方案项批量插入与数量上限：`SchemeItemMapper.insertBatch` + `@Size(max=50)`
  - [x] 后端测试 71 个全部通过，前端 lint + type-check 通过
- [x] 工厂兼做等级能力（2026-06-29）
  - [x] 新增 `factory_level_capability` 表：`id` + `factory_code` + `level_code` + `is_primary`
  - [x] 新增 `FactoryLevelCapability` 实体与 `FactoryLevelCapabilityMapper`
  - [x] `FactoryCreateRequest` / `FactoryResponse` 增加 `capableLevels`
  - [x] 新增 `PUT /api/v1/factories/{code}/capable-levels` 更新兼做等级
  - [x] `FactoryService` 创建/更新时同步维护能力等级表，主等级自动标记 `is_primary`
  - [x] 前端 `FactoryListView.vue` 增加兼做等级多选
  - [x] 前端 `FactoryDetailView.vue` 展示主等级/兼做等级，支持编辑兼做等级
  - [x] 更新 `FactoryServiceTest` 覆盖能力等级场景
  - [x] 后端测试 71 个全部通过，前端 lint + type-check 通过
- [x] 原厂搭配关系（2026-06-29）
  - [x] 新增 `rspu_relation` 表：表达 RSPU → RSPU 的 official / ai_verified / exclude 关系
  - [x] 新增 `RspuRelation` 实体、`RspuRelationMapper`、`RspuRelationService`、`RspuRelationController`
  - [x] 新增 `GET/POST/PUT/DELETE /api/v1/products/{rspuId}/relations` 接口
  - [x] `ProductDetailResponse` 增加 `officialMatches` / `matchedBy`
  - [x] `ProductDetailView.vue` 展示「官方搭配」与「适配来源」，支持添加/删除关系
  - [x] 前端新增 `types/relation.ts`、`api/relation.ts`
  - [x] 新增 `RspuRelationServiceTest`、`ProductQueryServiceTest` 覆盖关系查询与详情组装
  - [x] 后端测试 93 个全部通过，前端 lint + type-check 通过
  - [x] 更新 `docs/02-architecture/04-API设计.md`
- [x] 报价单 Excel 导出（2026-06-30 完成）
  - [x] 后端 `server/pom.xml` 引入 EasyExcel 3.3.4
  - [x] 后端新增 `QuoteExportService`，生成含「报价明细」和「汇总」两个 sheet 的 `.xlsx`
  - [x] 后端 `QuoteController` 增加 `POST /api/v1/quotes/export` 文件下载接口
  - [x] 前端 `api/quote.ts` 新增 `exportQuote`，通过 Blob 触发浏览器下载
  - [x] 前端 `QuoteBuilderView.vue` / `SchemeDetailView.vue` 增加「导出 Excel」按钮
  - [x] 后端测试 `QuoteExportServiceTest` 覆盖 Excel 字节输出与空列表异常
  - [x] 更新 `docs/02-architecture/04-API设计.md`
- [x] RSPU / RSKU 软删除（2026-06-30 完成）
  - [x] 后端 `ProductQueryService.deleteProduct` + `DELETE /api/v1/products/{rspuId}`
  - [x] 后端 `RskuService.deleteRsku` + 新增 `SkuController`：`DELETE /api/v1/sku/{rskuId}`
  - [x] 软删除操作写入审计日志（`logDelete`）
  - [x] 后端测试 `ProductQueryServiceTest` / `RskuServiceTest` 覆盖删除成功、重复删除、不存在场景
  - [x] 前端 `api/product.ts` 新增 `deleteProduct`，`api/rsku.ts` 新增 `deleteRsku`
  - [x] 前端 `ProductListView.vue` / `ProductDetailView.vue` 增加 RSPU 删除入口（二次确认）
  - [x] 前端 `ProductDetailView.vue` / `RskuDetailView.vue` 增加 RSKU 删除入口（二次确认）
  - [x] 更新 `docs/02-architecture/04-API设计.md`
- [x] 以图搜图 / 以文搜图（2026-06-30 完成）
  - [x] 后端 `EmbeddingService`：接入 DashScope `multimodal-embedding-v1` 生成图片/文本 embedding
  - [x] 后端 `ChromaDbClient` + `ChromaDbProperties`：封装 ChromaDB REST API（集合、upsert、query、delete），已升级为 v2 API
  - [x] 后端 `AiApiConfig`：新增 `embeddingRestClient` / `chromaRestClient`
  - [x] 后端 `AsyncTaskProcessor`：新品录入 AI 识别成功后生成 embedding，写入 `rspu_master.style_vector` 与 ChromaDB
  - [x] 后端 `RetrievalService` + `RetrievalController`：`POST /api/v1/retrieval/similar` 三层检索（向量召回 → 元数据过滤 → 规则重排）
  - [x] 后端 `VectorBackfillService` + `AdminController`：`POST /api/v1/admin/vectors/backfill` 存量图片向量回填
  - [x] 后端 `ProductQueryService.deleteProduct`：软删除时同步删除 ChromaDB 向量
  - [x] 后端测试 `AsyncTaskProcessorTest`、`RetrievalServiceTest`、`RetrievalControllerTest` 覆盖核心流程
  - [x] 前端 `api/retrieval.ts`、`types/retrieval.ts`
  - [x] 前端新增 `VisualSearchView.vue`（`/visual-search`）：支持图片上传/文本输入、类别/风格过滤、结果展示
  - [x] 前端 `App.vue` 顶部导航新增「以图搜图」入口
  - [x] 更新 `docs/02-architecture/04-API设计.md` 与 `docs/05-status/当前进度.md`
- [x] P0 修复与边界加固（2026-07-01 完成）
  - [x] 修复 `database/reset_db.sql` 与 `database/init_db.sql` 结构不一致
  - [x] 拆分 `AsyncTaskProcessor` 长事务：新增 `AiRecognitionPersistenceService`
  - [x] `ProductQueryService.deleteProduct` 改为发布 `RspuDeletedEvent` 异步清理 ChromaDB 向量
  - [x] 修复 `FactoryMaster` JSONB 字段映射并扩展工厂 API 透传 `certification` / `engineeringCases`
  - [x] 修复 `ProductDetailView.vue` 同组件路由复用不刷新
  - [x] 限制 `QuoteBuilderView.vue` URL 产品 ID 数量（去重后截断至 50）
  - [x] 敏感价格字段加密：`RskuSupply.factoryPrice` / `SchemeItem.factoryPrice` 使用 AES-256-GCM + TypeHandler 自动加解密
  - [x] 后端测试 113 个全部通过，前端 lint + type-check 通过
- [x] 以图搜图性能优化（2026-07-01 完成）
  - [x] 消除 `RetrievalService` N+1 查询：图片 URL 与 RSPU 元数据均改为批量查询
  - [x] 更新 `RetrievalServiceTest` 以覆盖批量查询路径
  - [x] 后端测试 121 个全部通过，前端 lint + type-check 通过
- [x] Controller 层补齐参数校验（2026-07-01 完成）
  - [x] 所有 Controller 类加 `@Validated`
  - [x] 所有 `@PathVariable String` 参数加 `@NotBlank`
  - [x] `ProductController.list` 加 `@Valid`，`ProductListRequest.size` 限制 1~100
  - [x] `AdminController.backfillVectors` 的 `batchSize` 限制 1~1000
  - [x] `RetrievalController.searchSimilar` 加 `@Valid`，`SimilarProductRequest.topK` 限制 1~100
  - [x] `ProductController.entry` 批量图片上传加空文件 / 类型 / 大小校验（单图上限 10MB）
  - [x] `GlobalExceptionHandler` 增加 `ConstraintViolationException` 统一处理
  - [x] 后端测试 121 个全部通过，前端 lint + type-check 通过
- [x] Service 层外部调用增强健壮性（2026-07-01 完成）
  - [x] 新增 `ExternalServiceException` 与全局异常处理
  - [x] `AiApiConfig` 为 AI / Embedding / ChromaDB 三个 RestClient 配置连接/读取超时与 IO 重试拦截器
  - [x] `application.yml` 增加 `timeout-seconds`、`max-retries`、`embedding-timeout-seconds`、`embedding-max-retries`、`chromadb.timeout-seconds`、`chromadb.max-retries`
  - [x] `ChromaDbClient` 增强集合查询/创建/upsert/query/delete 的判空与异常包装
  - [x] `EmbeddingService` 增强图片流判空、API 响应判空，使用 `ExternalServiceException`
  - [x] `VisionService` 增强图片流判空、AI 响应逐层判空，使用 `ExternalServiceException`
  - [x] 后端测试 121 个全部通过，前端 lint + type-check 通过
- [x] 治理列表页 N+1 查询（2026-07-01 完成）
  - [x] `ProductQueryService.listProducts` 风格/场景筛选改用 `EXISTS` 子查询
  - [x] `ProductQueryService.toSummary` 批量查询主图
  - [x] `RspuRelationService.toResponse` 批量查 RSPU / 图片 / 最低价
  - [x] `RskuService.toResponse` 批量查工厂
  - [x] `QuoteService.generateQuote` 批量查 RSKU / RSPU / 工厂 / 图片
  - [x] `AiMatchingService` 批量查 candidate 最低价 / cheapest RSKU / 主图 / 工厂
  - [x] 同步更新 `ProductQueryServiceTest`、`RspuRelationServiceTest`、`RskuServiceTest`、`QuoteServiceTest`、`AiMatchingServiceTest`
  - [x] 后端测试 121 个全部通过，前端 lint + type-check 通过
- [x] 数据库与 MyBatis-Plus 一致性加固（2026-07-01 完成）
  - [x] `RspuStyle` / `RspuScene` / `CategoryDict` 复合主键字段标注 `@TableId` / `@TableField`，补充复合主键说明
  - [x] `RspuMaster` / `RskuSupply` / `RspuRelation` / `RspuVariant` / `FactoryMaster` / `Scheme` 启用 `@TableLogic` 逻辑删除
  - [x] 移除 Service 层手动的 `isNull("deleted_at")` 条件，交由 MyBatis-Plus 自动过滤
  - [x] `scheme_item.factory_code` 增加外键约束
  - [x] `rsku_supply` 唯一索引拆分为部分唯一索引（variant_id IS NULL / IS NOT NULL），并排除已软删除记录
  - [x] `factory_level_capability` 增加“唯一主评级”部分唯一索引
  - [x] `SchemeItemMapper.insertBatch` 增加空集合保护 `insertBatchSafe`
  - [x] `scheme_item.scheme_item_id` DDL 改为 `BIGSERIAL`
  - [x] 同步更新 `SchemeServiceTest`
  - [x] 后端测试 121 个全部通过，前端 lint + type-check 通过
- [x] 业务规则边界校验（2026-07-01 完成）
  - [x] `RspuCodeService` 注入 `DictService` 校验 category/style/size 字典存在性，seq 超过 999 抛业务异常
  - [x] `RspuVariantService` 确认 `variantCodeMapper.allocateSequence` 原子分配，插入时捕获 `DataIntegrityViolationException` 兜底
  - [x] `SchemeService.create/update` 校验 `items` 非空、方案名称非空
  - [x] `FactoryService.extendCapability` 显式标注 `@Transactional(propagation = Propagation.REQUIRED)` 并补充事务语义说明
  - [x] 同步更新 `RspuCodeServiceTest`
  - [x] 后端测试 125 个全部通过，前端 lint + type-check 通过
- [x] 前端边界加固（2026-07-01 完成）
  - [x] `api/client.ts` 统一判断 `ApiResult.code`（非 200 抛 `ApiError`）
  - [x] 上传接口使用独立 `uploadClient`（120s 超时）
  - [x] `ProductEntryView.vue` 校验文件数量、单文件 ≤10MB、真实图片 MIME 类型
  - [x] `VisualSearchView.vue` 组件卸载时清理 ObjectURL
  - [x] `QuoteBuilderView.vue` 价格改用「分」累加避免浮点精度，价格为 0 显示 `¥0.00`
  - [x] `QuoteBuilderView.vue` `MAX_ITEMS=50` 在 `loadData` 中真正截断
  - [x] 产品/工厂/方案/RSKU 详情页校验路由参数非空
  - [x] `FactoryListView.vue` / `SchemeListView.vue` 增加前端本地分页
  - [x] 后端测试 125 个全部通过，前端 lint + type-check 通过
- [x] 历史价格数据加密迁移（2026-07-01 完成）
  - [x] 新增 `MigrationProperties` 配置类，开关 `rsdp.migration.encrypt-prices.enabled` 默认关闭
  - [x] 新增 `PriceEncryptionMigration` CommandLineRunner，迁移 `rsku_supply` 与 `scheme_item` 的明文 `factory_price`
  - [x] 幂等执行：已加密记录自动跳过，明文记录加密更新，非法值记录失败日志
  - [x] `application.yml` / `application-dev.yml` 增加迁移配置示例
  - [x] `PriceEncryptionMigrationTest` 覆盖关闭、加密、跳过、非法值、scheme_item 场景
  - [x] 后端测试 129 个全部通过
- [x] 工厂能力等级在报价/推荐中落地（2026-07-01 完成）
  - [x] `QuoteService.generateQuote` 增加能力等级校验：产品等级不在工厂 `capableLevels` 中的 RSKU 批量拒绝
  - [x] `AiMatchingService` 的 `batchMinPrices` / `batchCheapestRskus` 按能力等级过滤，推荐结果只选具备能力的工厂
  - [x] `RskuResponse` 增加 `factoryCapableLevels`，`RskuService` 批量填充
  - [x] 前端 `QuoteBuilderView.vue` RSKU 选项与已选标签显示能力匹配状态（正常/警告）
  - [x] 更新 `QuoteServiceTest`、`AiMatchingServiceTest`、`RskuServiceTest`
  - [x] 后端测试 131 个全部通过，前端 lint + type-check 通过
- [x] 以图搜图效果优化（2026-07-01 完成）
  - [x] `RetrievalService` 图片查询时调用 `VisionService.recognizeImage` 提取查询图标签
  - [x] 将查询图风格、主色、材质、场景、六维标签与候选 RSPU 标签对比，按规则重排加分
  - [x] 实现跨 RSPU 同款去重：按 `category + style + color + material` 指纹保留向量分最高的代表
  - [x] `SimilarProductResponse` 增加 `aestheticsConfidence`，前端 `VisualSearchView.vue` 展示置信度与向量/综合得分
  - [x] 向量召回候选池从 `topK * 3` 扩大到 `topK * 5`
  - [x] `VisionService` 识别失败时降级为空标签，不影响向量检索
  - [x] 更新 `RetrievalServiceTest`，覆盖文本查询、图片标签加成、同款去重、AI 识别失败降级
  - [x] 后端测试 132 个全部通过，前端 lint + type-check 通过
- [x] 工厂 Excel 报价单批量导入（2026-07-01 完成）
  - [x] 新增 `RskuImportService`，使用 EasyExcel 解析 Excel
  - [x] 新增 `RskuImportRow` / `RskuImportResult` / `RskuImportFailure` DTO
  - [x] 校验工厂/RSPU/变体存在性、出厂价正数、产品等级有效、工厂能力等级匹配
  - [x] 支持「存在时跳过」与「存在时更新」两种模式
  - [x] `RskuSupplyMapper` 新增 `insertBatch` / `insertBatchSafe` 批量插入
  - [x] 新增 `RskuImportController`：`GET /api/v1/rsku/import-template` 下载模板，`POST /api/v1/rsku/import` 上传导入
  - [x] 前端新增 `RskuImportView.vue`：下载模板、上传 Excel、展示导入结果与失败明细
  - [x] 更新 `RskuImportServiceTest`，覆盖成功导入、跳过、更新、校验失败、空文件场景
  - [x] 后端测试 137 个全部通过，前端 lint + type-check 通过
- [x] 工厂 Excel 导入代码 Review 与隐藏问题修复（2026-07-01 完成）
  - [x] 修复审计日志 `logUpdate` 在更新后才快照，导致 oldValue == newValue 的问题
  - [x] 修复 `isValidProductLevel(null)` 可能触发 NPE 的问题
  - [x] 预加载工厂能力等级并缓存 `factory_level` 字典，避免每行重复查询
  - [x] 增加 Excel 内部重复行检测（同一工厂+变体），防止重复插入触发唯一索引冲突
  - [x] 批量插入失败时自动回退到逐行插入并捕获 `DataIntegrityViolationException`，提升并发导入容错
  - [x] 增加文件大小（≤10MB）与 MIME 类型（Excel/CSV）校验
  - [x] 扩展 `parseExcel` 异常处理，捕获格式错误并给出友好提示
  - [x] 更新 `RskuImportServiceTest`：新增重复行、审计快照、文件类型校验测试
  - [x] 后端测试 140 个全部通过
- [x] RSKU 报价导入模板中文化与业务边界校验增强（2026-07-01 完成）
  - [x] `RskuImportRow` 所有 `@ExcelProperty` 改为中文表头（RSPU编码、工厂编码、变体编码、出厂价等）
  - [x] `RskuImportService` 增加字段长度校验（RSPU/变体 ≤64、工厂编码 ≤16、工厂SKU ≤64 等）
  - [x] 增加数值边界校验：出厂价 ≤99,999,999.99、交期 0~999、MOQ 0~999,999、质保 0~50
  - [x] 支持中文值自动映射：报价置信度 `高/中/低` → `high/mid/low`，产品等级 `S级/S级战略厂` → `S`
  - [x] 导入前自动 trim 所有字符串字段
  - [x] 更新 `RskuImportServiceTest`：中文表头、中文值映射、边界校验测试
  - [x] 后端测试 142 个全部通过，前端 lint + type-check 通过
  - [x] 重启后端并验证 `/api/v1/rsku/import-template` 下载成功
- [x] 冒烟测试环境启动（2026-07-01 完成）
  - [x] 启动基础设施：PostgreSQL（5433）、ChromaDB（8000）、Redis（6379）、MinIO（9000/9001）
  - [x] 修复启动阻塞：`RskuSupplyMapper.xml` 的 DTD system ID 错误（`mybatis-3.0.dtd` → `mybatis-3-mapper.dtd`）
  - [x] 后端 `mvn spring-boot:run` 在 `:8081` 启动成功
  - [x] 前端 `pnpm dev` 在 `http://localhost:5173/` 启动成功
  - [x] `GET /api/v1/dicts/factory_level` 接口响应正常
  - [x] 补充工厂列表页入口：`FactoryListView.vue` 新增「批量导入 RSKU 报价」按钮，跳转 `/rsku/import
  - [x] 前端 lint + type-check 通过
- [x] Code Review P0 问题集中修复（2026-07-02 完成）
  - [x] 前端 FormData 上传：移除 `web/src/api/product.ts` / `rsku.ts` / `retrieval.ts` 中手动设置的 `Content-Type: multipart/form-data`，由 Axios 自动生成带 boundary 的 header
  - [x] 前端详情页路由刷新：`FactoryDetailView.vue` / `RskuDetailView.vue` 将路由参数改为 `computed`，并配合 `onBeforeRouteUpdate` 重新加载
  - [x] AI 搭配推荐风格过滤：`AiMatchingService.fetchCandidates` 改为按 `rspu_style.style_code` 过滤，语义正确
  - [x] 生产配置统一：`application-prod.yml` 统一使用 `rsdp.ai.*` / `rsdp.chromadb.*` 层级；`application.yml` 移除默认激活的 `spring.profiles.active: dev`
  - [x] 价格解密兜底：`AesEncryptionUtil.decrypt` 对历史遗留明文价格兜底返回并告警，避免未迁移数据导致查询崩溃
  - [x] 后端测试 143 个全部通过，前端 lint + type-check 通过
- [x] RSPU/RSKU 主键与业务编码分离决策（2026-07-02 完成）
  - [x] 明确风格、尺寸、材质、工厂均为可变属性，不适合写入主键
  - [x] 决策：`rspu_id` / `rsku_id` 继续使用 UUID 主键；业务编码仅作为展示字段（未来可新增 `rspu_code` / `rsku_code`）
  - [x] 多风格/多尺寸/多材质通过 `rspu_style` / `rspu_scene` / `rspu_variant` / `rsku_supply` 关联表表达
  - [x] 更新 `AGENTS.md`、`docs/06-reference/01-业务规则说明.md`、新增 ADR `004-为什么RSPU和RSKU使用UUID主键而非业务编码作为主键.md`
  - [x] 关闭「用业务编码替代 UUID 主键」的 P0 目标，`RspuCodeService` 作为可选能力保留
- [x] 产品人工批量导入（Excel）（2026-07-02 完成）
  - [x] 数据库：`rspu_master` 增加 `external_code` 字段及唯一索引，同步更新实体与 DDL/重置脚本
  - [x] 后端：`ProductImportRow` / `ProductImportResult` / `ProductImportFailure` DTO
  - [x] 后端：`ProductImportService`：EasyExcel 解析、字典预加载校验、RSPU/变体/图片写入、跳过/更新模式、图片下载失败仅记录明细
  - [x] 后端：`ProductImportController`：`GET /api/v1/products/import-template` 下载模板、`POST /api/v1/products/import` 上传导入
  - [x] 后端：扩展 `StorageService.store(InputStream, ...)`，本地与 MinIO 实现已补齐
  - [x] 后端：`ProductImportServiceTest` 覆盖创建、更新、跳过、重复行、字典校验、图片成功/失败、空文件、超大文件场景
  - [x] 前端：`types/product.ts` 增加 `ProductImportResult` / `ProductImportFailure`
  - [x] 前端：`api/product.ts` 增加 `downloadProductImportTemplate` / `importProducts`
  - [x] 前端：新增 `ProductImportView.vue`（`/products/import`），支持下载模板、上传 Excel、切换更新/跳过、展示结果与失败明细
  - [x] 前端：`ProductListView.vue` 增加「批量导入」按钮跳转导入页
  - [x] 后端测试 153 个全部通过，前端 lint + type-check 通过
  - [x] 更新 `docs/02-architecture/04-API设计.md`
- [x] 产品导入收尾：安全与健壮性加固（2026-07-02 完成）
  - [x] 文件类型校验改用文件魔数：新增 `ExcelFileValidator`，`ProductImportService.validateFile` 不再依赖 `Content-Type`
  - [x] 图片 URL SSRF 防护：新增 `ImageUrlValidator`，默认禁止 local / 内网 / 保留地址，支持 `rsdp.import.allowed-image-hosts` 白名单
  - [x] 图片 URL 不安全时单独记录「不安全的图片 URL」失败明细
  - [x] 修复 Vue Router 中 `/products/import` 与 `/products/:rspuId` 的路由顺序冲突
  - [x] 修复 `processRowInTransaction` 未捕获 `DataIntegrityViolationException` 的问题
  - [x] 修复图片存储失败未记录到导入结果的问题
  - [x] 消除 `normalizeDictCode` 中 `dictName.startsWith` 模糊匹配歧义
  - [x] 补充 `ExcelFileValidatorTest`、`ImageUrlValidatorTest`，扩展 `ProductImportServiceTest`
  - [x] 后端测试 167 个全部通过，前端 lint + type-check 通过
- [x] 搭配方案/报价单支持产品数量选择（2026-07-02 完成）
    - [x] 数据库 `scheme_item` 已存在 `quantity` 字段，补充 `SchemeItemMapper.xml` 批量插入
    - [x] 修复 `SchemeService.generateQuote` 按数量生成报价单；`buildItemResponse` 回填 quantity/subtotal
    - [x] `SchemeService.create/update` 对重复 `rskuId` 聚合数量，与 `QuoteService` 行为一致
    - [x] `AiMatchingService` AI 推荐项默认 quantity=1、subtotal=factoryPrice
    - [x] 更新 `QuoteServiceTest`、`QuoteExportServiceTest`、`SchemeServiceTest` 覆盖数量/小计/总数量
    - [x] 前端 `QuoteBuilderView.vue` 增加数量输入、小计显示、数量参与报价/导出/保存方案
    - [x] 前端 `SchemeDetailView.vue` 展示数量/小计/总数量，导出报价单携带数量
    - [x] 更新 `docs/02-architecture/04-API设计.md`
    - [x] 后端测试通过，前端 lint + type-check 通过
- [x] Code Review 遗留问题集中修复（2026-07-02 完成）
  - [x] `RskuImportService.validateFile` 改用文件魔数校验，复用 `ExcelFileValidator`
  - [x] 合并 JSONB TypeHandler：`JsonbTypeHandler` 与 `PostgreSqlJsonbTypeHandler` 统一为 `com.rsdp.config.typehandler.JsonbTypeHandler`，同时支持 `String` 与 `Object` 字段
  - [x] Redis 接入 Spring Cache：新增 `CacheConfig`，生产环境使用 Redis、开发环境回退 ConcurrentMap；`DictService.listByType` 增加 `@Cacheable("dicts")`
  - [x] 风格数据库表 DDL 同步：`init_db.sql` / `reset_db.sql` 补充 `style_case` / `style_element` / `style_matching_formula` / `product_style_match` / `matching_feedback`
  - [x] 统一 `init_db.sql` 文档引用路径为 `docs/02-architecture/02-数据库设计.md`
  - [x] 后端测试 170 个全部通过，前端 lint + type-check 通过
- [x] Code Review P1 治理项集中修复（2026-07-03 完成）
  - [x] 修复 `LocalStorageService` 目录遍历风险：绝对路径也校验不越界
  - [x] 治理 `FactoryService.listFactories` N+1 能力等级查询：批量查询后组装响应
  - [x] 治理 `SchemeService.getSchemeDetail` N+1 查询：批量查询 RSPU / RSKU / 工厂 / 主图
  - [x] `RskuImportService` 更新报价时同步写入 `price_history`
  - [x] `PriceEncryptionMigration` 改为 LIMIT/OFFSET 分批查询，避免 OOM
  - [x] 修复 `RskuController.updatePrice` 未校验路径 `rspuId`：`RskuService.updateRskuPrice` 增加 `rspuId` 归属校验，移除 Controller 重复查询
  - [x] 修复 `AsyncTaskProcessor` 向量写入失败仍标记任务成功：新增 `partial_success` 状态，区分 AI 识别成功但向量缺失场景；前端同步支持状态展示
  - [x] 修复 `VectorBackfillService` PG 与 ChromaDB 不一致：调整写入顺序为先写 ChromaDB、成功后写 PG
  - [x] 后端测试 182 个全部通过，前端 lint + type-check 通过
- [x] Code Review P1：新品录入与元数据更新增加字典码校验（2026-07-03 完成）
  - [x] `ProductService.createEntry` 注入 `DictService`，校验 `categoryCode` 存在于 `category` 字典
  - [x] `ProductQueryService.updateProduct` 统一校验 `positioningLabel`（style）、`materialTags`（material）、`sceneTags`（scene）、`productLevel`（factory_level）字典码
  - [x] 优化字典校验错误提示：风格/场景/材质/产品等级/品类使用业务语义文案
  - [x] 新增 `ProductServiceTest.createEntry_shouldRejectInvalidCategoryCode`、`ProductQueryServiceTest.updateProduct_shouldRejectInvalidPositioningLabel` / `updateProduct_shouldRejectInvalidSceneTag`
  - [x] 后端测试 185 个全部通过，前端 lint + type-check 通过
- [x] Code Review P1：`selectBatchIds` 空列表保护（2026-07-03 完成）
  - [x] `RskuService.toResponses`：无 RSKU 或无有效 `factory_code` 时跳过批量工厂查询
  - [x] `QuoteService.generateQuote` / `batchRspuMap` / `batchFactoryMap`：空 `rskuId` / `rspuId` / `factoryCode` 列表时返回空 Map 或抛业务异常
  - [x] `RetrievalService.searchSimilar` / `rerank`：空 `imageIds` / `rspuIds` 时跳过批量查询
  - [x] `RspuRelationService.toResponses`：空目标 `rspuId` 列表时跳过批量查询
  - [x] 新增 `RskuServiceTest.listByRspu_shouldReturnEmptyListWhenNoRsku`
  - [x] 后端测试 188 个全部通过
- [x] Code Review P1：`FactoryService.getFactoryCapableLevels` 对缺失工厂不再抛异常（2026-07-03 完成）
  - [x] 工厂不存在、已删除或 `factoryCode` 为空时返回空列表
  - [x] 调用方（`RskuService` / `QuoteService` / `AiMatchingService` / `RskuImportService`）按空列表语义处理
  - [x] 新增 `FactoryServiceTest` 工厂不存在 / 编码为空场景覆盖
  - [x] 后端测试 190 个全部通过
- [x] 前端新品录入页加固（2026-07-03 完成）
  - [x] `ProductEntryView.vue`：`AbortController` 取消未完成的图片上传和任务轮询请求
  - [x] 任务列表持久化到 `localStorage`，页面切换/刷新后可恢复并继续轮询
  - [x] `setInterval` 改为 chained `setTimeout`，避免轮询请求并发
  - [x] `beforeunload` 事件监听，上传/识别未结束时关闭页面弹出确认提示
  - [x] 前端 lint + type-check 通过
- [x] 用户自助扩展 material / scene 字典标签（2026-07-03 完成）
  - [x] 后端新增 `POST /api/v1/dicts`，仅允许扩展 `material` / `scene` 两类标签字典
  - [x] 后端校验 `dictCode`（字母数字，自动大写）、`dictName` 非空；重复时返回「字典项已存在」
  - [x] 创建成功后 `@CacheEvict("dicts")` 清除字典缓存，保证前端选项即时刷新
  - [x] 修复 `CategoryDict` 复合主键插入：将 `@TableId` 设置为 `IdType.INPUT`，避免 MyBatis-Plus 默认雪花算法覆盖 `dict_type`
  - [x] 后端新增 `DictServiceTest`（8 例）与 `DictControllerTest`（4 例）
  - [x] 前端 `api/dict.ts` 新增 `createDict`
  - [x] 前端 `ProductDetailView.vue` 编辑弹窗：材质标签 / 场景标签选择器旁增加「+ 新增」按钮
  - [x] 新增字典项弹窗，创建成功后自动刷新对应字典并选中新标签
  - [x] 更新 `docs/02-architecture/04-API设计.md`
  - [x] 后端测试 202 个全部通过，前端 lint + type-check 通过
- [x] Code Review P1：`AiMatchingService` 候选加载 SQL 化（2026-07-03 完成）
  - [x] `fetchCandidatesByCategory` 改为 SQL 层 `LIMIT 30` 并排除锚点产品，避免按品类全量加载
  - [x] 新增 `RskuSupplyMapper.selectCapableByRspuIds`：在 SQL 层过滤未删除工厂 + 工厂能力等级匹配
  - [x] `batchMinPrices` / `batchCheapestRskus` 直接消费 SQL 过滤后的 capable RSKU，移除内存里的 `filterCapableRskus`
  - [x] 移除 `AiMatchingService` 对 `FactoryService` 的依赖
  - [x] 更新 `AiMatchingServiceTest` 以覆盖新 Mapper 调用路径
  - [x] 后端测试 202 个全部通过
- [x] Code Review P1：`AsyncConfig` 线程池队列满策略优化（2026-07-03 完成）
  - [x] 默认拒绝策略由 `CallerRunsPolicy` 改为 `AbortPolicy`，避免队列满时阻塞 HTTP 线程
  - [x] 新增 `LoggingRejectedExecutionHandler`，任务被拒绝时记录 ERROR 日志与线程池状态
  - [x] 支持通过 `rsdp.async.rejected-policy` 切换策略（abort / discard / discard-oldest / caller-runs）
  - [x] `AdminController` 新增 `GET /api/v1/admin/async/metrics` 暴露线程池实时指标
  - [x] 新增 `AsyncConfigTest` 覆盖默认策略、可配置策略、拒绝行为
  - [x] 后端测试 205 个全部通过
- [x] P0 登录认证体系（2026-07-03 完成）
  - [x] 后端引入 `spring-boot-starter-security` + `jjwt`，实现 JWT 无状态认证
  - [x] 新增 `sys_user` 表与默认管理员初始化（`admin / admin123`）
  - [x] 新增 `SysUser` / `SysUserMapper` / `SecurityUserDetailsService` / `AuthService` / `AuthController`
  - [x] 新增 `JwtUtil` / `JwtAuthenticationFilter` / `SecurityConfig`，权限规则按角色控制写接口
  - [x] `AuditLogService` 自动从 `SecurityOperatorContext` 获取当前操作人，替代硬编码 `"admin"`
  - [x] 前端 `LoginView.vue` 登录页 + `useUserStore` Pinia 状态管理 + Axios 自动携带 token
  - [x] `App.vue` 显示当前用户/退出登录，登录页隐藏导航栏
  - [x] 新增 `AuthControllerTest` / `AuthServiceTest` / `SecurityUserDetailsServiceTest` / `JwtUtilTest`
  - [x] 更新 `WebMvcTest` 以兼容 Spring Security（`@AutoConfigureMockMvc(addFilters = false)` + mock filter）
  - [x] 更新 `docs/02-architecture/04-API设计.md`、`.env.example`、`docker-compose.yml`
  - [x] 后端测试 215 个全部通过，前端 lint + type-check 通过
- [x] RBAC 权限抽象层 + 最小用户管理后台（第一批基础改造，2026-07-03 完成）
  - [x] 数据库扩展：`sys_role`、`sys_permission`、`sys_role_permission`、`sys_user_role`、`sys_user_factory`
  - [x] `sys_user` 移除 `role` 字段，通过 `sys_user_role` 关联；`seed_data.sql` 初始化角色/权限/默认 admin
  - [x] 新增 `Permission` 常量类与 `PermissionService`（Spring Cache 缓存权限列表）
  - [x] `SecurityConfig` 由 `hasRole` 改为 `hasAuthority`，URL 按权限字符串控制
  - [x] 数据权限框架：`DataScope` / `DataScopeContext` / `DataScopeHelper`，RSKU 查询/更新/导入接入工厂数据权限
  - [x] 用户管理后端接口：`GET/POST/PUT /api/v1/admin/users`、`PUT /api/v1/admin/users/{userId}/reset-password` / `status`
  - [x] 后端 `LoginResponse` 返回 `roles` / `permissions`，`AuthService` 填充完整权限列表
  - [x] 前端 `useUserStore` 升级：`roles` / `permissions`、`hasRole` / `hasAnyRole` / `hasPermission` / `hasAnyPermission`
  - [x] 前端路由守卫支持 `meta.roles` 与 `meta.permissions` 双维度鉴权
  - [x] 新增 `UserManagementView.vue`（`/admin/users`）：用户列表、新增/编辑、禁用/启用、重置密码
  - [x] `App.vue` 仅 ADMIN 可见「用户管理」入口，用户下拉显示多角色
  - [x] 前端 `types/router.d.ts` 扩展 `RouteMeta` 类型
  - [x] 适配 `AuthControllerTest` / `AuthServiceTest`
  - [x] 后端测试 215 个全部通过，前端 lint + type-check 通过
  - [x] 本地启动后端需设置 `RSDP_JWT_SECRET` / `RSDP_ENCRYPTION_KEY`（`scripts/start-local.sh` 已注入 dev 默认值）

## 进行中

- [ ] 无

## 阻塞项

- 暂无

## 下一步

1. Code Review P1 治理项按优先级逐步推进
2. 风格数据库 Skill：表结构、导入接口、产品匹配与推荐集成
3. 其他功能优先级需结合业务方向再确定
