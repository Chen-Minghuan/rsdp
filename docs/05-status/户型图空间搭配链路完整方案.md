# 户型图 → 空间尺寸 → 产品搭配 链路完整落地方案

> 版本：v2.0（完整版）
> 基于 rsdp 仓库 main 分支（2026-08-10，commit dd1f2f6）代码扫描结果编写
> 目标：上传 CAD 导出图/简易平面图 → 系统识别空间划分与尺寸 → 按空间尺寸从产品库推荐产品搭配
> 前期范围：仅客厅（LIVING）空间

---

## 目录

1. 需求理解与总体设计
2. 现有代码资产盘点（复用清单）
3. 数据模型设计（DDL）
4. 后端模块设计（含类结构、接口契约、prompt 草稿）
5. 空间尺寸硬规则引擎
6. 前端页面设计
7. 产品库数据量评估与数据准备
8. 分阶段实施路线（含工时估算）
9. 测试计划
10. 风险清单与应对
11. 验收标准

---

## 1. 需求理解与总体设计

### 1.1 业务场景

员工/设计师拿到客户的户型图（CAD 导出的 PNG/JPG，或手机拍的简易平面图），上传到 RSDP。系统：

1. 识别图上有哪些空间（客厅、餐厅、卧室……），前期只关心**客厅**
2. 提取客厅的尺寸（开间 × 进深，单位 mm），以及面积
3. 员工确认/修正识别结果（人工校正环节，不可省略）
4. 选择风格偏好、预算上限（可选）
5. 系统从产品库中按**尺寸硬规则**筛选候选，再由 **LLM 按风格协调性终审**，输出一套客厅搭配方案
6. 方案落入现有 scheme 体系 → 可转报价单 → 可转订单（商业闭环已通）

### 1.2 总体链路

```
户型图(PNG/JPG)            人工校正(必经)           硬规则筛选           LLM 终审
      │                         │                      │                  │
      ▼                         ▼                      ▼                  ▼
┌──────────┐  bbox+OCR   ┌────────────┐   尺寸规则引擎 ┌───────┐  风格协调 ┌────────┐
│ 户型图解析│ ──────────▶ │ 空间尺寸表  │ ─────────────▶│ 候选集│ ───────▶│搭配方案 │
│FloorPlan │             │ (可编辑UI)  │               │(≤30条)│          │落scheme│
│Service   │             └────────────┘               └───────┘          └────────┘
└──────────┘
```

### 1.3 三条核心设计原则（与项目既有文化一致）

1. **AI 识别 + 人工校正，不赌全自动**
   PDF 导入链路 5 轮实测迭代的教训：AI bbox 输出存在格式抖动（越界、漏检、幻觉）。户型图识别属于同类问题，人工校正环节是链路可靠性的一部分，不是临时补丁。

2. **硬规则先行，LLM 终审**
   "沙发放不放得下"是几何数学问题，用规则引擎算；"这套产品搭不协调"是美学问题，交给 LLM。各司其职，不让 AI 做算术。

3. **输出落到现有 scheme 体系**
   搭配结果落 `scheme` + `scheme_item`（带 spaceTag），天然获得：空间分区视图、报价单生成、订单转换、分享画布。新功能只是多一个"方案生成入口"，不重造下游轮子。

---

## 2. 现有代码资产盘点（复用清单）

| 资产 | 位置 | 复用方式 | 复用度 |
|---|---|---|---|
| `VisionService.chatText()` | service/VisionService.java:1062 | 户型图检测的 AI 调用入口（含 response_format/max_tokens） | 直接用 |
| `VisionService.detectPageRegions()` 模式 | service/VisionService.java:556 | 仿写 `detectRoomRegions()`：bbox + 文字提取的完整范式 | 仿写 |
| `parseBoundingBoxClamped()` bbox 越界收敛 | VisionService 内 | 户型图空间框解析直接用（PDF 血泪教训已固化） | 直接用 |
| `PAGE_DETECTION_SYSTEM_PROMPT` | VisionService.java:467 | prompt 结构设计参考（输出契约、坐标系约定） | 参考 |
| `AiMatchingService.generateRoomScheme()` | service/AiMatchingService.java:84 | 搭配终审骨架：候选≤30 → LLM 选品 → reasoning | 扩展 |
| `RoomSchemeRequest`（roomType/budget/style） | dto/request/RoomSchemeRequest.java | 加 widthMm/depthMm 字段 | 扩展 |
| `rspu_variant.dimensions`（JSON 宽深高） | V1__init_db.sql:98 | 尺寸硬筛选数据源 | 直接用 |
| `SizeSpecParser` | util/SizeSpecParser.java | dimensions 缺失时从 sizeText 解析兜底 | 直接用 |
| scene 字典 `LIVING=客厅` | V1__seed_data.sql:849 | 空间筛选条件 | 直接用 |
| 品类字典 SF/TB/FC/FS/DT/BD/LT | V1__seed_data.sql | 客厅品类组合模板 | 直接用 |
| 风格库 style_case/style_matching_formula | V1__init_db.sql:686/721 | **仅覆盖客厅，恰好匹配前期范围** | 直接用 |
| `scheme`/`scheme_item`（spaceTag） | V1__init_db.sql:418/447 | 搭配结果落库 | 直接用 |
| scheme → quote → order | QuoteService/OrderService | 商业闭环 | 直接用 |
| async_task 异步任务 + 前端轮询 | AsyncTaskProcessor + stores | 户型图识别走异步，模式照抄录入页 | 仿写 |
| `ImageUploadValidator` | util/ | 上传校验 | 直接用 |
| `StorageService` 抽象 | service/storage/ | 户型图原图存储 | 直接用 |
| `entry/by-regions` 人工确认模式 | ProductService | 人工校正接口设计参考 | 参考 |
| `ProductDocumentImportView` 上传+轮询 UI | views/ | 前端页面骨架照抄 | 仿写 |

**结论：新建代码集中在「户型图解析服务 + 尺寸规则引擎 + 一个前端页面」，约占工作量 40%，其余 60% 是组装与扩展。**

---

## 3. 数据模型设计（DDL）

新增迁移脚本 `database/V34__floor_plan_module.sql`（三处同步纪律：V34 独立文件 + V1__init_db.sql + reset_db.sql，幂等可重复执行）：

```sql
-- V34: 户型图分析模块
-- 户型图分析批次（一次上传一条）
CREATE TABLE IF NOT EXISTS floor_plan_analysis (
    analysis_id      VARCHAR(64) PRIMARY KEY,          -- FPA-<UUID>（IdGenerator）
    image_id         VARCHAR(64) NOT NULL,             -- 户型原图，指向 image_assets
    status           VARCHAR(16) NOT NULL DEFAULT 'pending',  -- pending/analyzing/awaiting_confirm/confirmed/failed
    task_id          VARCHAR(64),                      -- 关联 async_task
    raw_result       JSONB,                            -- AI 原始识别结果（不动，留档）
    confirmed_rooms  JSONB,                            -- 人工校正后的空间列表（最终生效数据）
    scale_ratio      DECIMAL(10,4),                    -- 识别/人工确认的比例尺（像素:实际mm），可空
    error_message    TEXT,
    created_by       VARCHAR(64),
    created_at       TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
    updated_at       TIMESTAMP,
    FOREIGN KEY (image_id) REFERENCES image_assets(image_id)
);

-- 空间识别明细（每个识别出的空间一行，人工校正就地更新）
CREATE TABLE IF NOT EXISTS floor_plan_room (
    room_id          VARCHAR(64) PRIMARY KEY,          -- FPR-<UUID>
    analysis_id      VARCHAR(64) NOT NULL,
    room_type        VARCHAR(32) NOT NULL,             -- 引用 room_type 字典（LIVING/BEDROOM/...）
    bbox             JSONB,                            -- {x, y, w, h} 归一化坐标 [0,1]
    width_mm         INTEGER,                          -- 开间（人工校正后为准）
    depth_mm         INTEGER,                          -- 进深
    area_m2          DECIMAL(8,2),
    dimension_source VARCHAR(16),                      -- ocr_text / scale_calc / ai_estimate / manual
    dimension_confidence VARCHAR(8) DEFAULT 'low',     -- high/mid/low
    dimension_text   VARCHAR(128),                     -- 图上尺寸标注原文（如 "4200×3800"）
    sort_order       INTEGER DEFAULT 0,
    deleted_at       TIMESTAMP,                        -- 人工删除误识别空间
    created_at       TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
    updated_at       TIMESTAMP,
    FOREIGN KEY (analysis_id) REFERENCES floor_plan_analysis(analysis_id)
);
CREATE INDEX IF NOT EXISTS idx_fpr_analysis ON floor_plan_room(analysis_id) WHERE deleted_at IS NULL;

-- 搭配方案与户型分析的关联（方案溯源）
ALTER TABLE scheme ADD COLUMN IF NOT EXISTS analysis_id VARCHAR(64);
COMMENT ON COLUMN scheme.analysis_id IS '来源户型图分析批次，可空';
```

**数据归属**：`created_by` 记录创建人，查询走现有 DataScopeHelper 归属隔离模式（非 ADMIN 只能看自己的，与 async_task 一致）。

---

## 4. 后端模块设计

### 4.1 新增文件清单

```
server/src/main/java/com/rsdp/
├── controller/
│   └── FloorPlanController.java                # 新增
├── service/
│   ├── FloorPlanService.java                   # 新增：上传/触发分析/人工校正/落库
│   ├── FloorPlanMatchingService.java           # 新增：尺寸硬规则筛选 + 终审编排
│   └── chroma/ ...                             # 不动
├── dto/
│   ├── request/
│   │   ├── FloorPlanConfirmRequest.java        # 人工校正提交
│   │   └── FloorPlanSchemeRequest.java         # 搭配生成请求
│   └── response/
│       ├── FloorPlanAnalysisResponse.java      # 分析结果（含空间列表）
│       └── FloorPlanRoomResponse.java          # 单个空间
├── entity/
│   ├── FloorPlanAnalysis.java
│   └── FloorPlanRoom.java
├── mapper/
│   ├── FloorPlanAnalysisMapper.java
│   └── FloorPlanRoomMapper.java
└── util/
    └── RoomDimensionRules.java                 # 新增：尺寸硬规则引擎
```

修改文件：
- `VisionService.java`：新增 `detectRoomRegions()` 方法（~120 行，含 prompt 常量）
- `AsyncTaskProcessor.java`：新增 `floor_plan_analysis` 任务类型分支
- `SecurityConfig.java`：新增 `/api/v1/floor-plan/**` 授权规则
- `application.yml`：`rsdp` 段新增 `floor-plan:` 配置块

### 4.2 接口契约

| # | 方法 | 端点 | 权限 | 说明 |
|---|---|---|---|---|
| 1 | POST | `/api/v1/floor-plan/analyze` | `product:read`（登录即可，可再收紧） | 上传户型图（multipart），存图 → 建 analysis 记录 → 创建异步任务 → 返回 analysisId + taskId |
| 2 | GET | `/api/v1/floor-plan/{analysisId}` | 归属校验 | 查询分析状态与空间列表（前端轮询复用 task 模式） |
| 3 | PUT | `/api/v1/floor-plan/{analysisId}/rooms` | 归属校验 | 人工校正：整体提交确认后的空间列表（增删改尺寸），状态 → confirmed |
| 4 | POST | `/api/v1/floor-plan/{analysisId}/scheme` | `scheme:create` | 基于 confirmed 空间生成搭配方案，落 scheme + scheme_item |
| 5 | DELETE | `/api/v1/floor-plan/{analysisId}` | 归属校验 | 软删（@TableLogic 模式） |

**接口 1 请求/响应**：
```
POST /api/v1/floor-plan/analyze
Content-Type: multipart/form-data
  image: File（jpg/png，≤10MB，走 ImageUploadValidator）
  hint: String 可选（用户补充说明，如"这是三室两厅"）

→ Result<{analysisId, taskId}>
```

**接口 3 请求**（人工校正，整体替换语义）：
```json
{
  "rooms": [
    {
      "roomId": "FPR-xxx",           // 已有空间带 id，新增不带
      "roomType": "LIVING",
      "widthMm": 4200,
      "depthMm": 3800,
      "bbox": {"x": 0.12, "y": 0.3, "w": 0.4, "h": 0.35}
    }
  ],
  "scaleRatio": null
}
```

**接口 4 请求**：
```json
{
  "roomId": "FPR-xxx",              // 目标空间（前期固定传客厅那条）
  "stylePreference": "WJ",          // 可空
  "budgetLimit": 30000,             // 可空
  "projectId": "PROJ-xxx"           // 可空，挂到设计项目
}
→ Result<{schemeId}>   // 落库后跳转方案详情页
```

### 4.3 AI 识别设计（VisionService.detectRoomRegions）

**输入处理**：图片长边 >2688 时等比缩放（沿用 DETECT_IMAGE_MAX_EDGE 经验值，户型图文字标注较小，分辨率给足），走 ImageResizer。

**System Prompt 草稿**：
```
你是建筑户型图分析专家。请分析这张户型平面图，识别其中的各个功能空间。

只输出 JSON，不要任何其他文字。输出格式：
{
  "rooms": [
    {
      "roomType": "living_room",
      "bbox": {"x": 0.12, "y": 0.30, "w": 0.40, "h": 0.35},
      "dimensionText": "4200×3800",
      "label": "客厅"
    }
  ],
  "scaleText": "1:50"
}

规则：
1. roomType 只能从以下枚举选择：living_room(客厅) / dining_room(餐厅) / bedroom(卧室) / kitchen(厨房) / bathroom(卫生间) / balcony(阳台) / study(书房) / hallway(过道) / other
2. bbox 为归一化坐标，x,y 为左上角，w,h 为宽高，取值范围 [0,1]，框要紧贴该空间的墙体边界
3. dimensionText 提取该空间内或附近标注的尺寸文字原文（如 "4200×3800"、"4.2m*3.8m"、"4200"），没有标注则为 null
4. 每个空间单独一个框，不得合并多个空间，不得遗漏闭合空间
5. 图上如有比例尺标注（如 "1:50"、"1:100"），提取到 scaleText，没有则为 null
```

**解析与容错**（全部沿用 PDF 链路成熟模式）：
- `response_format: json_object` + `max_tokens 4096`
- bbox 解析走 `parseBoundingBoxClamped`（越界收敛，不整框丢弃）
- JSON 解析失败 → 抛「解析 AI 识别结果失败」独立异常 → 异步任务标 failed，前端可 force 重试
- 识别结果 `rooms` 为空 → 状态置 awaiting_confirm（允许用户纯手工圈选录入，前端画框）

**尺寸提取三级优先级**（FloorPlanService 内实现）：

| 优先级 | 来源 | 实现 | confidence |
|---|---|---|---|
| ① | 图上尺寸标注 OCR | 解析 dimensionText：正则 `(\d{3,5})\s*[×x*]\s*(\d{3,5})` → mm；带单位换算（复用 `inferSizeCode` 的 cm×10/m×1000 换算逻辑，建议抽成 `util/Dimensions.java` 公共方法） | high |
| ② | 比例尺换算 | scaleText 有值 + bbox 像素尺寸：量取图上标注的某个已知尺寸线段像素长 → 推算像素:mm 比例 → 应用到各空间 bbox | mid |
| ③ | AI 估算 | 无标注无比例尺时，按常见户型经验由 LLM 估算（prompt 单独一轮轻量调用） | low |

人工校正后 `dimension_source = manual`，`confidence = high`。

### 4.4 异步任务接入

`async_task.task_type` 新增枚举值 `floor_plan_analysis`（task_type 是 VARCHAR，无需迁移）。`AsyncTaskProcessor` 新增分支：

```java
case "floor_plan_analysis" -> processFloorPlanAnalysis(task);
```

处理步骤：读 input_data 的 analysisId → 取图 → `detectRoomRegions` → 尺寸三级提取 → 写 floor_plan_room 列表 → analysis 状态 → awaiting_confirm。复用 `claimPendingTask` 原子认领防竞争（P0 修复已落地）。

### 4.5 配置项（application.yml `rsdp` 段新增）

```yaml
rsdp:
  floor-plan:
    detect-image-max-edge: 2688
    max-file-size-mb: 10
    rules:                        # 尺寸硬规则，可运维调整
      sofa-wall-ratio: 0.75       # 沙发长度 ≤ 沙发墙 × 0.75
      sofa-tea-min-mm: 350        # 沙发→茶几最小通道
      sofa-tea-max-mm: 450
      walkway-min-mm: 600         # 茶几→电视柜最小过人通道
      tv-cabinet-wall-ratio: 0.8
      small-living-area-m2: 12    # 小客厅分界
      small-living-sofa-max-mm: 2200
      large-living-area-m2: 20
```

---

## 5. 空间尺寸硬规则引擎（RoomDimensionRules）

### 5.1 客厅品类组合模板

```java
// 客厅方案必选/可选品类（品类码来自 category 字典）
LIVING_TEMPLATE = {
  required: ["SF"],              // 沙发（必选）
  optional: ["TB", "FC", "FS"],  // 茶几、电视柜(柜类)、休闲椅(座椅)
  maxPerCategory: {SF: 1, TB: 1, FC: 1, FS: 2}
}
```

### 5.2 筛选规则（按优先级依次执行）

```
输入：RoomDimension(widthMm, depthMm) + 品类候选集
│
├─ R1 面积分档
│    area < 12㎡  → 沙发长度上限 2200mm，不推 L 型
│    12~20㎡      → 三人位为主（1800~2400mm）
│    > 20㎡       → 可 L 型 / 3+1 组合
│
├─ R2 沙发长度约束
│    sofaWidth ≤ min(墙长 × 0.75, 开间 - 600mm过道)
│    （假设沙发墙 = 开间方向；人工校正 UI 允许用户指定朝向，P1 做）
│
├─ R3 深度核算（进深方向链式校验）
│    沙发深度 + 通道(350~450) + 茶几宽度 + 通道(≥600) + 电视柜深度 ≤ 进深
│    不满足 → 依次降级：去电视柜 → 换窄茶几 → 换小沙发
│
├─ R4 品类过滤
│    scene 含 LIVING（rspu_scene 关联）且 category_code 命中模板
│
└─ R5 数据质量过滤
     dimensions 非空（sizeText 经 SizeSpecParser 可解析的也算）
     且有 ≥1 条有效 RSKU 报价（rsku_supply 未软删）→ 保证可转报价单
```

**输出**：每品类 ≤6 条候选（按 product_style_match 风格得分 + 创建时间排序），总量 ≤30（对齐 AiMatchingService.MAX_CANDIDATES）。

### 5.3 LLM 终审（扩展 AiMatchingService）

`generateRoomScheme` 新增重载，prompt 中注入空间尺寸上下文：

```
你是家具搭配专家。客户客厅尺寸为 {widthMm}mm × {depthMm}mm（约 {area}㎡）。
请从候选产品列表中挑选一套客厅搭配：1 款沙发 + 0~1 款茶几 + 0~1 款电视柜 + 0~2 款休闲椅。
候选产品均已通过尺寸校验，你只需判断风格、颜色、材质的搭配协调性。
{预算约束段（budgetLimit 非空时）}
输出 JSON：{"rspuIds": [...], "reasoning": "..."}
```

**降级链**：LLM 返回空 → 规则兜底（每品类取风格匹配分最高者组合，reasoning 注明"规则推荐"），保证永远有结果。

### 5.4 方案落库

复用 `SchemeService.createScheme`：scheme 名自动生成「{客户/日期}客厅方案」，`scheme_item.spaceTag = 'LIVING'`，价格取 RSKU 最低价快照（沿用 copyFromTemplate 的价格快照语义），`scheme.analysis_id` 回填溯源。

---

## 6. 前端页面设计

### 6.1 新增页面 `FloorPlanEntryView.vue`（路由 `/floor-plan`）

导航挂载：「搭配方案 ▾」分组新增「户型图搭配」（对齐 navigation.ts 声明式配置模式）。

页面流程（四步向导，照抄 ProductExcelAiImportView 的 NSteps 模式）：

```
步骤1 上传          步骤2 空间识别        步骤3 搭配生成        步骤4 完成
┌──────────┐     ┌──────────────┐     ┌──────────────┐     ┌──────────┐
│ 拖拽上传   │     │ 原图+bbox叠加 │     │ 风格/预算选择 │     │ 方案卡片  │
│ 户型图     │ ──▶ │ 空间列表表格  │ ──▶ │ 目标空间确认  │ ──▶ │ 跳转方案  │
│ 可选提示语 │     │ (可编辑尺寸)  │     │ 生成按钮     │     │ 详情页    │
└──────────┘     └──────────────┘     └──────────────┘     └──────────┘
```

**步骤2 关键交互**（人工校正，本功能的可靠性核心）：
- 左侧：户型原图 canvas 叠加空间 bbox（编号框，可点选高亮；可手绘新框 → 新增空间行）
- 右侧：空间表格（vxe-table 复用）——空间名（下拉，room_type 字典）/ 宽(mm) / 深(mm) / 面积(自动算) / 尺寸来源标签（OCR/比例尺/AI估算/人工，低置信度黄色警示）/ 删除
- 「确认识别结果」→ 调接口 3 → 状态 confirmed

**步骤3**：风格下拉（style 字典）+ 预算输入 + 「生成搭配」→ 接口 4 → 轮询 → 跳方案详情页（SchemeDetailView 空间分区视图现成）。

### 6.2 类型与 API 封装

- `web/src/types/floorPlan.ts`：FloorPlanAnalysis / FloorPlanRoom / ConfirmRequest / SchemeRequest
- `web/src/api/floorPlan.ts`：analyze / getAnalysis / confirmRooms / generateScheme（走 client.ts，带 signal 参数，对齐现有 API 层规范）
- 轮询复用 task.ts 现有模式 + `useRequestAbort`

---

## 7. 产品库数据量评估与数据准备

### 7.1 数据量分级

**最小可用（能演示、效果不丢人）：约 50~80 个 RSPU**

| 品类（码） | 数量 | 尺寸覆盖要求 |
|---|---|---|
| 沙发 SF | 15~20 | 单人(<1m) / 双人(1.5~1.8m) / 三人(2~2.4m) / L型(>2.8m) 四档都要有 |
| 茶几 TB | 10~15 | 覆盖 1.0~1.4m 主流长度 |
| 电视柜 FC | 8~10 | 覆盖 1.6~2.4m |
| 休闲椅 FS | 5~8 | — |
| 边几（可并入 TB/FC） | 5~8 | — |

**舒适可用（搭配有选择感）：150~300 个 RSPU**
- 覆盖 3~5 种主流风格（建议：现代简约、侘寂、法式复古、原木/北欧、新中式）
- **每风格 × 每品类 ≥ 3 款**，否则风格筛选后经常凑不齐一套，LLM 终审被迫返回空

### 7.2 每条数据的必备字段（缺一就掉出链路）

1. **`dimensions`（宽深高 mm）——本链路的命门**。缺失优先级处理：sizeText 经 SizeSpecParser 自动解析 → 仍缺的列入补录清单
2. 主图（搭配展示）
3. 风格码 `rspu_style` 关联（AI 识别已自动打）
4. 场景关联 `rspu_scene = LIVING`（AI 识别已自动打，注意核验：部分产品可能只打了其他场景）
5. ≥1 条有效 RSKU 报价（否则方案无法转报价单）

### 7.3 数据体检 SQL（开工前先跑）

```sql
-- 1) 品类分布
SELECT category_code, count(*) FROM rspu_master
WHERE deleted_at IS NULL GROUP BY 1 ORDER BY 2 DESC;

-- 2) 尺寸非空率（命门指标，目标 ≥90%，按品类分组看）
SELECT m.category_code,
       count(*) AS total,
       count(v.dimensions) FILTER (WHERE v.dimensions IS NOT NULL) AS has_dim
FROM rspu_master m
LEFT JOIN rspu_variant v ON v.rspu_id = m.rspu_id AND v.deleted_at IS NULL
WHERE m.deleted_at IS NULL
GROUP BY 1;

-- 3) 客厅场景覆盖
SELECT count(DISTINCT m.rspu_id) FROM rspu_master m
JOIN rspu_scene s ON s.rspu_id = m.rspu_id
WHERE s.scene_code = 'LIVING' AND m.deleted_at IS NULL;

-- 4) 有报价率
SELECT count(DISTINCT m.rspu_id) FROM rspu_master m
JOIN rspu_variant v ON v.rspu_id = m.rspu_id AND v.deleted_at IS NULL
JOIN rsku_supply r ON r.variant_id = v.variant_id AND r.deleted_at IS NULL
WHERE m.deleted_at IS NULL;
```

### 7.4 补数最快路径

用你们已有的 **Excel AI 导入 + PDF 画册导入** 灌真实工厂数据——尺寸列会被 `SizeSpecParser` 自动展开成结构化 dimensions，风格/场景/六维由 AI 识别自动打标，这正是过去两周迭代的成果，补数成本极低。建议从展厅常备的 3~5 家工厂报价表/画册开始灌。

---

## 8. 分阶段实施路线（含工时估算）

### P0：最小闭环（3~4 天）——先看到效果

| 任务 | 内容 | 工时 |
|---|---|---|
| 数据层 | V34 迁移（2 张表 + scheme 加列），三处同步 | 0.5d |
| VisionService | `detectRoomRegions()` + prompt + 解析容错 + 单测 | 1d |
| FloorPlanService | 上传/异步任务/尺寸三级提取/人工校正接口 + 单测 | 1d |
| 尺寸规则引擎 | RoomDimensionRules + 候选筛选 + 单测 | 0.5d |
| 搭配编排 | AiMatchingService 扩展重载 + 降级链 | 0.5d |
| 前端 | FloorPlanEntryView 四步向导（bbox 叠加用简单 div 绝对定位即可，canvas 手绘框放 P1） | 1d |

**P0 验收**：上传一张真实户型图 → 识别出客厅 → 人工确认尺寸 → 生成一套 3~5 件的搭配方案并落 scheme。

### P1：体验完善（2~3 天）

- 前端 canvas 手绘框（误识别空间修正/纯手工录入）
- 沙发墙朝向指定（影响 R2 规则的墙长取值）
- 平面图上叠加产品摆放示意（纯前端，按产品宽度比例画占位矩形）
- 识别失败 force 重试（复用现有 AbortController 模式）
- analysis 历史列表页

### P2：能力扩展（按需排期）

- PDF 户型图（复用 PdfImportService 的 PDFBox 渲染）
- 比例尺自动识别 + 像素测量工具
- 多空间批量搭配（餐厅/卧室，需补 style_case 数据）
- DXF 原生解析（侧车 Python ezdxf 服务；**DWG 不做**）
- 识别效果实测迭代（参照 PDF 裁剪的"实测→诊断→修复"循环，预期 3~5 轮）

---

## 9. 测试计划（对齐项目现有测试纪律）

| 测试类 | 覆盖点 |
|---|---|
| `VisionServiceTest` +3 | detectRoomRegions 请求契约（response_format/maxTokens）、多空间 bbox 解析、越界 bbox 收敛 |
| `FloorPlanServiceTest` +5 | 上传建单、OCR 尺寸提取（4200×3800/4.2m*3.8m/无标注三例）、比例尺换算、人工校正状态机、归属校验 |
| `RoomDimensionRulesTest` +8 | R1 面积分档三档、R2 沙发上限、R3 深度链式校验（含降级）、R4 场景过滤、R5 无报价剔除 |
| `AiMatchingServiceTest` +2 | 尺寸上下文注入 prompt、LLM 空返回规则兜底 |
| 全量回归 | `mvn test` 基线 888 全绿 + 新增全绿；前端 type-check + ESLint + build 三件套 |
| 实测（最重要） | 5~10 张真实户型图（不同来源：CAD 导出图/手绘拍照/中介户型图）跑全流程，记录识别准确率，预期触发 2~3 轮 prompt/规则迭代 |

---

## 10. 风险清单与应对

| 风险 | 等级 | 应对 |
|---|---|---|
| 户型图空间划分识别不准 | 高 | 人工校正必经 + P1 手绘框兜底；prompt 预留 2~3 轮迭代预算 |
| 尺寸标注 OCR 误读（如把门宽当开间） | 中 | 尺寸来源标注置信度，低置信度 UI 黄色警示强制人工看一眼 |
| 产品库 dimensions 缺失率高 | 中 | 开工前跑体检 SQL；缺失严重的品类先补数再开发规则引擎 |
| 客厅品类候选不足导致 LLM 返回空 | 中 | 规则兜底降级链，永远有输出 |
| 用户上传 DWG/DXF 期待直接解析 | 低 | 上传校验只放 jpg/png + 引导文案「CAD 请导出图片」 |
| DashScope 欠费/限流（8月已发生一次） | 低 | 异步任务失败可 force 重试；中期迁 Ollama 已在线路图上 |

---

## 11. 验收标准（Definition of Done）

1. 上传 CAD 导出的户型图 PNG，系统识别出客厅并给出尺寸（OCR 有标注时准确率 ≥80%，以 10 张真实图实测为准）
2. 人工校正界面可改尺寸/删空间/补空间，确认后状态流转正确
3. 输入 4200×3800 客厅，输出搭配方案：沙发长度 ≤3150mm（R2 规则生效），全套含沙发+茶几，风格一致，reasoning 可读
4. 方案落 scheme 后可正常打开详情页、可转报价单，报价单价格与 RSKU 快照一致
5. 后端全量测试零失败，前端三件套通过，实体-DB 对账零差异
6. 文档同步：`04-API设计.md` 补 5 个接口、AGENTS.md 迁移列表补 V34、权限矩阵补 /floor-plan/** 段
